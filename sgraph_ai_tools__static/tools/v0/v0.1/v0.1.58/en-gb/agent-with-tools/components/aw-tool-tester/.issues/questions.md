- how can an individual component can be loaded and tested independently, i.e. we need a way to be able to easily and (ideally from the UI) be able to open a page which receives as a param the component to load/test and shows an UI that allows the easy testing and visualising of that the component is doing (and its dependencies, which is why each component needs to have a manifest)