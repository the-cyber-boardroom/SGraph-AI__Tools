# Tool API Primitive — Adoption Status

**Parent:** `v0.1.91__tool-api__index.md`
**Date:** 2026-04-18

---

## Implemented (Reference)

| Tool | Version | Methods | SKILL files | Notes |
|------|---------|---------|-------------|-------|
| `infographic-gen` | v0.1.37 | 10 | 3 (human, browser, api) | Full reference implementation |

**infographic-gen methods:** `connect`, `generate`, `getState`, `getGenerations`, `setSystemPrompt`, `getSystemPrompt`, `setPrompt`, `getPrompt`, `setModel`, `getModel`, `setTemplate`, `stop`

---

## Not Yet Implemented

All other active tools. Priority order based on complexity + value:

| Tool | Complexity | Suggested methods | Notes |
|------|------------|-------------------|-------|
| `chat` | Medium | `connect`, `send`, `getHistory`, `clearHistory`, `setModel`, `stop` | Highest daily use |
| `llm-dev` | Medium | `connect`, `send`, `getState`, `setSystemPrompt`, `setModel` | Developer tool — agentic driving would be high value |
| `one-shot-chat` | Low | `connect`, `send`, `getState` | Simpler than chat |
| `agentic` | High | `connect`, `start`, `stop`, `getState`, `getLog` | Tool-calling loop — API needed for testing |
| `openrouter` | Medium | `setKey`, `getBalance`, `getModels`, `getUsage` | Admin dashboard |
| `vault` | Medium | `connect`, `listFiles`, `readFile`, `writeFile` | Vault browser |
| `playbooklm` | Unknown | TBD | New — assess after stabilising |
| `speed-test` | Low | `run`, `getResults` | Simple trigger/read pattern |
| `folder-editor` | Medium | `open`, `listFiles`, `readFile`, `writeFile`, `getState` | VFS-backed |

---

## Architect Notes

**Why this matters:** Once a tool has the API, it can be:
- Driven by Playwright for CI smoke tests with zero UI dependencies
- Called by other tools (e.g. agentic loop driving infographic-gen headlessly)
- Scripted from the browser console during development
- Documented via auto-discovered SKILL files that LLM agents can read

**The pattern scales.** Every tool gets the dev panel (console/explorer/manifest) for free
the moment it calls `activate()`. No per-tool UI work needed.

**Key invariant:** The UI must never be the only path to tool functionality. If something
can only be done through clicking the UI, it is not yet part of the API.

---

## Dev Notes

**Effort per tool:** ~1–3 hours to add the API layer to an existing tool.
- The implementation functions already exist — `register()` wraps them.
- The hard part is writing complete SKILL files (especially SKILL-api.md).

**Recommended approach:** Add API to one tool per sprint alongside normal IFD work.
Do not retrofit all tools at once — the infographic pattern will evolve slightly
as more tools adopt it.

**Next candidate:** `chat` — highest traffic, methods are obvious from existing UI.
