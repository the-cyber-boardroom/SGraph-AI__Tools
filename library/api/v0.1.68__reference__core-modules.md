# Core Module API Reference

**Version:** v0.1.68 | **Date:** 8 April 2026 | **Source:** `grep export` on all source files
**Total:** 15 families, 33 JS files

All paths relative to `sgraph_ai_tools__static/core/`.

---

## sg-crypto — `core/crypto/v1/v1.0/v1.0.0/sg-crypto.js`

AES-256-GCM file encryption via Web Crypto API. Wire-compatible with SG/Send production.

**Exports:** `ALGORITHM`, `KEY_LENGTH`, `IV_LENGTH`, `isAvailable()`, `requireSecureContext()`, `generateKey()`, `exportKey(key)`, `importKey(raw)`, `encryptFile(key, data)`, `decryptFile(key, data)`, `bufferToBase64Url(buf)`, `base64UrlToBuffer(str)`

---

## sg-ssh — `core/ssh/v1/v1.0/v1.0.0/sg-ssh.js`

**Exports:** `generateSSHKeyPair()` — Ed25519 only.

---

## sg-pbkdf2 — `core/pbkdf2/v1/v1.0/v1.0.0/sg-pbkdf2.js`

**Exports:** `deriveKey(password, salt, iterations)`, `deriveKeyFromWords(words, salt, iterations)`, `calculateEntropy(words, wordlistSize)`

---

## sg-wordlist — `core/wordlist/v1/v1.0/v1.0.0/sg-wordlist.js`

**Exports:** `getWordList(locale)`, `getRandomWords(n, locale)`, `searchWords(prefix, locale)`, `SUPPORTED_LOCALES`
**Data:** `wordlists/en-gb.json` (~1954 words)

---

## sg-vault-client — Read-Only Vault Access

### v1.0.0 — `core/vault-client/v1/v1.0/v1.0.0/sg-vault-client.js`
Flat pointer model. **Exports:** `parseVaultKey()`, `deriveVaultKeys()`, `deriveFileId()`, `openVault()`, `fetchTree()`, `readFile()`, `deriveFileIdForPath()`, `fetchSettings()`, `listFileIds()`

### v1.1.0 — `core/vault-client/v1/v1.1/v1.1.0/sg-vault-client.js`
Self-describing IDs, batch reads, encrypted metadata. **Additional exports:** `deriveFileIdHex()`, `readFileAsJson()`, `batchRead()`, `fetchSubTree()` + re-exports from `vault-id-utils.js`: `parseFileId()`, `formatFileId()`, `fileIdToPath()`, `shortId()`, `looksLikeRef()`, `decryptField()`, `decryptEncFields()`

---

## vault-write — `core/vault-write/v1/v1.0/v1.0.0/sg-vault-write.js`

Vault write operations. **Exports:** `IV_LENGTH`, `writeVaultFile(vault, filePath, content, options)`, `deriveWriteKeys(passphrase, vaultId)`, `deriveWriteKeysFromSimpleToken(token)`

---

## sg-send-crypto — `core/send-crypto/v1/v1.0/v1.0.0/`

Three files in this directory:

| File | Exports |
|------|---------|
| `sg-send-crypto.js` | `isFriendlyToken()`, `generateToken()`, `deriveTransferId()`, `deriveKey()`, `buildSgmetaEnvelope()`, `encrypt()`, `SALT`, `ITERATIONS`, `TOKEN_PATTERN` |
| `sg-send-upload.js` | `uploadFile(opts)`, `DIRECT_UPLOAD_LIMIT` |
| `wordlist.js` | `WORDLIST` (320-word English wordlist) |

---

## sg-layout — `core/sg-layout/v0.1.0/`

Fractal window management Web Component (2163 lines).

| File | Exports |
|------|---------|
| `sg-layout.js` | `SgLayout` (Web Component), `SGL_EVENTS` (re-export) |
| `sg-layout-events.js` | `SGL_EVENTS` (frozen, 20 events: register, panel lifecycle, tabs, drag, commands, title/focus/close) |
| `sg-layout.css` | CSS custom properties (theme) |

**Evolution:** v0.1.0 row/col + resize → v0.1.1 tab stacks → v0.1.2 fractal nesting → v0.1.3 drag-to-dock + panel locking + closable + fixedSize

---

## sg-tree — `core/sg-tree/v0/v0.1/`

Tree view Web Component with IFD surgical overrides (version-stamped filenames).

| File | Version | Exports |
|------|---------|---------|
| `v0.1.0/sg-tree.js` | v0.1.0 | `SgTree` (Web Component) — base class |
| `v0.1.0/sg-tree-events.js` | v0.1.0 | `SGT_EVENTS` (frozen event constants) |
| `v0.1.2/sg-tree--v0.1.2.js` | v0.1.2 | Surgical override — prototype patches |
| `v0.1.3/sg-tree--v0.1.3.js` | v0.1.3 | Surgical override |
| `v0.1.4/sg-tree--v0.1.4.js` | v0.1.4 | Surgical override |

---

## sg-vfs — `core/sg-vfs/v0/v0.1/v0.1.0/`

Virtual File System with pluggable providers.

| File | Exports |
|------|---------|
| `sg-vfs-core.js` | `VirtualFileSystem` class |
| `sg-vfs-events.js` | `SGL_VFS` (frozen event constants) |
| `sg-vfs-path.js` | `normalisePath()`, `joinPath()`, `dirname()`, `basename()`, `extname()` |
| `sg-vfs-provider-base.js` | `VfsProviderBase` class |
| `sg-vfs-provider-memory.js` | `MemoryProvider` extends VfsProviderBase |
| `sg-vfs-provider-indexeddb.js` | `IndexedDbProvider` extends VfsProviderBase |
| `sg-vfs-provider-layered.js` | `LayeredProvider` extends VfsProviderBase |
| `sg-vfs-request-id.js` | `newRequestId()`, `requestCount()` |

---

## sg-sandbox — `core/sg-sandbox/v0/`

Python sandbox via Pyodide for tool execution.

| Version | Exports |
|---------|---------|
| v0.1.0 | `SGSandbox` class |
| v0.2.0 | `SGSandbox` class (updated) |

---

## sg-send-sim — `core/sg-send-sim/v0/v0.1/v0.1.0/sg-send-sim.js`

In-memory SG/Send API simulator for testing.

**Exports:** `SIM_VFS_ROOT`, `generateVaultId()`, `createSimVault(vfs, passphrase, vaultId)`, `openSimVault`, `writeSimFile()`, `readSimFile()`, `readSimFileAsJson()`, `getSimHead()`, `getSimCommitLog()`, `listSimObjects()`

---

## sg-image — `core/image/v1/v1.0/v1.0.0/sg-image.js`

**Exports:** `getImageInfo()`, `supportsAvif()`, `crop()`, `resize()`, `redact()`, `addText()`, `flattenTransparency()`, `renderOperations()`, `exportImage()`

---

## sg-video — `core/video/v1/v1.0/v1.0.0/sg-video.js`

FFmpeg WASM wrapper (~30 MB lazy-load). **Exports:** `loadFFmpeg()`, `getVideoInfo()`, `splitVideo()`, `trimVideo()`, `extractAudio()`, `extractVideo()`, `parseTime()`, `formatTime()`, `isWasmSupported()`

---

## sg-pyodide — `core/pyodide/v1/v1.0/v1.0.0/sg-pyodide.js`

**Exports:** `loadPyodide()`, `installPackages()`, `runPython()`, `runPythonCapture()`, `isPackageAvailable()`, `checkVaultCompatibility()`, `deriveVaultKeysPython()`, `getStatus()`

---

*Core module API reference produced: 8 April 2026.*
