# Session Record — Dev Explorer Start

**Date:** 19 Mar 2026
**Role:** Developer (Explorer team)
**Session:** `claude/start-tools-explorer-session-WariD`
**Base version:** v0.1.4 (updated after merge from dev, 19 Mar 2026)

---

## Session Objective

Start a new Developer explorer session to assess the current state of the tools.sgraph.ai codebase, identify what's been shipped since the last session, and determine priorities for the next iteration.

---

## What Exists Today (Dev Perspective)

### Core Modules (9 shipped)

| Module | Version | Dev Assessment |
|---|---|---|
| sg-crypto.js | v1.0.0 | Stable. Wire-compatible with Send production. Missing input validation on `encryptFile()`/`decryptFile()`. |
| sg-ssh.js | v1.0.0 | Functional. Ed25519 only (Send also supports RSA-4096). Fingerprint calculation still inline in tool, not extracted. |
| sg-pbkdf2.js | v1.0.0 | New. PBKDF2 key derivation for friendly passphrases. |
| sg-wordlist.js | v1.0.0 | New. ~1954 en-gb words for key generation. |
| sg-vault-client.js | v1.0.0, v1.1.0 | v1.0.0: base vault client. v1.1.0: adds vault-id-utils. 40+ test assertions. |
| sg-pyodide.js | v1.0.0 | New. Lazy Pyodide loader (~20 MB). Experimental. |
| sg-layout.js | v0.1.0 (features up to v0.1.4) | **Major new module.** Fractal window management Web Component. ~2100 lines. Row/column layout, tab stacks, drag-to-dock, resize handles, panel locking, closable tabs, fixedSize panels. Shadow DOM. |
| sg-layout-events.js | v0.1.0 | Event name constants for sg-layout (16 event types). |
| sg-layout.css | v0.1.0 | CSS custom property defaults for sg-layout theming. |

### Components (10 shipped)

| Component | Version | Dev Assessment |
|---|---|---|
| sg-header.js | v1.0.0 | Superseded by sg-site-header. |
| sg-footer.js | v1.0.0 | Superseded by sg-site-footer. |
| sg-component.js | v1.0.0 | Base class with Shadow DOM and resource loading. |
| sg-tokens.css | v1.0.0 | Brand tokens for all sgraph.ai domains. |
| sg-site-header.js | v1.0.0, v1.0.1 | v1.0.0 frozen. v1.0.1 adds locale slot. |
| sg-site-footer.js | v1.0.0 | Shared footer with configurable columns. |
| sg-locale-picker.js | v1.0.0, v1.0.1 | v1.0.1 fixes Shadow DOM `[hidden]` bug. |
| sg-upload-dropzone.js | v1.0.0 | Drag-and-drop file upload with visual feedback. |
| sg-key-generator.js | v1.0.0 | Friendly key creation with entropy visualisation. |
| sg-key-input.js | v1.0.0 | Mobile-optimised key input. |

### Tools (9 shipped across v0.1.0–v0.1.4)

Landing page, SSH Key Generator, File Hasher, File Encryptor, Key Generator, Vault Browser, Vault (Pyodide), sg-send-cli, pyodide-repl. All tools now at v0.1.4 with clean URLs and environment-aware nav.

### Tests (5 test pages)

- `test-vault-client.html` — 40+ assertions
- `test-pyodide.html` — Python/JS key derivation comparison
- `sg-layout/v0.1.0.test.html` — 23 tests: custom element, tree construction, serialisation, registration, tabs, events, fractal nesting
- `sg-layout/v0.1.2-fractal-demo.html` — nested layouts demo
- `sg-layout/v0.1.3-drag-demo.html` — drag-to-dock demo with panel locking

### Infrastructure

- CI/CD deploy workflow (consolidated)
- Deploy script with `--clean-urls` support
- i18n generator (5 locales, 17 supported in UI)
- Root redirect with locale detection

---

## What's Been Shipped Since Last Dev Session

Since the IFD restructure session (5–7 Mar) and the bootstrap code review:

1. **Clean URLs + environment-aware navigation** (Brief 01 + 02)
2. **sg-nav.js** — environment detection (`dev.sgraph.ai` vs `main.sgraph.ai` vs `sgraph.ai`)
3. **4 new core modules** — pbkdf2, wordlist, vault-client, pyodide
4. **5 new components** — upload-dropzone, key-generator, key-input, site-header v1.0.1, locale-picker v1.0.1
5. **5 new tools** — file-hasher, file-encryptor, key-generator, vault-browser, vault-pyodide
6. **Pyodide integration** — CPython-in-WASM with full sg-send-cli running in browser
7. **Cross-language crypto verification** — Python and JS produce identical vault keys
8. **sg-layout** — fractal window management Web Component (~2100 lines). v0.1.0 base through v0.1.4 features: row/column layout, tab stacks, drag-to-dock with cross overlay, resize handles, panel locking, `closable` tabs, `fixedSize` panels. 23 test assertions.
9. **vault-client v1.1.0** — added vault-id-utils module
10. **v0.1.4 tools release** — all tools bumped with vault-browser now including schema modules, derived keys, file preview, graph branches/commits, mermaid diagrams, JSON view, and nav history
11. **sg-layout implementation guide** — comprehensive reference for LLM sessions integrating sg-layout

---

## Pending Work (Prioritised for Dev)

### P0 — sg-layout Follow-up (Active Development)

1. **Test close button in SG/Vault** — verify positioning in the actual consumer project
2. **Add v0.1.4 demo page** — showcase `closable` and `fixedSize` features
3. **Verify collapsed panels** — `flex: 1` was removed from `.sgl-stack-title`; confirm collapsed panels still render correctly
4. **Tab overflow handling** — when too many tabs, add scroll arrows or dropdown

### P1 — Quality & Security

5. **Input validation on crypto module** — `encryptFile()`, `decryptFile()`, `importKey()` lack validation
6. **SSH keygen security** — extract fingerprint to `sg-ssh.js`, add "Clear Keys" button, clipboard error handling
7. **Test coverage** — crypto and SSH have zero browser tests (vault-client has 40+, sg-layout has 23)

### P2 — sg-layout Enhancements (from debriefs)

8. **Middle-click to close tabs** — standard browser/IDE convention
9. **Keyboard navigation** — Ctrl+W close active tab, Ctrl+Tab switch tabs
10. **Lock indicator in tab bar** — visual lock icon (currently only in demo panel content)

### P3 — New Tools (from original briefs)

11. **Video Splitter** — highest-priority undelivered tool, requires FFmpeg WASM
12. **LLM Client** — extract from workspace, build tool UI

### P4 — Infrastructure

13. **Full 17-locale i18n** — CI only generates 5 locale page sets
14. **sgraph.ai migration** — main site should consume shared components from tools CDN

---

## Questions for This Session

1. **sg-layout is clearly the active frontier** — should the Dev continue hardening it (demo pages, tab overflow, keyboard nav)?
2. **Has sg-layout been integrated into SG/Vault?** The debriefs mention it — should we verify/fix integration issues?
3. **Should we create test pages** for crypto and SSH modules, or is sg-layout the priority?
4. **Is the Video Splitter still on the roadmap**, or has sg-layout + vault work taken its place?

---

## Session Status

**Status:** STARTED — awaiting direction on priorities.
