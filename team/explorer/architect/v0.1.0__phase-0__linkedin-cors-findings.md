# Phase 0 — LinkedIn building-block verification

**Status:** PROPOSED — not yet run. This document is a fillable template.
Once the probe page (`sgraph_ai_tools__static/tests/linkedin-probe.html`) is
opened in a browser and "Run all probes" is clicked, paste the results
below and remove this banner.

---

## How to run

1. Deploy this branch to S3 (or run `python -m http.server` from
   `sgraph_ai_tools__static/` for local).
2. Open `https://tools.sgraph.ai/tests/linkedin-probe.html` (or the
   localhost equivalent).
3. Click **Run all probes**. Wait ~5 s.
4. Open DevTools → Network tab and screenshot the four requests so we
   have the raw response headers as evidence.
5. Paste the decision matrix and the raw log below.

---

## Decision matrix

| Question | Answer | Implies |
|---|---|---|
| Does `api.linkedin.com` accept browser requests? | _fill in_ | _fill in_ |
| Does `oauth/v2/authorization` accept cross-origin GET? | _fill in_ | _fill in_ |
| Does `oauth/v2/accessToken` accept browser POST? | _fill in_ | _fill in_ |
| Can the browser produce S256 PKCE challenges? | _fill in_ | _fill in_ |

---

## Raw log

```
(paste the contents of the "Raw log" panel here)
```

---

## Architect verdict

**Proxy strategy chosen:** _A (managed) / B (BYO) / D (direct)_

Reasoning: _one paragraph — what does the evidence force?_

**Phase 1 unblocked?** _yes / no, blocked by …_

---

## Source

- Brief: `team/humans/dinis_cruz/claude-code-web/v0.1.0__brief__tools-team__linkedin-publisher__{1..5}__*.md`
- Probe page: `sgraph_ai_tools__static/tests/linkedin-probe.html`
- Phase 0 done-criteria: brief 5/5, lines 7–17.
