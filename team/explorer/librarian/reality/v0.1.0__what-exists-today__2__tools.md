# Reality Document — Part 2: Tools, Manifests, Tests

**Last verified:** 26 Apr 2026
**Scope:** Standalone tools, shared assets, i18n, manifest schemas, per-tool manifests, build scripts, browser-based tests.

**Sibling parts:**
- [Part 1 — Libraries (core modules, components)](./v0.1.0__what-exists-today__1__libraries.md)
- [Part 3 — Operations (CI/CD, SEO, team, config, reuse plan)](./v0.1.0__what-exists-today__3__operations.md)
- [Index (cover sheet + cumulative change paragraph)](./v0.1.0__what-exists-today.md)

This part is the user-facing surface — every standalone tool that lives at `tools.sgraph.ai/{slug}/`, the manifests that drive the landing-page registry, plus the test pages that exercise components from Part 1.

---

### Tools

| Tool | Path | Version | Status |
|---|---|---|---|
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.0/en-gb/index.html` | v0.1.0 | **FROZEN** — lists tools, no locale picker |
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/en-gb/index.html` | v0.1.1 | **FROZEN** — locale picker, "Tools" nav link, site-header v1.0.1 |
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/index.html` | v0.1.2 | **NEW** — clean URLs, environment-aware nav, locale-picker v1.0.1, 17 locales |
| SSH Key Generator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.0/en-gb/ssh-keygen/index.html` | v0.1.0 | **FROZEN** — generates Ed25519, no locale picker |
| SSH Key Generator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/en-gb/ssh-keygen/index.html` | v0.1.1 | **FROZEN** — locale picker, "Tools" nav link, site-header v1.0.1 |
| SSH Key Generator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/ssh-keygen/index.html` | v0.1.2 | **NEW** — clean URLs, environment-aware nav |
| File Hasher | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/file-hasher/index.html` | v0.1.2 | **NEW** — SHA-256/SHA-1/SHA-512 file hashing, uses sg-upload-dropzone |
| File Encryptor | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/file-encryptor/index.html` | v0.1.2 | **NEW** — AES-256-GCM encrypt/decrypt, uses sg-crypto + sg-upload-dropzone |
| Key Generator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/key-generator/index.html` | v0.1.2 | **NEW** — Friendly key creation, uses sg-key-generator component |
| Vault Browser | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/vault-browser/index.html` | v0.1.2 | **NEW** — Pure JS vault browser. Enter vault key, derive keys, fetch+decrypt tree and files. Uses sg-vault-client. |
| Vault (Pyodide) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/en-gb/vault-pyodide/index.html` | v0.1.2 | **NEW** — Experimental. Loads Pyodide WASM, runs Python vault crypto in browser, compares Python vs JS key derivation. Uses sg-pyodide + sg-vault-client. |
| Image Tools | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.5/en-gb/image-tools/index.html` | v0.1.5 | **NEW** — Convert (PNG/JPG/WebP/AVIF), resize, crop, redact, text overlay. Uses sg-image + image components + sg-send-drop. |
| Video Tools | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.5/en-gb/video-tools/index.html` | v0.1.5 | **NEW** — Split, trim, extract audio/video. FFmpeg WASM via sg-video + sg-video-splitter + sg-send-drop. |
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.6/en-gb/index.html` | v0.1.6 | **NEW** — Adds LLM Tools section (One-Shot Chat, Infographic Generator, LLM Dev Environment). All previous tools retained. |
| One-Shot Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.6/en-gb/one-shot-chat/index.html` | v0.1.6 | **NEW** — Full-screen one-shot LLM tool. 3-column CSS Grid layout (all sg-llm-* as direct children of shared event bus). Connection + Reality + Attachments | Output + Stats + Debug | Bundle List. |
| Infographic Generator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.6/en-gb/infographic-gen/index.html` | v0.1.6 | **NEW** — 2-column tool: Reality (left) + sg-llm-infographic SVG renderer (right). Auto-injects style-specific system prompt from sg-llm-infographic.getSystemPrompt(). |
| LLM Dev Environment | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.6/en-gb/llm-dev/index.html` | v0.1.6 | **NEW** — Full 3-column dev environment: Reality + Attachments | Output + Stats + Debug | Bundle List (time-travel). All 10 sg-llm components composited. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.7/en-gb/chat/index.html` | v0.1.7 | **NEW** — Multi-turn chat tool. 3-column layout: Connection | Chat History + Input + Stats | Bundle List. Uses sg-llm-chat-history + sg-llm-chat-input. Image attachments inline, text files appended. |
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.7/en-gb/index.html` | v0.1.7 | **NEW** — Adds Chat card to LLM Tools section. Section renamed to "LLM Tools". |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.8/en-gb/chat/index.html` | v0.1.8 | **BUG FIX** — Uses sg-llm-chat-history v0.1.1 + sg-llm-chat-input v0.1.1 with all 4 chat bug fixes applied. |
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.8/en-gb/index.html` | v0.1.8 | Updated from v0.1.7. |
| OpenRouter | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.14/en-gb/openrouter/index.html` | v0.1.14 | **NEW** — Dedicated OpenRouter tool. Two-section layout: User (62%) and Admin (38%). User section: Vault / Balance / Usage / Models tabs. Admin section: Vault / Keys tabs. Both vaults use AES-256-GCM + PBKDF2-SHA256 (200k iterations) to encrypt credentials in localStorage. Keys never leave the device. Fires llm:connected on user bus when key entered. |
| OpenRouter | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.22/en-gb/openrouter/index.html` | v0.1.22 | **NEW** — Enhanced OpenRouter tool. Two-section layout: User (60%) and Admin (40%). User section: Connect / Key Stats (full details panel with credit bar, rate limits, cache) / Usage tabs (top); Models / Model Detail (bottom). Admin section: Connect / Keys / Generation tabs (top); Activity / Spending (analytics + SVG charts) / Logs (CSV importer + table + analytics) tabs (bottom). Imports sg-openrouter-key-stats v0.1.0, sg-openrouter-spending v0.1.0, sg-openrouter-log-viewer v0.1.0. localStorage caching for both key stats (5 min) and spending activity (15 min). |
| OpenRouter | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.23/en-gb/openrouter/` | v0.1.23 | **UPDATED** — JS API Primitive. Extracts all inline JS from index.html into 3 modules: `api.js` (9 OpenRouter fetch helpers), `connect.js` (shared state + SgOrUserConnect + SgOrAdminConnect Web Components + wireVault()), `openrouter.js` (main orchestrator). Adds bottom developer panel pattern (fractal outer column 74%/26%: top tool area with 4-panel dashboard, bottom dev stack with ⚡ Explorer / > Console / 📋 Manifest). Registers `SgToolApi` with 12 methods: `connect`, `disconnect`, `getState`, `getKeyStats`, `getModels`, `getModelDetail`, `getUsage`, `getSpending`, `getActivity`, `listKeys`, `createKey`, `deleteKey`. `connect()` validates key against OpenRouter API (skip-if-same-key optimization), drives UI panels via `SGL_LLM.CONNECTED` on `[data-llm-bus]`. Admin methods gated on management key. Adds `manifest.json` with full `api:` section + SKILL files (human/browser/api). |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.10/en-gb/chat/index.html` | v0.1.10 | **NEW** — Uses sg-llm-chat-input v0.1.3 + sg-llm-attachment-manager v0.1.3. Adds PDF, audio, and video attachment support to the chat tool. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.24/en-gb/chat/index.html` | v0.1.24 | **BUG FIX** — Uses sg-llm-request v0.1.6. Fixes PDF attachments for OpenRouter (all models). Correct `{type:'file', file:{filename, file_data}}` content block format replaces the broken `{type:'document'}` Anthropic-native blocks from v0.1.23. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.23/en-gb/chat/index.html` | v0.1.23 | **BUG FIX** — Uses sg-llm-request v0.1.5. Fixes PDF attachments for Anthropic direct (browser-access header) and OpenRouter + Claude models (document block translation). |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.11/en-gb/chat/index.html` | v0.1.11 | **BUG FIX** — Uses sg-llm-chat-history v0.1.8 + sg-llm-request v0.1.2. Binary attachments (PDF/audio/video) now actually reach the LLM API instead of being silently dropped. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.12/en-gb/chat/index.html` | v0.1.12 | **NEW** — Uses sg-llm-chat-history v0.1.9 + sg-llm-bundle v0.1.6 (shadow DOM capture fix) + sg-llm-bundle-list v0.1.2 (inspect buttons) + sg-llm-bundle-viewer v0.1.0. Bundle Data tab added to right-column bottom stack. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.13/en-gb/chat/index.html` | v0.1.13 | **NEW** — Adds all 4 OpenRouter components: Connection + Stats/Credits/Usage (left) | Chat History + Input/Attachments (centre) | Bundles/Models/Keys + Token Viz/Inspector (right). Based on v0.1.11. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.14/en-gb/chat/index.html` | v0.1.14 | **BUG FIX** — Uses sg-llm-request v0.1.3. Provider error messages (e.g. "No endpoints found that support input audio") now shown in full in the chat error bubble instead of truncated HTTP status. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.15/en-gb/chat/index.html` | v0.1.15 | **NEW** — Bundle export/import. Uses sg-llm-bundle v0.1.7 + sg-llm-bundle-list v0.1.3. ↓ button per entry downloads bundle as JSON. Import button + drag-and-drop JSON onto the Bundles panel to restore a session. |
| Infographic Generator (Explorer) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.8/en-gb/infographic-gen/index.html` | v0.1.8 | **UPDATED** — Explorer/power-user version. sg-layout with 4 inner panes (Examples, Prompt, Actions, Stats). sg-llm-reality block editor. Concurrent multi-model requests via mini-bus pattern (capture-phase SEND interceptor). Per-result tab with fractal cancel button. Image models (Gemini, GPT-5-image, Flux) + SVG text models. Auto-connect, model-change reconnect. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.12/en-gb/infographic-gen/index.html` | v0.1.12 | **NEW** — Simplified "Villager" version. Simple mode by default: large textarea + Send button. 7 template chips (Executive Summary, Architecture, Timeline, Comparison, Process Flow, Stats Dashboard, Mind Map). Advanced toggle reveals system prompt override. Per-result fractal sg-layout (image panel left 65% + details panel right 35% with model/duration/tokens/export). Concurrent multi-model via mini-bus + capture-phase interceptor. Per-tab cancel button. Ctrl+Enter sends. |
| Multi-Agent Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.13/en-gb/multi-agent-chat/index.html` | v0.1.13 | **NEW** — Chat with multiple specialised agents simultaneously. Agent Picker sidebar (left 22%) lists 6 Explorer team agents (Architect, Dev, Designer, DevOps, Historian, Librarian) with role descriptions embedded as system prompts. Each opened agent → new tab containing a per-agent isolated `data-llm-bus` div with sg-llm-request + fractal inner sg-layout (75% chat column: sg-llm-chat-history / sg-llm-chat-input | 25% stats: agent info header + sg-llm-stats). Multi-Ask broadcast textarea at bottom of picker sends the same message to all open agent buses simultaneously (dispatches `llm:chat-message` on each bus). Model selector in connection bar applies to all agents; re-connect updates all open buses. Ctrl+Enter in broadcast. |
| Root redirect | `sgraph_ai_tools__static/index.html` | — | Locale detection (17 locales + sessionStorage), redirects to clean URL `/en-gb/` |
| Vault | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.16/en-gb/vault/index.html` | v0.1.16 | **NEW** — Native sgit/vault tool. 2-column sg-layout: Connection + Files tree (left 28%) | File Viewer/Editor (right 72%). Supports both full vault keys and simple tokens. Browse, view and edit encrypted vault files — all operations client-side. Uses `sg-vault-connect` + `sg-vault-tree` + `sg-vault-viewer` + `sg-vault-write`. |
| Vault | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.26/en-gb/vault/index.html` | v0.1.26 | **UPDATED** — Component imports bumped to v0.1.2. Manifest includes vault-cache, vault-tree-model, vault-session, vault-mutations as core deps. |
| Vault | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.27/en-gb/vault/index.html` | v0.1.27 | **UPDATED** — Session-based vault tool. Components v0.1.3 (connect, tree, viewer) + new sg-vault-sync-status v0.1.0. Sync bar with push/pull buttons below layout. All reads cached, saves go through mutations module + two-branch commit model. |
| Vault | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.28/en-gb/vault/index.html` | v0.1.28 | **UPDATED** — Full vault IDE. Adds sg-vault-file-ops toolbar (new/rename/delete) + sg-vault-commit-log tab (commit history with decrypted messages, LOCAL/REMOTE badges). Bottom bar: file-ops + sync-status side by side. Viewer + Commits as tabbed right panel. |
| Vault | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.29/en-gb/vault/index.html` | v0.1.29 | **UPDATED** — Adds vault creation flow (sg-vault-create as a tab next to Connect) and rich file preview (sg-vault-file-preview). Right panel tabs: Preview (default) / Editor / Commits. Users can create new vaults from scratch in the browser; preview renders markdown, images, JSON, CSV/TSV, HTML inline. |
| Agentic Loop | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.19/en-gb/agentic/index.html` | v0.1.19 | **NEW (Phase 2) + BUG FIX** — Full agentic loop tool. Uses sg-agentic-loop (extracted orchestrator) + sg-tool-definition (visual tool editor). 3-column sg-layout: Connection + Tool Definitions (left 24%) | Task input + Trace timeline (centre 48%) | Stats + VFS panel (right 28%). Task panel: task textarea, max-iterations, cost-budget, human-approval checkbox. sg-agentic-loop handles the full loop. VFS panel: file list with click-to-preview, seed-file button. sg-tool-definition: built-in + template tools with enable/disable + custom add form. |
| Agentic Loop | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.18/en-gb/agentic/index.html` | v0.1.18 | **Phase 1** — Agentic LLM tool calling demo. 3-column sg-layout: Connection + Tools panel (left 24%) | Task input + Trace timeline (centre 48%) | Stats + VFS panel (right 28%). Inline agentic loop: dispatches `llm:send` with selected tools, listens for `llm:tool-calls` + `llm:tool-results-complete` to continue the loop, stops on text response or max 10 iterations. Trace shows each step (User/LLM/Tool call/Tool result/Done). VFS panel lists in-memory files, seed-file button. sg-tool-runner provides VFS builtins (list_folder, read_file, write_file, delete_file, run_code). sg-vfs-bus memory provider. Loop logic inline (sg-agentic-loop component planned for Phase 2). **BUG FIX applied** — `#llm-bus { flex:1; display:flex; flex-direction:column; min-height:0; }` so sg-layout fills the full viewport height. |
| Agentic Loop | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.20/en-gb/agentic/index.html` | v0.1.20 | **NEW (Phase 3)** — Phase 3 agentic tool. 3-column sg-layout: Connection + Tool Definitions (left 22%) | Task + (Trace \| JSON Send \| JSON Receive) tabs (centre 46%) | Stats + Python REPL + VFS (right 32%). Uses sg-tool-runner v0.1.1 (Python support). Centre stack adds JSON Send (sg-json-sender) and JSON Receive (sg-json-receiver) tabs alongside Trace. Right column adds sg-python-repl (Pyodide REPL) between Stats and VFS. Full-height fix applied. |
| Model Compatibility | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.20/en-gb/model-compatibility/index.html` | v0.1.20 | **NEW (Phase 3)** — Static model compatibility matrix. Tests: simple tool call, chained tool calls, JSON schema adherence, stop condition, streaming, error recovery. Covers: OpenRouter (Gemma 4 MoE, Gemma 4 31B, Mistral 7B, Llama 3.1 8B/70B, Qwen 2.5 72B), Ollama (Llama 3.1 8B, Mistral 7B, Gemma 3 12B, Qwen2.5 7B), Anthropic (Sonnet 4.5, Haiku 4.5), OpenAI (GPT-4o, GPT-4o mini). |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.16/en-gb/chat/index.html` | v0.1.16 | **BUG FIX** — Uses sg-llm-chat-input v0.1.4 (horizontal resize fix). |
| VFS Dev | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.17/en-gb/vfs-dev/index.html` | v0.1.17 | **UPDATED** — Phase 2 VFS developer tool. Provider selector (memory / indexeddb / layered); swaps `sg-vfs-bus` live. Operations panel: write/read/list/stat/createFolder/copy/delete/exists + 5 batch presets + Clear IDB button. Right column: `sg-vfs-event-viewer` with 4 tabs (Timeline / Operations / Providers / Latency). |
| sg-tree Demo | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.18/en-gb/sg-tree-demo/index.html` | v0.1.18 | **NEW** — Full-stack sg-tree v0.1.4 demo. 2-column flex layout: 300px tree panel (editable + multi-select + dnd) + right control/state panel. Loads v0.1.4 chain (editor + virtual scroll + multi-select + drag-and-drop). Interactive controls: expand/collapse all, selectAll/deselectAll, createFolder, addFiles (generates dummy nodes), reset. State panel shows selectedIds count + array. DnD usage hint. Listens to DATA_CHANGED + SELECTION_CHANGED + NODE_SELECTED events. |
| VFS Tree Demo | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.21/en-gb/vfs-tree-demo/index.html` | v0.1.21 | **NEW** — Live demo of sg-tree + VFS together. 3-column layout: Operations (write/createFolder/delete/rename + batch presets + tree controls) | sg-tree rendering live VFS directory (auto-refreshes on every structural change) + inline file preview/editor | sg-vfs-event-viewer 4-tab log. File click shows content with inline edit+save+delete. Filter, expand/collapse all. Provider switching. |
| Folder Editor | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.21/en-gb/folder-editor/index.html` | v0.1.21 | **NEW** — Pure client-side folder editor. 2-column flex layout: 280px tree panel (sg-tree v0.1.4: editable + multi-select + dnd) + main preview panel. VFS persistence via IndexedDB (`sg-folder-editor-v1` db). OS file drop (window-level, `webkitGetAsEntry` recursive traversal for folder hierarchies) + Upload Files button + New Folder button. Text files (50+ extensions) stored as plain strings; binary files as base64 data URLs. Preview panel: image preview (img tag), text preview (pre), binary notice. Rename (F2/✎ button) syncs to VFS via `vfs.rename()`. Delete (Del key) removes from VFS. DATA_CHANGED handler diffs id-maps for renames and deletes. Manifest persisted to `/fe/current/.manifest.json` — restores full tree on reload. Clear All button. SVG files previewed as images. |
| Chat | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.22/en-gb/chat/index.html` | v0.1.22 | **NEW** — Adds `sg-openrouter-generation` as third panel in the left column (sizes 35%/20%/45%). Left column: Connection + Stats + Generation. Uses `sg-llm-chat-history v0.1.10` (image copy/save buttons) + `sg-llm-chat-input v0.1.5` (ResizeObserver textarea fix). |
| SG/Send Simulator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.22/en-gb/send-sim/index.html` | v0.1.22 | **NEW** — VFS-backed simulation of the SG/Send vault API. 3-column layout: Vault Setup (passphrase + vaultId + key derivation, shows KDF time) + Write File (full commit cycle: blob→tree→commit→ref) + Vault State (HEAD, branch, decrypted file list) + Demo Presets (Blog Posts / Config Files / 5-Commit History) + Provider selector | sg-tree showing encrypted vault object tree (bare/data/, bare/refs/) + Object Inspector (raw cipher hex preview, IV display, Decrypt button → reveals JSON schema: commit_v2/tree_v1/ref_v1) | sg-vfs-event-viewer showing all VFS operations. Uses sg-send-sim.js core module. |
| Voice Memo | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.23/en-gb/voice-memo/index.html` | v0.1.23 | **NEW** — Mobile-first voice memo tool. manifest.json added in v0.1.48 (category: media) — now visible on landing page. 6-panel sg-layout (3-column): LEFT: ⚙️ Setup (10-step WASM health check: WebAssembly/Worker/AudioContext/MediaRecorder/Mic/load tiny.en/test tiny.en/load base.en/test base.en/WebGPU) + 🎙 Record (segment duration control, recorder, session info) + 🔐 Vault (LLM connection config); CENTRE: 💬 Live Captions (sg-audio-transcript real-time display) + 📝 Transcript (editable merged textarea, copy/save-to-VFS) + 🚀 Pipeline (4-stage: Capture→Enhanced Transcription→Brief→Infographic); RIGHT: 📊 Events (sg-vfs-event-viewer). VFS IndexedDB for zero-loss segment persistence at `/voice-memos/{date-sessionId}/`. Segment blobs written immediately on each 30s boundary. tiny.en auto-transcription queue. Stage 2/3 stubbed (Phase 5). |
| Folder Editor | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.23/en-gb/folder-editor/index.html` | v0.1.23 | **NEW** — Adds SG/Send sharing to the folder editor. "Share ↗" button in tree toolbar + "Share ↗" tab in bottom-right panel (alongside Seed Data). Selecting any file or folder and clicking Share reads from VFS, converts to Blob, and calls `sg-send-drop.offerFiles()`. Folders are recursively collected with relative paths preserved inside a `.zip` (JSZip lazy-loaded by sg-send-drop). `contentToBlob()` handles both text strings and dataURL-encoded binaries. `collectFolderFiles()` walks the tree recursively. Single file → direct send; multiple files or folder → auto-zip. Uses `sg-send-drop v1.0.0`. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.24/en-gb/infographic-gen/index.html` | v0.1.24 | **UPDATED** — Fixes generation detail panel: uses `sg-openrouter-generation v0.1.1` (retry on 404 + `hide-recent` attribute removes irrelevant Recent column). Generation fetch delay reduced to 500 ms (component handles retries). Added three action buttons: **Download** (PNG/SVG via `<a download>`), **Copy** (PNG to clipboard via Canvas + `navigator.clipboard.write()`), **Share ↗** (via `sg-send-drop v1.0.0`). `imgSrcToBlob()` helper converts data: or https: image URLs to PNG Blob. All buttons appear after request completes. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.25/en-gb/infographic-gen/` | v0.1.25 | **UPDATED** — Model picker replaced with `sg-infographic-model-picker v0.1.0` (curated list, no API fetch flood, cost/speed metadata per option, Top 3 Recommended optgroup). Export/copy/share extracted to `sg-infographic-export v0.1.0` component. Tool JS split into 3 files: `constants.js` (41 lines), `panels.js` (282 lines), `infographic-gen.js` (596 lines). `index.html` is now a thin ~30-line shell. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.26/en-gb/infographic-gen/` | v0.1.26 | **UPDATED** — JS API Primitive Phase 0. Tool JS further split into 4 files: `connect.js` (~55 lines, `wireConnect()`), `send.js` (~155 lines, `wireSend()` + Phase 0 window events), `ui-input.js` (~220 lines, `buildInputPanel()`), `infographic-gen.js` (~75 lines orchestrator). `send.js` dispatches `SGA_TOOL` window events at every exit point (GENERATION_STARTED, GENERATION_COMPLETE, GENERATION_ERROR, GENERATION_CANCELLED) — all include `instanceId`. `manifest.json` extended with `api:` section documenting the 4 Phase 0 events. Footer `<sg-tool-manifest>` added to `index.html`. Imports `sg-tool-api-events.js`. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.27/en-gb/infographic-gen/` | v0.1.27 | **UPDATED** — JS API Primitive Phase 2. Delta-only version: 3 files changed. `infographic-gen.js`: instantiates `SgToolApi`, registers 9 methods (`connect`, `generate`, `getState`, `setPrompt`, `getPrompt`, `setModel`, `getModel`, `setTemplate`, `stop`), calls `api.activate()` → `window.__tool` live. `generate()` uses FIFO queue on Phase 0 window events — returns `Promise<detail>`. `connect()` returns `Promise<{model, provider}>` with 15s timeout. `connect.js`: fires `SGA_TOOL.TOOL_CONNECTED` on window after successful OpenRouter validation. `manifest.json`: updated to v0.1.27 with full `api.actions` schema (params, returns, async flag, notes per method). |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.28/en-gb/infographic-gen/` | v0.1.28 | **BUG FIX** — Delta: `send.js` only. (1) No-API-key guard now flashes `apiKeyEl` red and focuses it instead of silent return. (2) `GENERATION_COMPLETE` window event detail now includes `imageSrc: string\|null` — the extracted image URL available to `generate()` callers and Playwright. `manifest.json`: `generate.returns` updated to include `imageSrc`. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.29/en-gb/infographic-gen/` | v0.1.29 | **NEW** — SKILL files (Phase 3 content layer). `SKILL-human.md`: plain-English user guide (quick start, all 7 templates, document mode, model picker, export). `SKILL-browser.md`: Playwright/automation guide with full copy-paste examples (connect, generate, access imageSrc, events, meta, full Playwright test). `SKILL-api.md`: machine-readable capability spec (all methods with signatures/params/returns/errors, all events with detail schemas, known limitations). Unlocks `window.__tool.meta.getSkills()`. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.30/en-gb/infographic-gen/` | v0.1.30 | **UPDATED** — Delta: `infographic-gen.js` only. (1) Adds `sg-tool-api-explorer` as a second tab ("API Explorer") in the left panel stack alongside "Create". Explorer auto-binds to `window.__tool` after `tool:ready`. (2) Wires the `skills:` option into the `SgToolApi` constructor so `window.__tool.meta.getSkills()` now returns the three SKILL files (bug: was wired in v0.1.27 but missing the URL mapping). Version bump to ui=0.1.30. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.31/en-gb/infographic-gen/` | v0.1.31 | **UPDATED** — Delta: `send.js` + `infographic-gen.js`. (1) `renderUI: false` headless mode — `generate({ renderUI: false })` runs the LLM request but skips all DOM tab creation; window events and Promise resolution still work. (2) OpenRouter cost API polling — after generation, polls `fetchGeneration` every 1s for up to 12 attempts (starting 2s after completion) so cost data appears as soon as OpenRouter makes it available (~10s). Version bump to ui=0.1.31. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.32/en-gb/infographic-gen/` | v0.1.32 | **UPDATED** — Delta: `send.js` + `infographic-gen.js`. (1) FIFO → callId UUID — `generate()` generates `crypto.randomUUID()` as `callId`, sets it in the SEND event detail; `send.js` echoes it in all window events; `infographic-gen.js` uses `Map<callId, {resolve,reject}>` instead of FIFO array so concurrent `generate()` calls resolve to the correct result. (2) `connect()` skip-if-same-key — if `apiKey` and `model` are unchanged from the last successful connect, resolves immediately without an OpenRouter network request. Version bump to ui=0.1.32. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.33/en-gb/infographic-gen/` | v0.1.33 | **UPDATED** — Bottom Developer Panel. Outer sg-layout becomes a column split (tool 74% / dev 26%). Top tool area hosts fractal inner sg-layout row (Create 36% / Results 64%). Bottom dev panel is a stack with three tabs: ⚡ Explorer (`sg-tool-api-explorer`), > Console (`sg-tool-api-console`), 📋 Manifest (`sg-tool-api-manifest`). Removed API Explorer from left Create panel (moved to dev panel). `wireSend` now receives `innerLayout` (not outerLayout) for result tab creation. This establishes the reusable bottom dev panel pattern for all SgToolApi tools. Retains all v0.1.32 features: callId, skip-if-same-key, renderUI, cost polling. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.34/en-gb/infographic-gen/` | v0.1.34 | **UPDATED** — Collapsible dev panel via footer bar. Removes outer sg-layout column split; replaces with CSS flex column (toolArea flex:1 / devPanel 0→280px / footerBar fixed). Dev panel starts **collapsed** — footer bar click toggles it open/closed. Footer bar replicates `sg-tool-manifest` visual exactly (icon, name, version, status badge, dep count) but controls the dev panel. `sg-tool-manifest` removed from `index.html` (replaced by inline footer bar). Adds 4th dev tab **📄 Skills** — inline accordion fetching SKILL-human.md, SKILL-browser.md, SKILL-api.md on demand. footerInner border-radius animates (6px → 0 0 6px 6px) when panel opens. Dep count now reads 12 (sg-tool-manifest removed from shared deps). |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.35/en-gb/infographic-gen/` | v0.1.35 | **UPDATED** — Resizable dev panel. Adds 6px drag handle (`cursor:ns-resize`) between tool area and dev panel; teal highlight on hover. Mousedown drag updates `devCurrentH` (min 140px, max 75vh), disables CSS transition during drag for smooth feel, re-enables on mouseup. Default height bumped 280→340px so Console call button is fully visible. Footer bar top border changes to teal (`#4ECDC4`) when panel is open for clear visual separation. Resize handle shown/hidden with the panel. |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.37/en-gb/infographic-gen/` | v0.1.37 | **UPDATED** — Three additions. (1) `setSystemPrompt(text)` / `getSystemPrompt()` JS API methods — set the system prompt textarea directly without embedding override instructions in the user prompt; takes effect on the next `generate()` call. (2) `getGenerations(n?)` — session-scoped ring buffer (max 50) of full request/response records: `request.messages` is the exact multimodal array sent to sg-llm-request; `response` mirrors the `REQUEST_COMPLETE` detail (content, tokens, latency, rawChunks, rawResponse). Populated by new `onGenerationData` callback in send.js. (3) Skills tab now renders markdown via new `markdown.js` module (extracted from sg-llm-output internals). Dev panel logic extracted to `dev-panel.js`; `infographic-gen.js` reduced ~50%. Delta: `infographic-gen.js`, `send.js`, `manifest.json`, `markdown.js` (new), `dev-panel.js` (new), `SKILL-api.md` (new), `SKILL-browser.md` (new), `SKILL-human.md` (new). |
| Infographic Generator (Simple) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.36/en-gb/infographic-gen/` | v0.1.36 | **UPDATED** — Image attachment support in Text mode. Delta: `ui-input.js`, `send.js`, `infographic-gen.js`, `manifest.json`. A 📎 button in the action bar opens a file picker (PNG/JPG/WebP/GIF). A thumbnail strip (32×32 preview, filename, ✕ clear) appears between the textarea and the action bar when an image is loaded. Strip hidden in Document mode (which has its own dropzone). When an image is attached in Text mode, `send.js` builds a multimodal user message: `[{ type:'image_url', image_url:{ url: dataUrl } }, { type:'text', text: prompt }]`. `sg-llm-request` handles the format transparently across OpenRouter/OpenAI, Anthropic (converts to base64 source block), and Ollama. `getCurrentImage()` added to `buildInputPanel` return value and wired through `wireSend`. `getState()` now includes `image: { name } | null`. Placeholder hint updated. |
| Playwright smoke test | `sgraph_ai_tools__static/tests/playwright/infographic-gen-smoke.js` | v0.1.0 | **NEW** — Node.js Playwright test script validating the full JS API Primitive contract: `window.__tool` ready check, `SGA_TOOL` constants, all 9 methods via `meta.getMethods()`, manifest/skills/version meta, `getState()`, no-key flash guard, `setTemplate()`/`setPrompt()`/`getPrompt()`, `setModel()`/`getModel()`, `connect()`, `generate()` with imageSrc assertion, and execution log. 10 test groups. `OPENROUTER_API_KEY` env var required only for connect/generate tests; all other tests run without a key. |
| Playwright smoke test | `sgraph_ai_tools__static/tests/playwright/openrouter-smoke.js` | v0.1.0 | **NEW** — Node.js Playwright test for OpenRouter JS API Primitive. 9 test groups: window.__tool ready, all 12 methods present, manifest with api: section, all 3 SKILL files, getState() shape, connect() (needs API key), getKeyStats(), getModels() with filter/top, listKeys() (needs management key). Tests [1]–[5] run without any API key. |
| Speed Test | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.38/en-gb/speed-test/` | v0.1.38 | **NEW** — Standalone upload/download speed measurement tool. IFD delta v0.1.38: `index.html`, `speed-test.js`, `manifest.json`, `SKILL-human.md`. Measures download speed via streaming fetch with real-time throughput tracking; measures upload speed via sequential 256 KB POST chunks. Progress bars, live speed (MB/s + Mbps), duration summary. Cancel via AbortController. Share results: pre-formatted text block + clipboard copy. Configurable target URL (default `https://send.sgraph.ai`) and probe size (1/4/10/25 MB). `window.__speedTest` public API: `runAll()`, `cancel()`, `setTargetUrl(url)`, `getLastResults()`, `measureDownloadSpeed(url, opts)`, `measureUploadSpeed(url, opts)`. NOTE: v0.1.38 had two bugs (crypto.getRandomValues crash, non-existent server endpoints) and did not import from sibling core modules. Superseded by v0.1.39. |
| Speed Test | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.39/en-gb/speed-test/` | v0.1.39 | **UPDATED** — Complete rewrite fixing all arch-review issues. IFD delta v0.1.39: `index.html`, `speed-test.js`, `speed-test-sim.js`, `speed-test-live.js`, `manifest.json`, `SKILL-human.md`, `SKILL-browser.md`, `SKILL-api.md`. (1) Imports from sg-send-upload v1.1.0 + sg-send-download v1.0.0: no reimplemented logic. (2) Fixed `crypto.getRandomValues` crash via `randomBytes()` helper in speed-test-sim.js that fills in 65,536-byte chunks. (3) No non-existent server endpoints — sim mode is offline-only crypto benchmark; live mode uses ChunkedUploader/ChunkedDownloader against real API. (4) sg-layout with collapsible bottom dev panel + drag-to-resize handle. (5) SgToolApi registration → `window.__tool` with 11 discoverable methods. (6) Three SKILL files. Three modes: Sim (AES-256-GCM encrypt + decrypt throughput, works offline), Live upload (ChunkedUploader real network), Live download (ChunkedDownloader HTTP Range requests). History: last 10 runs with min/avg/max statistics. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.47/en-gb/playbooklm/` | v0.1.47 | **UPDATED** — IFD delta (10 files). **Vault-first integration + image compression.** (1) **NEW `api/slide-optimizer.js`** (v0.1.0): `optimizeSlide(imageSrc)` → `{png, dist, li, sizes}`. Canvas JPEG encoding at generation time. Three variants: PNG archive (original), dist 1200px q0.82 (~113 KB, 87% smaller), li 800px q0.70 (~50 KB). No WASM, no libraries. (2) **NEW `api/vault-ops.js`** (v0.1.0): all vault I/O isolated here. Functions: `createDeck`, `loadDeck`, `listDecks`, `saveSources`, `savePresentation`, `saveSlideBriefs`, `saveSlide`, `saveDeckMeta`. Manages `playbooklm/decks/{id}/` folder structure + `playbooklm/deck-index.json`. Commit messages: `PLM/{title}/step{n}: …`. Respects `vaultProfile` (full/distribution/minimal) for PNG archiving. (3) **UPDATED `api/pipeline-state.js`** (v0.1.3): vault fields (`_vault`, `_vaultDeckId`, `_vaultProfile`, `_vaultPendingChanges`, `_deckTitle`). `SlideVersion` typedef gains `distBlob`, `liBlob`, `sizes`. New methods: `setVault()`, `incrementPending()`, `clearPending()`, `setDeckTitle()`, getters for all vault fields. (4) **UPDATED `api/pipeline-steps.js`** (v0.1.6): `optimizeSlide()` called after every `_callImageLLM()`; blobs stored in `addSlideVersion()`. Background vault saves after steps 2/3/4 (`_bgSave`). `_exportPDF()` uses `distBlob` when present (107-slide deck: 116 MB → 14.8 MB). `buildArchiveBlob()` adds `v{n}-dist.jpg` + `v{n}-li.jpg` per version + PDF from dist blobs. (5) **UPDATED `api/pipeline-api.js`** (v0.1.3): 5 new vault methods: `connectVault`, `loadDeck`, `listDecks`, `saveStep`, `disconnectVault`. Total: 27 registered methods. (6) **NEW `ui/ui-vault-panel.js`** (v0.1.0): `buildVaultStartScreen()` (embeds `sg-vault-manager`, deck picker with load/new deck flow) + `buildVaultStatusBar()` (persistent bar: ● Saved / ⚠ N unsaved / ✗ failed; gear icon → deck config popover with title edit + storage profile + disconnect). (7) **UPDATED `ui/ui-shell.js`** (v0.1.7): vault start screen replaces welcome placeholder in Results tab; vault status bar inserted above tool area; `onVaultConnected` wires `state.setVault()` then clears start screen. (8) **UPDATED `ui/ui-export.js`** (v0.1.2): descriptions updated to reflect JPEG compression; export logic unchanged (pipeline-steps handles compression). (9) **UPDATED `manifest.json`** (v0.1.47): 3 new phase 2 imports (`sg-vault-manager`, `vault-client`, `vault-write`); api.version 0.1.3; 27 methods documented. (10) **NEW platform component**: `sg-vault-manager` v0.1.0 at `components/vault/sg-vault-manager/`. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.46/en-gb/playbooklm/` | v0.1.46 | **UPDATED** — IFD delta (3 files). Single fix: `buildArchiveBlob()` in `pipeline-steps.js` (v0.1.5) now generates the slide deck PDF using `window.jspdf.jsPDF` and adds it as `playbooklm-deck.pdf` at the root of the session archive ZIP. jsPDF is already loaded by manifest phase 1. If jsPDF is unavailable or an image fails, the PDF block is silently skipped and all other archive contents are unaffected. `pipeline-api.js` and `manifest.json` bumped to v0.1.46. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.45/en-gb/playbooklm/` | v0.1.45 | **UPDATED** — IFD delta (8 files). (1) **Slide version history**: `pipeline-state.js` (v0.1.2) adds `_slideVersions: Map<index, SlideVersion[]>` + `_selectedVersions: Map<index, number>`. New methods: `addSlideVersion(index, data)` (auto-selects latest, syncs `_slideResults` for backward compat), `getSlideVersions(index)`, `getSelectedVersion(index)`, `setSelectedVersion(index, vIdx)`. `SlideVersion` typedef captures `imageSrc, title, briefPrompt, systemPrompt, model, cost, ts, latencyMs`. (2) **`_callImageLLM` returns `{imageSrc, cost, latencyMs}`** (was just `imageSrc`). `generateSlide` now calls `state.addSlideVersion()` with full metadata. Accepts optional `customBrief` + `customSystemPrompt` params for iterative editing. (3) **Fractal slide workspace** (`ui-generation.js` v0.1.2): each slide tab now hosts a nested `sg-layout` (38% info | 62% image). Info panel: version history list (clickable to switch), per-version stats, editable brief + system prompt textareas, "Generate New Version" button. Image panel: thumbnail with ◀/▶ version navigation, download link. (4) **Deck view** (`ui-deck-view.js` NEW v0.1.0): `🎞 Deck` tab in right panel. Reactive CSS grid of all slide thumbnails; shows spinner during generation, version badge when >1 version. Click thumbnail activates slide workspace tab. (5) **Session Archive export** (`ui-export.js` v0.1.1 + `pipeline-steps.js`): `buildArchiveBlob()` builds full ZIP: selected slide images, all version images (`slide-versions/`), `presentation-strategy.md`, `slide-briefs.md`, per-slide prompt history (`prompts/`), `cost-summary.md`, original sources (`sources/`). `exportDeck({format:'archive'})` triggers download. (6) **SG/Send share button**: builds archive ZIP, triggers download, opens `send.sgraph.ai` in new tab, shows toast with instructions. (7) **3 new API methods**: `getSlideVersions(index)`, `getSelectedVersion(index)`, `setSelectedVersion({index, versionIdx})`. API version bumped to 0.1.1, 22 total methods. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.44/en-gb/playbooklm/` | v0.1.44 | **UPDATED** — IFD delta (6 files). (1) **Haiku model ID fix**: `anthropic/claude-haiku-4-5-20251001` → `anthropic/claude-haiku-4.5` (OpenRouter short form). Fixed in `ui-model-config.js` (PHASE_CONFIGS for steps 2+3) and added to MODEL_PRICING in `pipeline-steps.js` (was causing $0.0000 cost on Haiku calls). Legacy alias kept. (2) **Monitor panel** (`ui-monitor-panel.js` NEW v0.1.0): `🔍 Monitor` tab in right panel with 3 sub-tabs: "🔍 LLM" — `sg-llm-response-inspector` on persistent `[data-llm-bus]` div; text `plm:llm-log` 'done' events relayed as synthetic `llm:request-complete`; image 'response-raw' events relay full `rawDetail` directly. "⚡ Console" — `sg-tool-api-console` bound to `window.__tool` (19 methods). "🔑 Gen IDs" — custom list collecting OpenRouter generation IDs from `plm:llm-log` events; "Fetch cost" button calls `https://openrouter.ai/api/v1/generation?id=…`. (3) **Stepper step-4 fix** (`ui-shell.js` v0.1.5): `getCompletedSteps()` now adds step 4 to `done` when `slideBriefs.length > 0` (not only when slides are complete) — fixes "step 4 disappears when navigating back to step 3". (4) `pipeline-steps.js` v0.1.3: text 'done' log now includes `rawResponse`, `generationId`, `latencyMs`; image 'response-raw' log now includes `rawDetail` (actual object for relay) + `generationId`. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.43/en-gb/playbooklm/` | v0.1.43 | **UPDATED** — IFD delta (8 files). (1) **Image extraction fix**: `_extractImageSrc` in `pipeline-steps.js` now reads `img.image_url.url` (OpenRouter format) instead of `img.url` which was always `undefined` — fixes "No image returned from LLM" error. Also handles `inline_data` Gemini format. (2) **State-aware stepper** (`ui-stepper.js`): ✓ marks driven by `getCompletedSteps()` callback (checks actual PipelineState data) in addition to positional logic — clicking step 1 no longer wipes done indicators for completed steps. `refreshStatus()` method exported; `state.onChange(refreshStatus)` keeps indicators live. (3) **⚡ LLM Log tab** in `ui-dev-panel.js`: 5th dev panel tab capturing `plm:llm-log` CustomEvents. Shows cards per LLM call with type badge (text/image), model, status, token counts, cost, timestamp; expandable body with full system prompt, request JSON, response-raw detail. Max 50 entries, Clear button. (4) **`<select>` model selector** (`ui-model-config.js`): Redesigned from `<datalist>` (showed raw IDs) to `<select>` + `<optgroup>` with friendly labels grouped by provider. Added Haiku 4.5, Gemini 2.5 Pro, GPT-4o variants. "Custom model ID…" fallback option reveals a monospace text input (teal border). (5) `pipeline-steps.js` dispatches `plm:llm-log` CustomEvents for every LLM call (start/done/error/response-raw phases). `model-cost.css` updated with `.plm-model-select` + `.plm-model-custom-input` styles. `pipeline-api.js` bumped to ui=0.1.43. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.42/en-gb/playbooklm/` | v0.1.42 | **UPDATED** — IFD delta (9 files). Per-phase model configuration + cost tracking. (1) **NEW `ui/ui-model-config.js`**: `PHASE_CONFIGS` mapping phases 2/3/4 to model lists with friendly labels; `getPhaseModel(phase)`, `savePhaseModel(phase, model)`, `buildModelSelector(phase)` building an inline selector (label + `<datalist>` input + type badge). Persists to `localStorage['plm-model-config']`. (2) **NEW `ui/ui-cost-tracker.js`**: `buildCostDisplay(state)` injecting a cost bar after `#conn-status` showing "Session: $X.XXXX · Deck: $X.XXXX ↺". Yellow highlight (`.plm-cost-has-spend`) when spend > 0; reset button calls `state.resetDeckCost()`. (3) **NEW `styles/model-cost.css`**: `.plm-model-selector`, `.plm-model-selector-input`, `.plm-model-selector-badge`, `.plm-model-badge-text` (purple), `.plm-model-badge-image` (teal), `.plm-cost-display`, `.plm-cost-has-spend`, `.plm-cost-reset`. (4) **UPDATED `api/pipeline-state.js`**: `_sessionCost` (in-memory), `_deckCost` (localStorage-persisted as `'plm-deck-cost'`), `recordCost(amount)`, `resetDeckCost()`, `get sessionCost`, `get deckCost`. Default text model changed from invalid `google/gemini-2.5-flash-preview` to `anthropic/claude-sonnet-4-6`. (5) **UPDATED `api/pipeline-steps.js`**: `MODEL_PRICING` table + `IMAGE_FLAT_COST`; `_calcTextCost(model, usage)`, `_calcImageCost(model, rawResponse)`; cost tracked after each LLM call via `state.recordCost()`. (6) `ui-shell.js`, `ui-presentation.js`, `ui-briefs.js`, `ui-generation.js` updated to embed model selectors and wire cost display. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.41/en-gb/playbooklm/` | v0.1.41 | **UPDATED** — IFD delta (3 files). Bug fixes for stepper display + API key persistence. (1) `ui-stepper.js`: panels initialized with `panel.style.display = 'none'` (not CSS class) and `setStep` uses `el.style.display = ''/'none'` — fixes "opens on step 5" regression where inline `style.cssText = 'display:flex'` from panel builders always beat the `.hidden` CSS class rule. (2) `ui-shell.js`: persists API key to `sessionStorage['plm-api-key']` on successful connect and restores it on load — fixes "API key lost on refresh" regression. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.40/en-gb/playbooklm/` | v0.1.40 | **UPDATED** — IFD delta (6 files). Stepper component + example sources. (1) **NEW `ui/ui-stepper.js`**: extracts step progress indicator (1→2→3→4→5 track with connectors) from `ui-shell.js` into a standalone module. Returns `{ setStep, getPanelEl }`. Step indicator shows numbers, active highlight, ✓ for completed steps. Connector fills between completed steps. Clicking a lower step navigates back without losing data. (2) **NEW `ui/ui-examples-data.js`**: provides sample source texts (marketing brief, technical spec snippets) for one-click demo fill in Step 1. (3) `ui-sources.js`: adds "Load example" button wired to `ui-examples-data.js`. (4) `styles/pipeline.css`: new `.plm-step-connector.done` fill rule. |
| Video Creator | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.47/en-gb/video-creator/` | v0.1.47 | **NEW** — Browser-based narrated slideshow creator. Manifest-loader driven. Upload slide images (PNG/JPEG) → write per-slide narration → Load TTS Model (Kokoro 82M ONNX, ~160 MB cached) → Generate Audio (sg-tts 8-worker pool, parallel sentences, ~0.87× realtime) → Record Video (canvas + MediaRecorder) → Download WebM. Architecture: `api/video-events.js` (SGA_VIDEO constants), `api/video-state.js` (PipelineState), `api/video-pipeline.js` (canvas drawing, caption bar, AudioContext merge, recordSlideshow()), `api/video-api.js` (SgToolApi: 10 methods — connect/loadSlides/setNarration/generateAudio/record/stopRecording/download/getStatus/getConfig/setConfig). UI: `ui/ui-shell.js`, `ui/ui-slides.js`, `ui/ui-narration.js`, `ui/ui-export.js`. Skills: SKILL-human/browser/api. Uses sg-tts v0.1.0 + sg-video-recorder v0.1.0 + sg-video v1.0.0. |
| Auth MVP | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.50/en-gb/auth-mvp/` | v0.1.50 | **NEW** — Interactive MVP testing ground for all auth building blocks. 2-panel sg-layout (left 38%: Setup + 7 check sections with inline components; right 62%: Output/Events/Vaults tabs). 7 checks: Auth Status, Google OAuth, Token Storage, User Profile, Logout Workflow, Credential Store, Credential List. Run individually or "Run All". Google Client ID stored in localStorage. Right panel Vaults tab shows `sg-vault-picker`. Live event log captures all `sg-auth:*`, `vault:*`, `sg-cred:*` events. SgToolApi with 5 methods on `window.__tool`: `getAuthStatus`, `getCredIndex`, `detectCredStore`, `clearAllTokens`, `runCheck(id)`. Bottom dev panel (collapsible, drag-to-resize): 📄 Skills / ⚡ Explorer / > Console / 📋 Manifest tabs. Files: `index.html`, `auth-mvp.js`, `dev-panel.js`, `markdown.js`, `manifest.json`, `SKILL-human.md`. Dependencies: all 6 auth components + sg-vault-picker. Phase 2 plan: `team/explorer/dev/plans/v0.1.50__auth-mvp__phase2-vault-pki-integration.md`. |
| Send / Receive | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.49/en-gb/send-receive/` | v0.1.49 | **NEW** — Reference implementation for the SG/Send workflow. 2-panel sg-layout (left: 📤 Send + 📋 History tabs; right: 📥 Receive tab). Send panel: access token input + `sg-send-drop` + file picker. Receive panel: `sg-send-receive`. History panel: localStorage history (max 50 entries, copy/re-receive/clear). Bottom developer panel (collapsible, drag-to-resize): 📄 Skills (first, most important for agents) / ⚡ Explorer / > Console / 📋 Manifest tabs. SgToolApi with 11 methods on `window.__tool`: `sendFile`, `sendText`, `sendFolder`, `receiveFile`, `receiveText`, `receiveFolder`, `getHistory`, `clearHistory`, `getState`, `setToken`, `setAccessToken`. Cryptographic protocol: token=`word-word-NNNN`, PBKDF2-SHA-256 @ 600k iterations, AES-256-GCM, SGMETA envelope. JSZip lazy-loaded from CDN for folder operations. Three SKILL files: SKILL-human.md (user guide), SKILL-browser.md (Playwright automation), SKILL-api.md (agent-facing API spec — the canonical reference for any tool that needs to send/receive files). The SKILL-api.md "Purpose" section gives agents the 3 canonical patterns: `window.__tool.sendFile()`, `<sg-send-drop>`, `<sg-send-receive>`. Files: `index.html`, `send-receive.js`, `dev-panel.js`, `markdown.js`, `manifest.json`, `SKILL-human.md`, `SKILL-browser.md`, `SKILL-api.md`. |
| Video Recorder | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.48/en-gb/video-recorder/` | v0.1.48 | **NEW** — Browser-based video/audio/screen recorder. Supports 9 capture modes including 2 viz+audio modes. Three save targets: SG/Send (encrypted link), local folder, SGraph Vault. Architecture: `api/recorder-events.js` (SGA_RECORDER constants incl. TRACK_LOST), `api/recorder-state.js` (RecordingConfig + RecordingState), `api/recorder-pipeline.js` (RecordingSession class — per-recording isolated state, stopAll() with Promise.allSettled + 10s timeouts per recorder, MediaRecorder.onerror wired, _watchTracks() monitors camera+screen video tracks for unexpected loss and auto-stops pipeline), `api/save-sg-send.js`, `api/save-folder.js`, `api/save-vault.js`, `api/recorder-api.js` (SgToolApi 13 methods). UI: `ui-shell.js` (sg-layout row split, vizProvider with 0×0 canvas guard, _reRenderViz with 2× real-time timeout, speed selector 1×/2×/4×/8×), `ui-controls.js` (3-panel mode builder, _previewActive boolean for preview toggle, TRACK_LOST warning banner), `ui-recording-tab.js` (post-recording player + per-track downloads + SG/Send share + viz regeneration). Viz regeneration: if sg-audio-viz wasHidden during recording, user can re-render viz from audio at selectable speed; at speed>1× output is viz-only (audio track dropped to avoid pitch shift). WebM duration patched in EBML header post-recording. Uses sg-capture v0.1.0 + sg-video-recorder v0.1.2 + sg-audio-viz v0.1.0. |
| PlaybookLM | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.39/en-gb/playbooklm/` | v0.1.39 | **NEW** — Five-step AI-powered slide deck generator. Upload source documents → generate a presentation strategy → create slide briefs → generate slide images (one per brief) → export as PDF or ZIP. Manifest-loader driven: `index.html` is a near-static shell that loads `manifest.json`, which phases in CSS (4 files), jsPDF + JSZip (CDN UMDs), custom-element registrations, and finally the entry module `api/pipeline-api.js`. Architecture: `api/pipeline-events.js` (PLM_EVENTS constants), `api/pipeline-state.js` (PipelineState class — pure state, no DOM), `api/pipeline-steps.js` (_callTextLLM direct fetch; _callImageLLM mini-bus; steps 2–5: generatePresentation, generateSlideBriefs, generateSlide, generateAllSlides, exportDeck), `api/pipeline-api.js` (SgToolApi: 19 registered methods, api.activate(), then init() → ui-shell). UI: `ui/ui-shell.js` (sg-layout, 5-tab left panel, welcome right panel), `ui/ui-sources.js` (Step 1: file drop zone + source list), `ui/ui-presentation.js` (Step 2: strategy generation + editable textarea), `ui/ui-briefs.js` (Step 3: brief cards, editable title + prompt), `ui/ui-generation.js` (Step 4: slide grid, per-slide generate, right panel tabs), `ui/ui-export.js` (Step 5: PDF/ZIP export cards), `ui/ui-dev-panel.js` (footer bar + collapsible dev panel: Explorer/Console/Manifest/Skills). Styles: `styles/playbooklm.css`, `styles/pipeline.css`, `styles/slide-grid.css`, `styles/export.css`. Skills: `skills/SKILL-human.md`, `skills/SKILL-browser.md`, `skills/SKILL-api.md`. JS API: 19 methods on `window.__tool` — connect, loadSources, getSources, removeSource, generatePresentation, getPresentation, setPresentation, generateSlideBriefs, getSlideBriefs, setSlideBrief, addSlideBrief, removeSlideBrief, generateSlide, generateAllSlides, getSlideResults, exportDeck, getState, getPipelineStatus, stop. |
| SG/Send Theme Explorer (page-builder) | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.42/en-gb/page-builder/` | v0.1.42 | **NEW** — Phase 1 MVP: `_page.json` editor with live preview using `PageLayoutRenderer` from the SG/Send CDN (imported as a global script, no iframe, direct DOM render). Left panel (40%): theme picker dropdown (6 named schemes: default/navy/slate/minimal/brand/dark-deck), JSON textarea with 200ms debounced re-render, error bar (invalid JSON), Copy/Demo/Clear buttons, and 13 palette chips that append a default block on click. Right panel (60%): viewport switcher (🖥 Desktop / ⬜ Tablet 768px / 📱 Phone 375px) + live preview container constrained to the selected width. Stub `browseInstance` satisfies the renderer's required parameter for asset-less renders. `theme.background` applied as container background colour. SgToolApi registration: 6 methods (`setJson`, `getJson`, `setTheme`, `getTheme`, `render`, `getState`) on `window.__tool`. Files: `index.html`, `page-builder.js`, `constants.js`, `page-builder.css`, `manifest.json`, `SKILL-human.md`. CDN deps: `page-layout-renderer.js` + `send-browse-v031--page-layout.css` from `dev.send.sgraph.ai`. |
| Playwright smoke test | `sgraph_ai_tools__static/tests/playwright/upload-download-smoke.js` | v0.1.0 | **NEW** — Node.js Playwright smoke test for sg-send-upload v1.1.0, sg-send-download v1.0.0, and speed-test tool v0.1.38. 10 test groups: [1] sg-send-upload exports (9 named exports), [2] ChunkedUploader construction + resume state shape, [3] detectCloudDriveFile (iCloud stub, Google Drive MIME, local file, null), [4] resume state round-trip (getResumeState/fromResumeState), [5] formatSpeed/formatEta helpers, [6] sg-send-download exports (7 named exports), [7] ChunkedDownloader construction + chunk init, [8] decryptWithProgress callback sequence (stage/percent values), [9] speed-test page load + window.__speedTest API methods, [10] speed-test UI elements. Groups [1]–[8] run on about:blank via dynamic import; groups [9]–[10] require the dev server. |
| Landing page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.25/en-gb/index.html` | v0.1.25 | **NEW** — Manifest-driven landing page. Fetches `manifest.json` from all 24 tools via `sg-tool-registry.js`. 6 categories (Security & Crypto, AI/LLM, Media, Vault & Send, Code & Runtime, Developer Sandbox). Featured hero row (Chat, File Encryptor, Infographic Gen, Vault). Audience filter pills (All / End User / Developer, URL hash state). Developer Sandbox collapsed by default via `<details>`. "Requires API key" sub-badge on LLM tools. Description clamped to 2 lines. Search auto-opens collapsed sections. |
| Locale redirect | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/index.html` | v0.1.1 | **FROZEN** — auto-detects browser language |
| Locale redirect | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/index.html` | v0.1.2 | **NEW** — 17 locales + sessionStorage check |
| 404 page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/404.html` | v0.1.1 | **FROZEN** |
| 404 page | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/404.html` | v0.1.2 | **NEW** — clean URL paths |
| YouTube Upload | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.53/en-gb/youtube-upload/` | v0.1.0 | **NEW** — Standalone tool for uploading a video to YouTube using a user-supplied Google OAuth client ID. Manifest-driven (sg-tool-api + manifest-loader). sg-layout split: Upload controls (left, with `<sg-upload-dropzone>` + `<sg-youtube-upload>`) ‖ Activity log + result card (right). Collapsible JS API dev panel (Explorer / Console / Manifest / Skills). API methods: `connect`, `disconnect`, `setClientId`, `setFile`, `uploadVideo`, `getStatus`, `health`. Skills: `SKILL-human.md`, `SKILL-browser.md`, `SKILL-api.md`. All bytes go browser→YouTube directly; no SGraph server. Token cached in localStorage `sg-auth-token-youtube-upload` (vault swap planned). |
| YouTube Editor | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.53/en-gb/youtube-editor/` | v0.1.0 | **NEW (v0.2 contents)** — adds playlists tab (`<sg-youtube-playlists>`), in-editor playlist picker, in-editor inline YouTube preview iframe, grid stats enrichment + filter + sort, hand-off receiver (`/handoff/sg-youtube-handoff.js` exports `sendToYouTubeEditor` + `pickupHandoff`; the editor consumes `window.opener.__sgYtHandoff` on boot, focuses Upload tab, pre-fills file + title), and 5 new SgToolApi methods (`listMyPlaylists`, `listPlaylistItems`, `findVideoPlaylists`, `addToPlaylist`, `removeFromPlaylist`). Delete UI removed from the editor tab (component still exposes `deleteVideo()` for the SDK). Original v0.1.0 description below. — Browser-only YouTube channel companion combining upload + listing + edit. Manifest-driven (sg-tool-api + manifest-loader). sg-layout: left = Account panel (client-id, sign-in, channel card with avatar/stats); right stack = "📋 My Videos" (paginated grid via uploads playlist) + "⬆ Upload" (resumable insert) + per-video "✎ Editor" tabs that open on row click and accumulate (mirrors video-recorder pattern). Uses the broader `youtube` OAuth scope (full read+edit+delete+thumbnails). Token cached in localStorage `sg-auth-token-youtube-editor` (separate from youtube-upload provider so both tools coexist). API methods: `connect`, `disconnect`, `setClientId`, `getMyChannel`, `listMyUploads`, `loadVideo`, `updateVideo`, `setThumbnail`, `deleteVideo`, `uploadVideo`, `getStatus`, `health`. Skills: `SKILL-{human,browser,api}.md`. All bytes go browser→YouTube directly. |
| LinkedIn Publisher | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.55/en-gb/linkedin-publisher/` | v0.1.0 | **NEW** — Phase 0 only: connection probe panel that runs four building-block checks on page load + a Re-run button. Files: `index.html` (site header + 4 cards), `manifest.json` (Client ID `78tdt0b9upct3u`, redirect `https://dev.tools.sgraph.ai/en-gb/linkedin-publisher/oauth-callback.html`, scopes `openid profile email w_member_social`), `styles/linkedin-publisher.css`, `api/probe-{api,oauth,pkce}.js`, `ui/ui-probe-panel.js`, `ui/main.js`. Probes: (1) `api.linkedin.com/rest/posts` CORS — HTTP 401 = pass, TypeError = CORS-blocked; (2) `oauth/v2/authorization` GET CORS — diagnostic only (real flow is popup nav); (3) `oauth/v2/accessToken` POST CORS with bogus PKCE form body — HTTP 400 = pass, TypeError = needs proxy; (4) Web Crypto S256 PKCE challenge generation. Auto-fills a 4-row decision matrix mapping results to Options A/B/D from brief 2/5. Findings template at `team/explorer/architect/v0.1.0__phase-0__linkedin-cors-findings.md`; member-first pivot in `team/humans/dinis_cruz/claude-code-web/v0.1.1__brief__tools-team__linkedin-publisher__amendment__member-first-mvp.md`. Status `dev`. Not yet listed in `_common/js/sg-tool-registry.js` TOOL_SLUGS — add when Phase 1 ships. |
| SG Video Editor | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.54/en-gb/sg-video-editor/` | v0.1.0 | **NEW** — Phase 1 multi-clip browser video editor. Manifest-driven (sg-tool-api + manifest-loader). Plain DOM orchestrator (`ui/ui-shell.js`) composes a top bar with Export controls, asset panel (left, drag source for `application/x-sg-asset`), `<sg-preview-canvas>` (preview + transport), and `<sg-timeline>` (single video lane, ruler, playhead). Project state container in `ui/state.js` (wrapped schema: `{schemaVersion, project, assets, tracks, operations}` + in-memory `assetRegistry: Map<assetId, Blob>`). 10 SgToolApi methods: `loadAsset` (probe via hidden `<video>`, register Blob), `addClip`, `trimClip`, `removeClip`, `moveClip`, `splitClip` (round-3 part A: split clip at timeline-time; both halves share assetId; non-destructive), `setClipColor` (round-5 part B), `getProject`, `setProject` (validate via composer-schema), `exportMp4` (canvas captureStream + MediaRecorder; WebM→MP4 fallback). Round-3 part A timeline UX: hover-× delete button → `sg-timeline:clip-deleted` (the Delete/Backspace keyboard shortcut originally shipped here was removed in Round-9-L per user feedback); S key splits selected clip at playhead → `sg-timeline:clip-split`; composer `play()` auto-advances playhead to the next clip when in dead space. Round-5 part B: clips render in 6 cycling distinct-hue shades (indigo / teal / amber / rose / purple / sky, `clip--shade-0..5`); optional per-clip `color` field overrides the auto-shade as inline style and is preserved across `splitClip`; new SgToolApi method `setClipColor({clipId, color})` (op: `setClipColor`, `color: null` removes override) wired through state-clip-ops.js + api-methods.js + manifest; timeline toolbar colour-picker control opens a swatch popover with 12 presets + native `<input type="color">` + "Use auto" reset. Single video track only (`t-video-1`); titles, crop, masks, transitions, multi-track audio deferred to Phases 2-4. Skills: `SKILL__{human,browser,api}.md`. `handoff/` directory reserved for Phase 4 cross-tool handoffs. No network, no localStorage. Image assets supported (PNG/JPEG/WebP/GIF), default 5-second display duration on drop. Round-6 part B: undo/redo via deep-clone snapshot stack (max 50 per side, oldest dropped on overflow) — every state mutation snapshots the pre-state into `state-history.js` (`createHistory({maxEntries})`); new SgToolApi methods `undo`/`redo`/`canUndo`/`canRedo`; new `Undo` / `Redo` toolbar buttons on `<sg-timeline>` (events `sg-timeline:undo-requested` + `sg-timeline:redo-requested`, enable-state plumbed via new `setHistoryFlags()` prop on the component); shell-level Cmd/Ctrl+Z and Cmd/Ctrl+Shift+Z (or Cmd/Ctrl+Y) global shortcuts via `ui-shortcuts.js`, skipped while focus is on INPUT/TEXTAREA/contenteditable. AssetRegistry blobs are shared across snapshots — undoing an `addAsset` leaves the blob inert in memory (cheap, harmless). Round-7-A: `addClip`/`moveClip`/`trimClip` throw `Error{code:'overlap'}` when the new placement would overlap another clip on the same track; failed mutations leave state and history unchanged so the timeline visually snaps back on the next render (overlap helper in `state-overlap.js`). Round-8-A: state + API + composer are now multi-track-aware — four new SgToolApi methods (`addTrack`, `removeTrack`, `moveClipToTrack`, `reorderTracks`) wired through `state-track-ops.js`, `api-track-methods.js` and the manifest; `tracks[]` is bottom-up (last index = top z-order); `removeTrack` refuses to drop the last video track; `moveClipToTrack` uses `wouldOverlap` against the target track. Composer renders the full track stack each tick (top wins) with audio from the top-most active unmuted clip. Timeline UI side (R8-B) still renders a single lane until the upcoming UI pass. Round-9-A: two new SgToolApi methods `setClipTransform({clipId, transform})` and `setClipCrop({clipId, crop})` (clamped via `clampTransform` / `clampCrop`; `transform=null` / `crop=null` removes the field) wired through `state-clip-ops.js`, `api-methods.js`, `sg-video-editor-api.js`, and the manifest; `splitClip` now copies `transform` + `crop` onto the right-half clip; `moveClipToTrack` already preserved them by virtue of moving the entire clip object. Probe helpers extracted from `api-methods.js` into a new sibling `api-probe.js`. Composer pipeline (draw + scheduler-frame + export-tick) honours the new fields with default values reproducing today's aspect-fit. The on-canvas overlay UI with drag handles is deferred to R9-B. Round-9-B: timeline toolbar gains three editor-mode buttons (`Select` / `Move` / `Crop`) via `timeline-mode-buttons.js`, dispatching the new `sg-timeline:editor-mode-requested` event; `<sg-timeline>` exposes `setEditorMode(mode)` so the shell can sync the highlighted-active state. The shell (`ui-shell.js` + new helpers `ui-shell-overlay.js` + `ui-shell-composer.js`) tracks `editorMode` locally, pushes the active clip (selection ∩ `timelineStart ≤ playhead < timelineEnd`, with `srcWidth`/`srcHeight` resolved from the asset entry) into `<sg-preview-canvas>.setActiveClip(...)` whenever selection / playhead / project changes, and routes `sg-preview:transform-requested` / `sg-preview:crop-requested` events back into `api.setClipTransform` / `api.setClipCrop`. Round-9-D snap-abut: `state-overlap.js` exports a new `snapToClearSlot(track, start, end, excludeClipId)` helper that, on overlap, returns a flush-abut start (after-the-neighbour by default; before if more of the proposed range sits to the left of the neighbour mid-point; falls back to the other side; returns `null` if both sides still collide). `addClip` / `moveClip` / `addShapeClip` / `addTextClip` accept an opt-in `snap: true` param routed through `api-methods.js` → `state.js` → `state-clip-ops.js` (`addClipOp` / `moveClipOp` / `addShapeClipOp` / `addTextClipOp`); when `snap` is true and the proposed range overlaps a neighbour, the op snap-abuts via `snapToClearSlot`, otherwise falls back to the existing `Error{code:'overlap'}`. The drag-drop wiring in `ui-shell-layout.js` (clip-added, clip-moved, clip-track-changed) and the `+ Shape` / `+ Text` toolbar in `ui-add-clip-buttons.js` pass `snap: true` so a slight overshoot snaps flush; programmatic API callers without `snap: true` keep the strict overlap rejection. Round-9-E drag-drop polish: (1) `moveClipToTrackOp` accepts optional `timelineStart` + `snap` params so a cross-track drag tests overlap against the user's chosen position on the destination (atomic); routed through `state.js` + `api-track-methods.js`; the shell layout's `onClipTrackChange` now does a single `api.moveClipToTrack({clipId, toTrackId, timelineStart, snap:true})` instead of the old chained `moveClipToTrack` → `moveClip` two-step (which mis-tested overlap against the source position). (2) `addTrackOp` accepts optional `insertAboveTrackId` so new tracks can land directly above a target lane instead of always at the top of the stack. (3) New `withOverlapAutoTrackAbove(api, targetTrackId, fn)` helper in `ui-shell-layout.js`: on `code:'overlap'` (snap couldn't clear), inserts a fresh video track ABOVE the target via `addTrack({insertAboveTrackId})` and retries with the user's chosen `timelineStart`; emits a `tool:toast` notification. Wired into `onAdded` (asset-panel drops) and `onClipTrackChange` (cross-track clip drags). (4) Asset-panel rows now toggle `is-dragging` (teal outline + 0.45 opacity, mirroring `.clip.is-dragging`) on the source card during a drag-out, cleared on `dragend` / drop / cancel / panel destroy. `<sg-timeline>` `dispatch(CLIP_SELECTED)` no longer re-renders the lanes when the clipId hasn't changed (avoids orphaning the captured `drag.clipEl`/`sourceLane` mid-trim) and `timeline-interactions.js` re-resolves the live source lane each pointermove for trim gestures so the in-drag readout overlay (ghost + label with `in/out/dur`) remains visible during clip resize. Round-9-F (selection / lock / rename / copy-paste / shortcuts): (1) Tracks are now selectable + lockable + renameable. New `setTrackLockedOp` + `renameTrackOp` in `state-track-ops.js`; `assertTrackUnlocked` exported and called from `addClipOp` / `trimClipOp` / `moveClipOp` / `removeClipOp` / `addShapeClipOp` / `addTextClipOp` and from `removeTrackOp` + `moveClipToTrackOp` (target AND source check). Locked ops throw `Error{code:'locked', trackId}`. Mute / lock-toggle / rename are NOT gated. New SgToolApi methods `setTrackLocked({trackId, locked})` and `renameTrack({trackId, name})`. UI: `<sg-timeline>` `timeline-track-headers.js` adds a lock 🔒/🔓 button next to M / ×, a click-to-select gesture on the header label / area, and an inline rename input via either double-click on the label or a hover-revealed ✎ pencil button (Enter saves, Escape cancels, blur saves, whitespace clears the override). New events `sg-timeline:track-lock-requested` / `sg-timeline:track-renamed` / `sg-timeline:track-selected` plumbed through `ui-shell-layout.js`. Selected-track state lives in `ctx.selectedTrackId` (UI-only); `<sg-timeline>` mirrors it via `setSelectedTrack(trackId)`. CSS: `.track-header--selected` (teal inset shadow), `.track-header--locked` (amber bg), `.lane--locked` (amber stripe pattern + not-allowed cursor), `.track-header__rename` (hover-reveal pencil), `.track-header__rename-input`. (2) `snapToClearSlot` accepts an optional `maxSnapDistance` (seconds) — `null` from the helper now also fires when both immediate sides exceed the cap. Plumbed through `state-clip-ops.js` (`addClipOp` / `moveClipOp` / `addShapeClipOp` / `addTextClipOp`) and `api-methods.js`. The `+ Shape` / `+ Text` toolbar passes `maxSnapDistance: 2` so a playhead-on-clip drop only snaps to the immediate edge — never walks 100s+ across a long clip; on no clear slot the existing `withOverlapAutoTrack` falls through and adds a fresh top track. The toolbar also prefers the user's selected track over `t-video-1` when one is selected. Drag-drop callers in `ui-shell-layout.js` keep the unlimited cap (the user is visibly aiming with the cursor). (3) Copy / paste clips. New `copyClipOp` + `pasteClipOp` in `state-clip-ops.js`; in-memory single-slot `clipboard` in `state.js` with `'clipboard'` event; new SgToolApi methods `copyClip` / `pasteClip` / `hasClipboard`. Toolbar Copy + Paste buttons via new `timeline-clipboard-buttons.js` (mounted between Split and + Track in `timeline-zoom.js`); enabled-state plumbed via new `setClipboardFlags()` on `<sg-timeline>`. New events `sg-timeline:clip-copied` (toolbar Copy + Cmd-drag) and `sg-timeline:clip-paste-requested` (toolbar Paste). Cmd-drag (Mac) / Ctrl-drag (Windows / Linux) on a clip body triggers a copy gesture: `timeline-interactions.js` detects `metaKey || ctrlKey` on `pointerdown` for `move` role, carries `isCopy` through the drag state, and on pointerup dispatches `CLIP_COPIED` instead of `CLIP_MOVED` / `CLIP_TRACK_CHANGED`; visual cue is an amber ghost (`.sgt-ghost.is-copy`) with a `[copy]` time read-out prefix. CSS also includes a `.drop-indicator.is-copy` style with a `+` badge for future asset-panel-drop copy support. The shell wires `clip-copied` → `api.copyClip` then optionally `api.pasteClip(toTrackId, timelineStart, snap:true)` for the drag case; `clip-paste-requested` → `api.pasteClip` resolved against `ctx.selectedTrackId` ?? first video track ?? `t-video-1`, time = playhead. Locked target tracks reject with the toast bus; missing-asset payloads reject with `invalid-arg`. (4) Global keyboard shortcuts via new `ui/ui-keyboard.js` (`attachGlobalShortcuts({api, getSelectedClipId, getSelectedTrackId, getPlayhead, getComposer})`): Space → composer play/pause toggle; Cmd/Ctrl+C → `api.copyClip` on the selected clip; Cmd/Ctrl+V → `api.pasteClip` at selected track + playhead. Text-entry guard skips when `document.activeElement` is INPUT/TEXTAREA/SELECT/contenteditable INCLUDING shadow-DOM descendants (so the rename-track inline input doesn't steal Cmd+V). Cmd/Ctrl+Z and Delete remain disabled here per the user's earlier preference (commit 83d004f). (5) Shortcuts cheatsheet panel via new `ui/ui-shortcuts-panel.js` mounted in a new bottom-left layout slot — the left column changed from a single Assets stack to a `column { Assets (70%), Shortcuts (30%) }` mirroring the centre-column split. Static read-only cheatsheet listing all 8 shortcut bindings. CSS in `styles/sg-video-editor.css`: `.sgve-shortcuts`, `.sgve-sc-panel`, `.sgve-sc-key` (teal chip), `.sgve-sc-glue` (separators). Round-9-G (preview refresh + redraw button): (1) Bug fix — the preview no longer stays black after the user drops the first asset onto an empty timeline at time 0. Root cause was in `composer-playback.seek()`: when the freshly-created `<video>` was at `readyState=0`, the only paint trigger was a `seeked` listener — but assigning `currentTime = 0` on a not-yet-loaded video where the existing `currentTime` is also 0 isn't a real seek and doesn't fire `seeked`, so the canvas stayed black until the user pressed Space (which forces `v.play()` and a real load). Fix is the new `waitForVideoFrame(video, cb)` helper that races `seeked` + `loadeddata` + `canplay` and cleans up siblings on first fire. (2) New `composer.refresh()` method on the playback handle synchronously re-paints the current frame via `sched.paintAt(playhead)`. (3) New SgToolApi method `refreshPreview()` on the tool — registered through `api-methods.js` + `sg-video-editor-api.js` + manifest — calls `composer.refresh()`; returns `{ ok }` (false when no composer is attached). (4) `<sg-preview-canvas>` transport bar gains a fourth button (↻ "Redraw current frame") inserted after fast-forward via `preview-transport.buildTransport`; `wireTransport` wires its click to `composer.refresh()`; `setTransportEnabled(false)` leaves the redraw button enabled at all times. Round-9-H (persistence + lock-fix + selection polish): (1) **Editable project name + Project section** — `project.project.name` is now first-class. New `renameProjectOp(project, {name})` in new `ui/state-project-ops.js` (trims whitespace; empty falls back to `'Untitled'`); state surface gains `renameProject(params)` with history snapshot + operations log entry; new SgToolApi method `renameProject({name})`. The Properties pane previously rendered an empty placeholder when nothing was selected — now it renders a "Project" section with the editable name (single-line input; Enter / blur saves, Escape cancels; same UX as the track-rename inline input). The properties panel also now skips re-rendering while a focused input lives inside it (avoid steal-focus mid-edit). Field/row builders extracted from `ui-properties-panel.js` into `ui/ui-prop-fields.js` (~170 LOC); the project section lives in `ui/ui-prop-project.js` (~50 LOC) so future per-project settings (fps, output resolution, …) plug in there as one-block extensions. New `inlineRenameInput(...)` helper centralises the Enter/Escape/blur pattern. (2) **Manual save / load to localStorage** — new `ui/state-storage.js` (pure, no DOM) defines the storage layout: `sgve:project:<slug>` for the JSON-stringified wrapped project, `sgve:projects-index` for `Array<{slug, name, savedAt, byteSize}>` (sorted newest-first), `sgve:autosave:current` for autosave. `slugify(name)` lowercases + collapses non-alphanumeric runs to `-` + trims edges (empty → `'untitled'`). Asset BLOBS DO NOT serialise — `stripAssetsForStorage(p)` removes `blob`/`file`/`objectUrl`, keeps id/name/mime/assetType/duration/width/height/bytes; on restore each asset is tagged `__missingBlob: true` for the asset panel. Soft 4 MB byte guard throws `Error{code:'too-large'}`. Cheap dirty hash: `String(json.length) + ':' + json.slice(0, 256)` — used by `state.hasUnsavedChanges()`. New state methods on `state.js`: `replaceProject(p)` (pushes history snapshot + emits `project:replaced` + `change` + marks clean), `markSaved()`, `markDirty()`, `hasUnsavedChanges()`, `getLastMutationAt()`. Nine new SgToolApi methods (registered via new `api/api-storage-methods.js`): `saveProject`, `loadProject`, `listSavedProjects`, `deleteSavedProject`, `hasUnsavedChanges`, `autosave`, `getAutosave`, `discardAutosave`, `isAutosaveNewer`. UI in `ui/ui-save-load.js` (Save / Save As… / Load / Delete / New buttons + saved-projects list with time-ago + byte-size); native `confirm()` for overwrite + dirty prompts. Mounted into a "Save / Load" section beneath Project name in `ui/ui-prop-project.js`. (3) **beforeunload guard** — `ui/ui-shell.js` registers `window.addEventListener('beforeunload', …)` that prevents unload when `state.hasUnsavedChanges()` is true OR when the most recent mutation is newer than the last autosave flush (i.e. inside the debounce window); shows the browser's native confirm. Listener is removed in `destroy()`. (4) **Debounced autosave** — new `ui/ui-autosave.js` (`attachAutosave({state, api, debounceMs=750})`) listens for non-transient `change` events and schedules `api.autosave()` after 750ms of idle. Transient mutations (drag scrubs) are skipped. On editor init the helper checks `api.getAutosave()` against `api.isAutosaveNewer({savedAt})` and prompts the user once via `confirm()` ("Restore unsaved work from N seconds ago?"); Yes → `api.setProject({project})`; No → `api.discardAutosave()`. After a successful manual `saveProject` the autosave slot is cleared. (5) **Auto-select parent track when a clip is selected** — `ui/ui-shell-layout.js`'s `onSelected` handler now walks the project tracks to find the parent of the newly-selected clip and mirrors it into `ctx.selectedTrackId` + `timelineEl.setSelectedTrack(...)`. Clip de-selection (`clipId === null`) intentionally leaves `selectedTrackId` alone. Independent track-header click still overrides via the existing `onTrackSelected` handler. (6) **Lock-check on `splitClipOp`** — `ui/state-split.js` now imports `assertTrackUnlocked` and calls it on `loc.track.id` BEFORE any mutation. Bug fix: the lock-sweep in commit `d412dda` covered `state-clip-ops.js` but missed `splitClipOp` (extracted in `5eaa153` prior to the lock work). Locked tracks now reject `splitClip` with `Error{code:'locked', trackId}`. The setTrackLocked SKILL doc + manifest description for splitClip updated accordingly. CSS additions in `styles/sg-video-editor.css`: `.sgve-prop-btn--primary` (teal), `.sgve-prop-btn--secondary` (neutral), `.sgve-prop-rename` (full-width inline input), `.sgve-saveload` + variants. Round-9-J (asset blobs in IndexedDB): asset bytes (videos / images) now persist to IndexedDB so a reload restores full pixels — Round-9-H only persisted project metadata to localStorage, leaving every clip as a `__missingBlob` placeholder after refresh. New module `ui/state-asset-storage.js` wraps the browser's IDB with a small async key→blob API: `saveAsset({id, blob, name, kind, mimeType, width, height, duration, bytes})`, `loadAsset(assetId)`, `deleteAsset(assetId)`, `listAssetIds()`, `pruneOrphanedAssets(usedIds)`, `computeAssetStorageUsage()`, `clearAllAssets()`. Database name `sgve`, object store `assets`, keyed by asset id. Pattern mirrors the wheel cache in `tools/.../sg-send-cli/index.html` rather than the heavier `core/sg-vfs/v0.1.0/sg-vfs-provider-indexeddb.js` (the VFS provider models hierarchical paths + folders, overkill for our flat key→blob map). `api/api-storage-methods.js` extended: `saveProject` / `loadProject` / `autosave` / `deleteSavedProject` are now ASYNC — they `await persistBlobsToIdb(state)` before localStorage writes (saves), `await hydrateBlobsFromIdb(state)` after restore (loads), and `await pruneOrphanedAssets(referencedIds)` after deletes (the GC sweep computes the union of asset ids across every saved project + autosave + the live project, never evicting a still-referenced blob). Two new SgToolApi methods: `hydrateAssets()` (used by the autosave-restore path after `setProject({project: slot.project})` since that path doesn't go through `loadProject`'s built-in hydration) and `getStorageUsage()` (returns `{totalBytes, assetBytes, assetCount, projectJsonBytes}` for the new "Storage" line in the Properties pane). State container (`ui/state.js`): `replaceProject(next)` now `assetRegistry.clear()`s before swapping (stale registry blobs from the previous project would mask the IDB hydration); new `attachAssetBlobs(blobsById)` method writes blobs into the registry, clears the `__missingBlob` flag on each matched asset, and emits a single `change`. UI: `ui/ui-save-load.js` and `ui/ui-autosave.js` rewritten to `await` every SgToolApi call (SgToolApi's `_invoke` is itself async-by-default, so even synchronous registered methods returned a Promise that the Round-9-H code wasn't awaiting — that bug silently hid the saved-projects list); `ui/ui-storage-usage.js` is a new ~50-LOC module mounted under Save/Load that polls `api.getStorageUsage()` and renders `"X.XX MB (N assets)"`. `ui/asset-row.js` adds a visible "missing — re-upload" hint and an `is-missing` CSS class with a striped thumb for assets still flagged `__missingBlob` after hydration (e.g. pre-Round-9-J saves where blobs were never written to IDB). `ui/state-storage.js`: soft byte limit dropped from 4 MB to 1 MB — the project JSON without blobs is small (a 100-clip project with 30-char names is ~10 KB); the cap now catches pathological growth before localStorage chokes rather than acting as the de-facto blob ceiling. `manifest.json` + `skills/SKILL__api.md` updated with async tags, IDB layout, and the two new methods. Backwards compatibility: pre-Round-9-J saved projects still load; their assets simply stay flagged `__missingBlob` (the user can re-upload a fresh file with the same id-shape and re-save to write into IDB). Verified via Node-only tests with a fake-IDB shim (30 assertions across `state-asset-storage` round-trips + `api-storage-methods` save→load roundtrip + `deleteSavedProject` orphan pruning + `getStorageUsage`); browser walk-through pending the user. Round-9-I (UX polish + export, shipped before Round-9-J in chronological order — the headline was reserved alphabetically; this paragraph appears after Round-9-J in the doc only because the agents committed in interleaved order): five tasks. (1) **Timeline scroll** — `<sg-timeline>` `:host` now sets `height: 100%; overflow: hidden` and the inner `.root` viewport runs `overflow: auto` on both axes with `overscroll-behavior: contain` so the wheel never chains to the surrounding page. `.ruler` switched from `position: relative` to `position: sticky; top: 0; z-index: 6` so it stays pinned during vertical scroll while clips slide underneath; `.track-header` keeps its `position: sticky; left: 0` rail for horizontal scroll but loses the stray `top: 0` that previously popped it under the ruler on vertical scroll; `.playhead` bumped z-index 5 → 8 so it stays above the now-positioned ruler. Result: vertical and horizontal scrollbars work inside the timeline, ruler pins on Y, headers pin on X, neither overlaps the other on diagonal scroll. (2) **Compact lanes** — `LANE_HEIGHT` 80 → 44px (with `var(--sgt-lane-height)` for any future override). Clip vertical gutter 8 → 4px; clip-label tightened (`right: 22px` reserves space for the hover-× delete button so the text never collides with it); duration glyph 10 → 9px; clip-delete 18×18 → 16×16; track-header padding 4 → 2 vertical; M / 🔒 / × buttons 22×20 → 20×18 with 10px font. `:host` `min-height` 148 → 110 (toolbar + ruler + one lane fits comfortably). (3) **Distinct track colours** — new `ui/state-track-palette.js` re-exports the 6-shade contrast palette (indigo / teal / amber / rose / purple / sky — same colours the clip auto-shade uses) with two helpers: `pickTrackColor(i)` (cycle by track index) and `ensureTrackColors(project)` (in-place assign defaults to any track missing `track.color`, idempotent). `state-init.createInitialProject` seeds the bootstrap `t-video-1` track with `color: pickTrackColor(0)`; `state-init.validateWrapped` runs `ensureTrackColors` so legacy / freshly-loaded projects pick up colours without flickering on first render. `state-track-ops.addTrackOp` auto-assigns `pickTrackColor(insertAt)` on every fresh track and accepts an optional `params.color` override. New `state-track-ops.setTrackColorOp(project, {trackId, color})` — `color: null` re-applies the auto palette pick. New SgToolApi method `setTrackColor({trackId, color})` registered through `api-track-methods.buildTrackMethods` + `api-methods.js` + `sg-video-editor-api.js` + manifest + SKILL_api.md. The new method is the ONLY track method that goes through `state.getProject() → setTrackColorOp(project) → state.setProject(project)` rather than a dedicated state container method (Round-9-J's persistence agent owned `state.js` for this batch — keeping the diff there at zero avoided the merge cost; tradeoff is one full validate + project-level history snapshot per recolour rather than a focused op log entry, acceptable for an infrequent cosmetic change). Render priority for clip rectangles: `clip.color → track.color → palette[trackIndex % 6]` (CSS `clip--shade-N` class). `timeline-lane-render.buildClipEl` accepts the owning track and applies `track.color` as inline `style.background` when no `clip.color` override exists; the `clip--shade-N` class still gets set so a per-clip "use auto" reset falls back without DOM surgery. `timeline-track-headers.renderTrackHeader` exposes `track.color` as a `--track-color` CSS custom property; `sg-timeline.css` paints a 3px `::before` strip on `.track-header` from that variable so empty / muted / locked lanes still surface the track colour. Locked tracks may still be recoloured (cosmetic, mirrors the existing rename / mute policy from `d412dda`). (4) **Export button width + progress bar** — the export button used to jitter between "Export MP4" (idle) and "Exporting NN%" (busy). New `ui/ui-export-progress.js` (~95 LOC, `mountExportButton({host})`) measures the longest possible label ("Exporting 100%") into a hidden probe span at mount time and pins `min-width = label + 28px` so the width never reflows; an inner `.sgve-export-btn__label` span uses `font-feature-settings: "tnum"` (tabular numerals) so digit widths stay even across "Exporting 9%" → "Exporting 99%". A `--sgve-progress` CSS custom property drives an `::before` pseudo-element width (teal at 32% alpha) for the L→R fill behind the centred label. `ui-export-controls.js` refactored to consume the handle (`setLabel` / `setProgress` / `setBusy` / `setStatus`) instead of inline-style strings. CSS in `styles/sg-video-editor.css`: `.sgve-export-btn` (position:relative + overflow:hidden + text-align:center), `.sgve-export-btn::before` (the fill), `.sgve-export-btn.is-exporting`, `.sgve-export-btn__label` (above the fill), `.sgve-export-status`. (5) **Post-export Download + Drag-out** — once the export pipeline returns a Blob the orchestrator (now a 122-LOC state machine in `ui-export-controls.js`) flips from the single button to a row of three affordances: **Download** (the standard `URL.createObjectURL` + invisible `<a download>` click; URL revoked 4s after click), **Drag handle** (exposes BOTH SG-native and OS-native drag data — SG-native carries `application/x-sg-asset` MIME with a JSON descriptor `{kind:'export-mp4', source:'sg-video-editor', name, mime, size, createdAt}` so descriptor-aware SG receivers can special-case it; OS-native pushes a real `File` via `DataTransferItemList.add(file)` so the desktop, `<sg-upload-dropzone>`, and any plain `dataTransfer.files` receiver work — that's the contract `<sg-upload-dropzone>` reads), and **Re-export** (link-style; bounces back to idle so the user can change the project and run again). The drag-out logic lives in new `ui/ui-export-actions.js` (~175 LOC). Errors during export bounce back to idle with the message in `setStatus(...)` rather than a fourth state. CSS in `styles/sg-video-editor.css`: `.sgve-export-actions` (flex row, gap 0.4rem), `.sgve-export-download` (teal-tinted primary), `.sgve-export-drag` (`cursor: grab` / `:active` + `.is-dragging` → `cursor: grabbing` + amber tint), `.sgve-export-reexport` (transparent / link-styled). Files split per the session-wide directive: `ui-export-controls.js` (orchestrator, 122 LOC), `ui-export-progress.js` (idle/exporting button, 94 LOC), `ui-export-actions.js` (post-export row, 175 LOC) — each file has a single responsibility, none crosses 200 LOC. Verified Node-only: palette cycling + `ensureTrackColors` idempotency for Task 3, `extFor` mime → extension map for Task 5; UI / CSS for Tasks 1, 2, 4 are by-inspection only (no headless browser available). Side-effect concerns: the parallel Round-9-J agent accidentally swept my Task 4 CSS additions into their `f4b4827` commit (they ran `git add -A` while my work was unstaged) — the visual styling shipped under Round-9-J's name but the matching JS landed in this batch's Round-9-I Task 4 commit; behaviour is correct end-to-end. Round-9-K (3 surgical fixes): (1) **Save toast wrong filename** — `ui/ui-save-load.js` now flushes any focused INPUT/TEXTAREA via `flushFocusedInput()` at the top of `onSaveClick` + `onSaveAsClick` so the inline-rename Name input commits BEFORE the save reads `project.name`. Without the flush a click on Save while the input still had focus could race the blur-driven `api.renameProject` against the click handler's `getProject().name` read, leaving the toast announcing the OLD name. (2) **`beforeunload` fires after clean save** — two paired fixes. (a) `state.markSaved(savedJson?)` now accepts the exact JSON that hit storage (returned by `saveProjectToStorage` and the new `writeAutosave` return value `{savedAt, json}`); both manual-save + autosave pass `r.json` through. The dirty-baseline hash now matches on-disk bytes instead of "the live project at the moment markSaved ran", which closes the race where a mutation lands during the IDB await and gets falsely stamped as the new clean baseline. (b) `ui/ui-shell.js` `onBeforeUnload` drops the secondary `lastMutationAt > autosave.lastFlushAt` check that was firing on every reload after a successful manual save (manual-save updates `lastSavedHash` but not `autosave.lastFlushAt`, so the secondary check stayed true even when the project was cleanly saved). The hash-based `state.hasUnsavedChanges()` is now the sole source of truth for the guard. (3) **Track vs clip selection visual prominence** (`components/sg-timeline/.../sg-timeline.css`) — clip selection bumped 2px outline → 2.5px outline + soft outer teal glow (`box-shadow: 0 0 12px 2px rgba(20,184,166,0.55)`) + `z-index: 3`; track selection (`.track-header--selected` + `.lane-row--selected .lane`) dropped from full-alpha 2px inset to a 1px @ 40-45% alpha border + a 10% teal background tint on the lane. Same hue (teal) so header + lane still read as one selection, but the clip ring clearly dominates against every palette colour. Verified via Node sanity tests: 3 toast-name assertions + 13 dirty-check assertions covering manual save, autosave, post-mutation reload-handler-fires, and the markSaved race-protection. CSS hierarchy verified by inspection — clip-selection box-shadow renders outside the clip's `overflow: hidden` border-box (correct per spec), specificity of `.lane-row--selected .lane` matches the muted-lane rule and wins by source order. Browser walk-through pending the user. Round-9-L (clip-delete keyboard shortcut removed): the Delete/Backspace clip-removal shortcut in `components/sg-timeline/v0/v0.1/v0.1.0/timeline-keyboard.js` was removed — clip deletion still ships via the hover-× button on each clip and via `api.removeClip(...)` programmatically; the user reasoned "deleting a clip is quite a big thing and we already have a button for it" (same precedent as commit `83d004f`, which dropped the Cmd/Ctrl+Z undo/redo shortcuts after similar feedback). The shared `timeline-focus.js` helper (`isTextEntryFocus()`, shadow-DOM-piercing) is RETAINED — it still guards the remaining S split shortcut as well as the global Space / Cmd+C / Cmd+V handlers in `ui/ui-keyboard.js`. Round-9-M (memory observability + preload fix): (1) `core/video-composer/composer-playback.js` flips `<video>.preload` from `'auto'` to `'metadata'` so dragging a large (100+ MB high-res) video onto the timeline no longer eagerly decodes ahead — the active clip still loads on `seek()` / `play()` so playback latency is unchanged; inactive clips no longer pay the decode-buffer tax. (2) New `?debug=1` query flag exposes a Debug tab in the Properties pane (`ui/debug/ui-debug-panel.js`) showing live clip count, composer + DOM `<video>` element counts, live blob URL count (via wrapped `URL.createObjectURL` / `revokeObjectURL` counters in `ui/debug/memory-probe.js`), composer rebuild counter, IDB usage, JS heap (`performance.memory`, Chrome only), and an asset list with intrinsic dimensions + bytes. `[sgve]`-prefixed console logs at asset-added, clip-added / removed, and composer-attach (heap snapshots throttled to 5s). (3) Known architectural issue flagged but NOT fixed: `ui-shell.handleChange` rebuilds the entire composer on every non-transient mutation, which transiently stacks two decode pipelines for the same blob — the proper fix is an asset-keyed `<video>` registry lifted out of the composer; out of scope for a single-session fix. |

### Shared Assets

| Asset | Path | Notes |
|---|---|---|
| Shared CSS | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/_common/css/tools.css` | **FROZEN** — Design tokens + base styles (same as v0.1.0) |
| Shared CSS | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/_common/css/tools.css` | Design tokens + base styles |
| sg-nav.js | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/_common/js/sg-nav.js` | **NEW** — Environment-aware cross-site nav (`getSgraphBaseUrl`, `detectLocale`, `buildToolsNav`) |
| sg-tool-registry.js | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.25/_common/js/sg-tool-registry.js` | **NEW** — Tool manifest loader. Exports `TOOL_SLUGS` (24 slugs), `CATEGORIES` (6 categories with audience tags), `loadManifests()` (parallel fetch via `Promise.allSettled`), `groupByCategory()`, `renderToolCard()`. Powers the data-driven landing page. |
| sg-tool-registry.js | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.47/_common/js/sg-tool-registry.js` | **UPDATED v0.1.47** — Registry fix: adds `speed-test`, `playbooklm`, `page-builder` to `TOOL_SLUGS` (28 total). Adds `misc` category (audience: user) for speed-test. Adds `ai` category (label: 'AI Pipelines', audience: user) for playbooklm + page-builder. Fixes speed-test `manifest.json` category from invalid `"tools"` → `"misc"`. All three tools now visible on the landing page. |
| sg-tool-manifest.js | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.25/_common/js/sg-tool-manifest.js` | **NEW** — `<sg-tool-manifest>` Shadow DOM web component. Fetches manifest from `src` attribute, renders collapsible panel showing tool name, version, status badge, and dependency table (core / components / shared / external). Graceful degradation on fetch failure. |

### i18n

| Item | Path | Notes |
|---|---|---|
| Master translations | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/i18n/en-gb.json` | 35 keys |
| en-US translations | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/i18n/en-us.json` | Starter locale |
| fr-FR translations | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/i18n/fr-fr.json` | Starter locale |
| de-DE translations | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/i18n/de-de.json` | Starter locale |
| es-ES translations | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/i18n/es-es.json` | Starter locale |

### Manifests

| Module | Path |
|---|---|
| sg-crypto | `sgraph_ai_tools__static/core/crypto/v1/v1.0/v1.0.0/manifest.json` |
| sg-ssh | `sgraph_ai_tools__static/core/ssh/v1/v1.0/v1.0.0/manifest.json` |
| sg-header | `sgraph_ai_tools__static/components/header/v1/v1.0/v1.0.0/manifest.json` |
| sg-footer | `sgraph_ai_tools__static/components/footer/v1/v1.0/v1.0.0/manifest.json` |
| sg-component | `sgraph_ai_tools__static/components/base/v1/v1.0/v1.0.0/manifest.json` |
| sg-tokens | `sgraph_ai_tools__static/components/tokens/v1/v1.0/v1.0.0/manifest.json` |
| sg-site-header v1.0.0 | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.0/manifest.json` |
| sg-site-header v1.0.1 | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.1/manifest.json` |
| sg-site-footer | `sgraph_ai_tools__static/components/site-footer/v1/v1.0/v1.0.0/manifest.json` |
| sg-locale-picker v1.0.0 | `sgraph_ai_tools__static/components/locale-picker/v1/v1.0/v1.0.0/manifest.json` |
| sg-locale-picker v1.0.1 | `sgraph_ai_tools__static/components/locale-picker/v1/v1.0/v1.0.1/manifest.json` |
| sg-upload-dropzone | `sgraph_ai_tools__static/components/upload-dropzone/v1/v1.0/v1.0.0/manifest.json` |
| sg-key-generator | `sgraph_ai_tools__static/components/key-generator/v1/v1.0/v1.0.0/manifest.json` |
| sg-key-input | `sgraph_ai_tools__static/components/key-input/v1/v1.0/v1.0.0/manifest.json` |
| sg-pbkdf2 | `sgraph_ai_tools__static/core/pbkdf2/v1/v1.0/v1.0.0/manifest.json` |
| sg-wordlist | `sgraph_ai_tools__static/core/wordlist/v1/v1.0/v1.0.0/manifest.json` |
| sg-vault-client | `sgraph_ai_tools__static/core/vault-client/v1/v1.0/v1.0.0/manifest.json` |
| sg-pyodide | `sgraph_ai_tools__static/core/pyodide/v1/v1.0/v1.0.0/manifest.json` |
| tools UI v0.1.0 | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.0/manifest.json` |
| tools UI v0.1.1 | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.1/manifest.json` |
| tools UI v0.1.2 | `sgraph_ai_tools__static/tools/v0/v0.1/v0.1.2/manifest.json` |

### Tool Manifests (v0.1.25 — NEW)

Per-tool `manifest.json` files declaring identity, category, status, keywords, and dependencies (core/components/shared/external). Generated by `scripts/generate_manifests.py` from HTML import analysis. 24 tools covered across IFD versions v0.1.4 through v0.1.24. Used by the manifest-driven landing page (v0.1.25) and the `<sg-tool-manifest>` web component.

| Tool | Manifest Path | Version | Category |
|---|---|---|---|
| SSH Key Generator | `tools/v0/v0.1/v0.1.4/en-gb/ssh-keygen/manifest.json` | 0.1.4 | security |
| File Hasher | `tools/v0/v0.1/v0.1.4/en-gb/file-hasher/manifest.json` | 0.1.4 | security |
| File Encryptor | `tools/v0/v0.1/v0.1.4/en-gb/file-encryptor/manifest.json` | 0.1.4 | security |
| Key Generator | `tools/v0/v0.1/v0.1.4/en-gb/key-generator/manifest.json` | 0.1.4 | security |
| Chat | `tools/v0/v0.1/v0.1.22/en-gb/chat/manifest.json` | 0.1.22 | llm |
| One-Shot Chat | `tools/v0/v0.1/v0.1.8/en-gb/one-shot-chat/manifest.json` | 0.1.8 | llm |
| Infographic Generator | `tools/v0/v0.1/v0.1.24/en-gb/infographic-gen/manifest.json` | 0.1.24 | llm |
| Multi-Agent Chat | `tools/v0/v0.1/v0.1.13/en-gb/multi-agent-chat/manifest.json` | 0.1.13 | llm |
| Agentic Loop | `tools/v0/v0.1/v0.1.20/en-gb/agentic/manifest.json` | 0.1.20 | llm |
| Model Compatibility | `tools/v0/v0.1/v0.1.20/en-gb/model-compatibility/manifest.json` | 0.1.20 | llm |
| Image Tools | `tools/v0/v0.1/v0.1.5/en-gb/image-tools/manifest.json` | 0.1.5 | media |
| Video Tools | `tools/v0/v0.1/v0.1.5/en-gb/video-tools/manifest.json` | 0.1.5 | media |
| Vault | `tools/v0/v0.1/v0.1.16/en-gb/vault/manifest.json` | 0.1.16 | vault |
| Vault Browser | `tools/v0/v0.1/v0.1.4/en-gb/vault-browser/manifest.json` | 0.1.4 | vault |
| OpenRouter | `tools/v0/v0.1/v0.1.22/en-gb/openrouter/manifest.json` | 0.1.22 | vault |
| SG/Send CLI | `tools/v0/v0.1/v0.1.4/en-gb/sg-send-cli/manifest.json` | 0.1.4 | dev |
| Python REPL | `tools/v0/v0.1/v0.1.4/en-gb/pyodide-repl/manifest.json` | 0.1.4 | code |
| Folder Editor | `tools/v0/v0.1/v0.1.23/en-gb/folder-editor/manifest.json` | 0.1.23 | code |
| LLM Dev Environment | `tools/v0/v0.1/v0.1.8/en-gb/llm-dev/manifest.json` | 0.1.8 | code |
| VFS Dev | `tools/v0/v0.1/v0.1.17/en-gb/vfs-dev/manifest.json` | 0.1.17 | dev |
| VFS Tree Demo | `tools/v0/v0.1/v0.1.21/en-gb/vfs-tree-demo/manifest.json` | 0.1.21 | dev |
| SG Tree Demo | `tools/v0/v0.1/v0.1.18/en-gb/sg-tree-demo/manifest.json` | 0.1.18 | dev |
| Send Simulator | `tools/v0/v0.1/v0.1.22/en-gb/send-sim/manifest.json` | 0.1.22 | dev |
| Vault (Pyodide) | `tools/v0/v0.1/v0.1.4/en-gb/vault-pyodide/manifest.json` | 0.1.4 | dev |

### Scripts

| Script | Path | Notes |
|---|---|---|
| generate_manifests.py | `scripts/generate_manifests.py` | **NEW** — Parses each tool's `index.html`, extracts `<script src>` and ES module `import` paths, classifies dependencies (core/components/shared/external), and writes `manifest.json` alongside each tool. Metadata (name, description, icon, status, category) defined in-script from landing page data. |

### Tests

| Test | Path | Notes |
|---|---|---|
| Test runner index | `sgraph_ai_tools__static/tests/index.html` | **NEW** — Lists all browser-based test pages |
| sg-vault-client tests | `sgraph_ai_tools__static/tests/test-vault-client.html` | **NEW** — 40+ assertions: parseVaultKey, deriveVaultKeys, deriveFileId, 2 cross-language test vectors, determinism, uniqueness |
| sg-llm-request tests | `sgraph_ai_tools__static/tests/sg-llm/request.test.html` | **NEW** — custom element, mock non-streaming request, 401 error, AbortController cancel |
| sg-llm-connection tests | `sgraph_ai_tools__static/tests/sg-llm/connection.test.html` | **NEW** — custom element, provider tabs, localStorage persistence |
| sg-llm-output tests | `sgraph_ai_tools__static/tests/sg-llm/output.test.html` | **NEW** — streaming chunks, complete, error, markdown render, BUNDLE_LOADED restore |
| sg-llm-reality-core tests | `sgraph_ai_tools__static/tests/sg-llm/reality-core.test.html` | **NEW** — BLOCK_TYPES, createBlock (all 5 types), estimateTokens, assembleMessages (all block types + multimodal + disabled), assembleMemoryUpdateMessages |
| sg-llm-block tests | `sgraph_ai_tools__static/tests/sg-llm/reality-block.test.html` | **NEW** — all 5 block types, setData/getData round-trip, block-change/remove/toggle events, history add/remove pairs, token count, setImageCount |
| sg-llm-reality tests | `sgraph_ai_tools__static/tests/sg-llm/reality.test.html` | **NEW** — block management, REALITY_CHANGED detail, send modes, memory update, attachment handling, bundle restore, getState/setState |
| sg-llm-attachments tests | `sgraph_ai_tools__static/tests/sg-llm/attachments.test.html` | **NEW** — text/image attachment, cache, remove, clear all, model warning, bundle restore |
| sg-llm-stats tests | `sgraph_ai_tools__static/tests/sg-llm/stats.test.html` | **NEW** — estimate display, REQUEST_COMPLETE metrics, session totals accumulation, speed calculation, streaming toggle, error/cancel |
| sg-llm-debug tests | `sgraph_ai_tools__static/tests/sg-llm/debug.test.html` | **NEW** — SEND capture, mode/status tracking, multi-request isolation, REQUEST_COMPLETE/ERROR/CANCEL, DOM entry rendering, colour-coded badges, expand/collapse, clear |
| sg-llm-bundle tests | `sgraph_ai_tools__static/tests/sg-llm/bundle.test.html` | **NEW** — save/load/delete, parent_id chain (3-bundle), auto-save, index management, event-triggered save/load/delete, BUNDLE_SAVED/LOADED/DELETED/LIST_CHANGED events |
| sg-llm-bundle-list tests | `sgraph_ai_tools__static/tests/sg-llm/bundle-list.test.html` | **NEW** — list rendering from bundle manager, newest-first order, active highlighting, BUNDLE_LOAD_REQUESTED/delete events, max-entries limit, show-save-button, list refresh on BUNDLE_DELETED |
| sg-llm-infographic tests | `sgraph_ai_tools__static/tests/sg-llm/infographic.test.html` | **NEW** — custom element, style presets, orientation, SVG extraction (REQUEST_COMPLETE/CHUNK), live streaming preview, INFOGRAPHIC_READY emission, SVG sanitisation, getCurrentSvg API, export button states, INFOGRAPHIC_EXPORTED |
| sg-llm-model-picker tests | `sgraph_ai_tools__static/tests/sg-llm/model-picker.test.html` | **NEW** — 12 assertions: custom element, shadow root, window export, anthropic builtins, getModel, setModel, llm:model-changed event, llm:connected auto-load, Ollama mock, OpenRouter mock, refresh reload, failed fetch preserves models |
| sg-llm-token-viz tests | `sgraph_ai_tools__static/tests/sg-llm/token-viz.test.html` | **NEW** — 17 assertions: custom element, window export, shadow root, getStats() initial state, SEND→estimate, REQUEST_COMPLETE→actuals, cost for known model, session accumulation, sparkline SVG, breakdown sections (system/history/images), unknown model cost=0, REQUEST_ERROR no history entry, clear() reset, session cost sum, shadow DOM token display |
| sg-llm-chat-history tests | `sgraph_ai_tools__static/tests/sg-llm/chat-history.test.html` | **NEW** — custom element, system prompt, message assembly (text/image/file), SEND emission, CONNECTED/MODEL_CHANGED, streaming (REQUEST_START/CHUNK/COMPLETE/ERROR), clear, exportMarkdown |
| sg-llm-chat-input tests | `sgraph_ai_tools__static/tests/sg-llm/chat-input.test.html` | **NEW** — send button, Enter key, Shift+Enter no-send, empty no-send, disable/enable, REQUEST_START/COMPLETE/ERROR, cancel, attachment add/remove, clear, focus |
| **Tool smoke tests** | `sgraph_ai_tools__static/tests/sg-llm/tools/` | **NEW** — Integration smoke tests for each tool page |
| Chat tool smoke test | `sgraph_ai_tools__static/tests/sg-llm/tools/chat.test.html` | **NEW** — 17 assertions: 7 element registrations, bus DOM, shadow roots (5), chat-history structure (messages/system-bar/empty-state), chat-input structure (textarea/send-btn), public API (getMessages, setSystemPrompt, clear, showSaveButton) |
| One-Shot Chat smoke test | `sgraph_ai_tools__static/tests/sg-llm/tools/one-shot-chat.test.html` | **NEW** — 17 assertions: 9 element registrations, bus DOM, shadow roots (6), reality.getState(), stats.getStats(), debug.getEntries() |
| Infographic Generator smoke test | `sgraph_ai_tools__static/tests/sg-llm/tools/infographic-gen.test.html` | **NEW** — 15 assertions: 6 element registrations, bus DOM, infographic shadow DOM, style-preset attr, orientation attr, getSystemPrompt() non-empty + contains 'SVG', 6 style options, setStyle(), style-specific prompts differ, getCurrentSvg() null, export buttons disabled |
| LLM Dev Environment smoke test | `sgraph_ai_tools__static/tests/sg-llm/tools/llm-dev.test.html` | **NEW** — 21 assertions: 9 element registrations, 9 bus DOM presence, shadow roots (6), reality/stats/debug/bundle public APIs, bundle-list.showSaveButton, event bus isolation (all share parentElement) |
| Test suite index | `sgraph_ai_tools__static/tests/index.html` | **UPDATED** — Comprehensive test index with 5 sections: LLM Tools smoke tests (4), sg-llm Chat Components (2), sg-llm Core Components (13), sg-layout (2+2 demos), Core Modules (3). Badge types: unit/smoke/demo/fast/slow |
| sg-pyodide tests | `sgraph_ai_tools__static/tests/test-pyodide.html` | **NEW** — Loads Pyodide, tests Python stdlib crypto, PBKDF2, HMAC-SHA256, cross-language key derivation comparison (Python vs JS) |
| sg-layout tests | `sgraph_ai_tools__static/tests/sg-layout/v0.1.0.test.html` | **NEW** — 23 tests: custom element, tree construction, serialisation, registration protocol, add/remove panels, tab stacks, tab switching, close tab, events, fractal nesting, depth tracking, nested serialisation, walkLayouts |
| sg-tree tests v0.1.0 | `sgraph_ai_tools__static/tests/sg-tree/v0.1.0.test.html` | **NEW** — 60+ assertions: SGT_EVENTS constants (11), custom element registration (3), setData/getData isolation (5), expand/collapse/expandAll/collapseAll (10), expandPath ancestors (4), select/disabled (3), getScrollTop/setScrollTop (2), filter + filter(null) (4), getExpandedIds/setExpandedIds round-trip (3), getLayoutState/setLayoutState (5), all 9 events fire with correct detail (12+), keyboard nav (ArrowDown/Up/Left/Right/Enter/Home/End/Space) (10), badge rendering (4), ARIA attributes (7), empty tree (3), expandableTypes custom type (2), _rowHeight internal property (2), override queue (2). Plus interactive demo with event log. |
| sg-tree tests v0.1.1 | `sgraph_ai_tools__static/tests/sg-tree/v0.1.1.test.html` | **NEW** — 20+ assertions: method existence (5), editable attr (1), _commitRename updates label (1), DATA_CHANGED on rename (1), _deleteNode removes node (2), cannot delete root (1), delete clears selection (1), _createFolder returns id + inserts node + correct type/label (4), auto-label "New Folder" (1), _addFiles count/labels/type (4), _addFiles with File object (2), DATA_CHANGED on delete (1). Plus interactive controls. |
| sg-tree tests v0.1.2 | `sgraph_ai_tools__static/tests/sg-tree/v0.1.2.test.html` | **NEW** — 14+ assertions: _rowHeight=28 (1), _visibleRange exists (1), DOM rows ≤ maxRows for 2000-node tree (1), DOM rows > 0 (1), _visible has 2000 entries (1), scrollTo sets scroll near expected offset (1), DOM rows still bounded after scrollTo (1), getScrollTop/setScrollTop round-trip (2), filter with virtual scroll (2), filter(null) restores count (1), setData resets count (1), empty tree shows .sgt-empty (1). Plus live stats and scrollTo buttons. |
| sg-tree tests v0.1.3 | `sgraph_ai_tools__static/tests/sg-tree/v0.1.3.test.html` | **NEW** — 22+ assertions: method existence (3), initial state (2), selectAll count and ids (3), deselectAll (2), select(id) in multi mode (1), _rangeSelect range (4), reverse range (1), getSelectedId() compat (1), SELECTION_CHANGED events (3), single mode compat (3), getLayoutState includes selectedIds (2), setLayoutState restores multi (2). Two live trees (multi + single). |
| sg-tree tests v0.1.4 | `sgraph_ai_tools__static/tests/sg-tree/v0.1.4.test.html` | **NEW** — 16+ assertions: method/property existence (6), _moveNode before (2), _moveNode after (2), _moveNode into folder (3), cannot drop into own descendant (1), cannot drag root (1), DATA_CHANGED after _moveNode (1), node count preserved across moves (1), multiple moves (2). Plus interactive tree with programmatic test buttons. |
| sg-git-graph tests | `sgraph_ai_tools__static/tests/sg-git-graph/v0.1.0.test.html` | **NEW** — 16 assertions: registration, window export, shadow DOM structure, SVG rendering, correct node count, branch legend, abandoned class, merge stroke, getSelected() null, commit-selected event, detail panel, clearSelection, commit-deselected event, graph attribute auto-load, bezier curves for cross-lane edges |
| sg-llm-attachment-manager tests | `sgraph_ai_tools__static/tests/sg-llm/attachment-manager.test.html` | **NEW** — 14 assertions: registration, window export, shadow DOM, initial empty state, addFile() text, addFile() image, badge rendering + thumbnail, removeAttachment(), badge update, clear(), getAttachments() returns copy, disable-on-send enable/disable, no-disable behaviour, placeholder attribute, unsupported type ignored |
| sg-llm-response-inspector tests | `sgraph_ai_tools__static/tests/sg-llm-response-inspector/v0.1.0.test.html` | **NEW** — 17 assertions: registration, window export, shadow root, getData() null initially, empty state, receives REQUEST_COMPLETE via data-llm-bus, tabs rendered, summary panel active, summary content, image tab badge, chunk tab badge, clear() reset, data-llm-bus resolution, non-streaming response, chunks panel note, full data JSON tree, expand/collapse controls |
| sg-youtube-upload tests | `sgraph_ai_tools__static/tests/sg-youtube-upload/v0.1.0.test.html` | **NEW** — 17 assertions: scope constant, `requestAccess` is a function, rejects when GIS missing, `YouTubeUpload` ctor requires accessToken, `uploadVideo` rejects without file, rejects without title, token cache provider key, empty cache returns null, round-trip stores accessToken+expiresAt, expired token returns null, 60 s skew handling, custom element registered, demo component reaches ready, exposes connect/upload/setFile/signOut, setFile stores blob. Live demo component embedded; OAuth + upload paths require client-id and are exercised in browser/Playwright via SKILL-browser.md. |
| sg-youtube-api tests | `sgraph_ai_tools__static/tests/sg-youtube-api/v0.1.0.test.html` | **NEW** — 22 assertions: scope constants, ctor guards, exposed methods, guard-rails (updateVideo/setThumbnail/deleteVideo/getVideos), stubbed-fetch round-trip for getMyChannel/listMyUploads/getVideos/updateVideo/setThumbnail/deleteVideo. No real network. |
| sg-youtube-videos tests | `sgraph_ai_tools__static/tests/sg-youtube-videos/v0.1.0.test.html` | **NEW** — 11 assertions: registration, public API surface, stubbed-fetch refresh + loadMore pagination, videos-loaded events, video-selected event from row click. Live demo grid embedded. |
| sg-youtube-video-editor tests | `sgraph_ai_tools__static/tests/sg-youtube-video-editor/v0.1.0.test.html` | **NEW** — 16 assertions: registration, public API, form populated from loadVideo, tags joined, privacy badge class, save() emits video-saved + PUT body has snippet + categoryId + status, delete confirm guard, deleteVideo issues DELETE. Live demo editor embedded. |

