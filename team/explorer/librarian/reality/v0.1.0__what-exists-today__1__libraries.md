# Reality Document — Part 1: Libraries

**Last verified:** 02 May 2026
**Scope:** Deployable folder layout, core modules, skills library, components.

**Sibling parts:**
- [Part 2 — Tools, manifests, tests](./v0.1.0__what-exists-today__2__tools.md)
- [Part 3 — Operations (CI/CD, SEO, team, config, reuse plan)](./v0.1.0__what-exists-today__3__operations.md)
- [Index (cover sheet + cumulative change paragraph)](./v0.1.0__what-exists-today.md)

This part is the dependency graph that everything else consumes — pure-JS core modules, web components, and the deployable folder layout. New entries go here when a non-tool building block ships.

---

### Deployment Base Folder

| Item | Path | Notes |
|---|---|---|
| Deployable root | `sgraph_ai_tools__static/` | Synced to S3 `websites/sgraph-tools/` |
| Version file | `version` | Contains `v0.1.0` |

### Core Modules

| Module | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-crypto.js | `sgraph_ai_tools__static/core/crypto/v1/v1.0/v1.0.0/sg-crypto.js` | v1.0.0 | `ALGORITHM`, `KEY_LENGTH`, `IV_LENGTH`, `isAvailable()`, `requireSecureContext()`, `generateKey()`, `exportKey()`, `importKey()`, `encryptFile()`, `decryptFile()`, `bufferToBase64Url()`, `base64UrlToBuffer()` | Verified — wire-format compatible with Send production crypto |
| sg-ssh.js | `sgraph_ai_tools__static/core/ssh/v1/v1.0/v1.0.0/sg-ssh.js` | v1.0.0 | `generateSSHKeyPair()` | Built from scratch — Ed25519 only (Send also supports RSA-4096) |
| sg-pbkdf2.js | `sgraph_ai_tools__static/core/pbkdf2/v1/v1.0/v1.0.0/sg-pbkdf2.js` | v1.0.0 | `deriveKey()`, `deriveKeyFromWords()`, `calculateEntropy()` | **NEW** — PBKDF2 key derivation for friendly passphrase-based keys |
| sg-wordlist.js | `sgraph_ai_tools__static/core/wordlist/v1/v1.0/v1.0.0/sg-wordlist.js` | v1.0.0 | `getWordList()`, `getRandomWords()`, `searchWords()`, `SUPPORTED_LOCALES` | **NEW** — Word lists for friendly key generation (en-gb: ~1954 words) |
| sg-vault-client.js | `sgraph_ai_tools__static/core/vault-client/v1/v1.0/v1.0.0/sg-vault-client.js` | v1.0.0 | `parseVaultKey()`, `deriveVaultKeys()`, `deriveFileId()`, `openVault()`, `fetchTree()`, `readFile()`, `deriveFileIdForPath()`, `fetchSettings()`, `listFileIds()` | **NEW** — Browser vault client. PBKDF2 + HMAC-SHA256 + AES-256-GCM via Web Crypto. Wire-compatible with sg-send-cli. |
| sg-pyodide.js | `sgraph_ai_tools__static/core/pyodide/v1/v1.0/v1.0.0/sg-pyodide.js` | v1.0.0 | `loadPyodide()`, `installPackages()`, `runPython()`, `runPythonCapture()`, `isPackageAvailable()`, `checkVaultCompatibility()`, `deriveVaultKeysPython()`, `getStatus()` | **NEW** — Lazy Pyodide loader. Runs CPython-in-WASM from CDN (~20 MB). Includes vault crypto compatibility checker. |
| sg-git-graph.js | `sgraph_ai_tools__static/components/sg-git-graph/v0/v0.1/v0.1.0/sg-git-graph.js` | v0.1.0 | `SgGitGraph` (Web Component) | **NEW** — Interactive branch/commit graph visualiser. Pure SVG, no external deps. Phase 1: static JSON data source. Branches, merges, abandoned branches, click-to-inspect detail panel, hover tooltips, cubic bezier cross-lane edges. API: `setGraph()`, `loadUrl()`, `getSelected()`, `clearSelection()`. Emits `sg-git-graph:commit-selected/deselected`. |
| sg-layout.js | `sgraph_ai_tools__static/core/sg-layout/v0.1.0/sg-layout.js` | v0.1.3 | `SgLayout` (Web Component), `SGL_EVENTS` | **NEW** — Fractal window management. Row/column layout, resize handles, registration protocol, serialisation. v0.1.1: tab stacks. v0.1.2: fractal registration. v0.1.3: drag to dock (drag tabs between panels, split on drop to left/right/top/bottom, merge on center). |
| sg-tree.js | `sgraph_ai_tools__static/core/sg-tree/v0/v0.1/v0.1.0/sg-tree.js` | v0.1.0 | `SgTree` (Web Component) | **NEW** — Standalone tree view. Shadow DOM, flat row rendering (not nested DOM), single-select, expand/collapse, filter, keyboard nav (ArrowUp/Down/Left/Right, Enter, Home, End, j/k, *), ARIA. 15 public methods: setData, getData, getSelectedId, select, expand, collapse, expandAll, collapseAll, expandPath, scrollTo, getScrollTop, setScrollTop, filter, getExpandedIds, setExpandedIds, getLayoutState, setLayoutState. Override queue (window._SgTreeQ) for surgical v0.1.x overlays. _rowHeight=28 (required by v0.1.2), _renderRow returns element (required by v0.1.1). |
| sg-tree-events.js | `sgraph_ai_tools__static/core/sg-tree/v0/v0.1/v0.1.0/sg-tree-events.js` | v0.1.0 | `SGT_EVENTS` (frozen object, 9 event types) | **NEW** — Event name constants for sg-tree. NODE_SELECTED, NODE_DESELECTED, SELECTION_CHANGED, NODE_EXPANDED, NODE_COLLAPSED, NODE_ACTIVATED, NODE_CONTEXT, DATA_CHANGED, READY. |
| sg-tree--v0.1.1.js | `sgraph_ai_tools__static/core/sg-tree/v0/v0.1/v0.1.1/sg-tree--v0.1.1.js` | v0.1.1 | Surgical override layer | **NEW** — Editor Mode overlay on v0.1.0. Import chain: pulls v0.1.0 transitively. When `editable="true"`: hover row shows ✎ rename button; F2 starts inline rename (Enter/blur=commit, Escape=cancel); Delete key removes node+subtree. New prototype methods: `_startRename(id)`, `_commitRename(id, newLabel)`, `_deleteNode(id)`, `_createFolder(parentId?, label?)` (auto-starts rename), `_addFiles(parentId?, fileList)` (accepts FileList, File[], or {label} array). All mutations dispatch DATA_CHANGED. |
| sg-tree--v0.1.2.js | `sgraph_ai_tools__static/core/sg-tree/v0/v0.1/v0.1.2/sg-tree--v0.1.2.js` | v0.1.2 | Surgical override layer | **NEW** — Virtual Scrolling overlay. Chains v0.1.1→v0.1.0. Overrides `_renderVisibleRows` with top-spacer + visible-window + bottom-spacer pattern. DOM row count always ≤ ceil(viewportH/28)+8 regardless of tree size. `_onScroll()` re-renders visible window without rebuilding `_visible`. `scrollTo()` extended to handle nodes outside virtual window. Performance target: 10,000+ nodes at 60fps. |
| sg-tree--v0.1.3.js | `sgraph_ai_tools__static/core/sg-tree/v0/v0.1/v0.1.3/sg-tree--v0.1.3.js` | v0.1.3 | Surgical override layer | **NEW** — Multi-Select overlay. Chains v0.1.2→v0.1.1→v0.1.0. When `selectable="multi"`: plain click=single+anchor, Shift+click=contiguous range, Ctrl/Cmd+click=toggle, Space=toggle focused. `_selectedIds` Set replaces single `_selectedId` (backwards compat preserved). New methods: `getSelectedIds()`, `selectAll()`, `deselectAll()`. `getSelectedId()` returns first selected. `getLayoutState`/`setLayoutState` extended with `selectedIds` array. SELECTION_CHANGED dispatched with full `selectedIds`. |
| sg-tree--v0.1.4.js | `sgraph_ai_tools__static/core/sg-tree/v0/v0.1/v0.1.4/sg-tree--v0.1.4.js` | v0.1.4 | Surgical override layer | **NEW** — Drag-and-Drop overlay. Chains v0.1.3→v0.1.2→v0.1.1→v0.1.0. Full import: `import 'sg-tree--v0.1.4.js'` loads all 4 layers. `draggable="true"` on all non-root rows. Drop position (before/into/after) determined by mouse Y within row (top 25%/mid 50%/bottom 25%). Teal line indicator for before/after; row outline for drop-into. `_moveNode(dragId, targetId, position)` mutates `_data`, dispatches DATA_CHANGED. Guards: cannot drag root, cannot drop into own descendant. `_dropIndicator` DOM element in shadow root. |
| sg-layout-events.js | `sgraph_ai_tools__static/core/sg-layout/v0.1.0/sg-layout-events.js` | v0.1.0 | `SGL_EVENTS` (frozen object, 16 event types) | Event name constants for sg-layout framework |
| sg-layout.css | `sgraph_ai_tools__static/core/sg-layout/v0.1.0/sg-layout.css` | v0.1.0 | CSS custom properties | Default theme for sg-layout — colours, geometry, motion |
| sg-image.js | `sgraph_ai_tools__static/core/image/v1/v1.0/v1.0.0/sg-image.js` | v1.0.0 | `getImageInfo()`, `supportsAvif()`, `crop()`, `resize()`, `redact()`, `addText()`, `flattenTransparency()`, `renderOperations()`, `exportImage()` | **NEW** — Client-side image processing via Canvas API. Crop, resize, redact (black/pixelate), text overlay, format conversion. |
| sg-send-crypto.js | `sgraph_ai_tools__static/core/send-crypto/v1/v1.0/v1.0.0/sg-send-crypto.js` | v1.0.0 | `isFriendlyToken()`, `generateToken()`, `deriveTransferId()`, `deriveKey()`, `buildSgmetaEnvelope()`, `encrypt()`, `SALT`, `ITERATIONS`, `TOKEN_PATTERN` | **NEW** — Simple token generation + derivation. PBKDF2-SHA256 key derivation, SGMETA envelope builder, AES-256-GCM encryption. Interop with SG/Send friendly-crypto.js. |
| sg-send-upload.js | `sgraph_ai_tools__static/core/send-crypto/v1/v1.0/v1.0.0/sg-send-upload.js` | v1.0.0 | `uploadFile()`, `DIRECT_UPLOAD_LIMIT` | **NEW** — SG/Send 3-step upload client (create → upload → complete). Direct upload ≤ 5MB, presigned multipart for larger files. |
| sg-send-upload.js | `sgraph_ai_tools__static/core/sg-send-upload/v1/v1.1/v1.1.0/sg-send-upload.js` | v1.1.0 | `ChunkedUploader` (class), `uploadFile()`, `detectCloudDriveFile()`, `formatSpeed()`, `formatEta()`, `DEFAULT_CHUNK_SIZE`, `DEFAULT_MAX_CONCURRENT`, `DEFAULT_MAX_RETRIES`, `RETRY_BASE_DELAY_MS` | **NEW** — Robust chunked upload client. `ChunkedUploader`: constructor takes `{ciphertext, transferId, sendUrl, accessToken, chunkSize, maxConcurrent, maxRetries, onProgress, onWarning}`. `start()` initiates presigned multipart, uploads all chunks in parallel, completes multipart. `cancel()`, `getResumeState()` (serialisable snapshot), `restoreResumeState(state)`, static `fromResumeState(state, ciphertext, opts)`. Per-chunk exponential back-off retry (default 4 retries). Rolling-window speed tracker → `UploadProgress.bytesPerSecond` + `etaSecs`. Internet dropout: waits for `navigator.onLine` before each chunk. Cloud drive detection: iCloud stub (`.icloud` extension, empty size), Google Drive MIME types → `{ provider, hint }`. Pre-flight size check throws `Error{ code: 'oversized' }`. `formatSpeed(bps)` → `'2.4 MB/s'`, `formatEta(secs)` → `'1m 23s'`. |
| sg-send-download.js | `sgraph_ai_tools__static/core/sg-send-download/v1/v1.0/v1.0.0/sg-send-download.js` | v1.0.0 | `ChunkedDownloader` (class), `decryptWithProgress()`, `formatSpeed()`, `formatEta()`, `DEFAULT_CHUNK_SIZE`, `DEFAULT_MAX_CONCURRENT`, `DEFAULT_MAX_RETRIES`, `RETRY_BASE_DELAY_MS` | **NEW** — Robust chunked download client. `ChunkedDownloader`: constructor takes `{url, chunkSize, maxConcurrent, maxRetries, onProgress}`. `probe()` HEAD request for Content-Length. `start()` fetches chunks via HTTP Range requests in parallel. `getBuffer()` assembles chunks into a single Uint8Array after `start()`. `cancel()`, `getResumeState()`, `restoreResumeState(state)`, static `fromResumeState(state, opts)`. Per-chunk retry with exponential back-off, internet dropout handling (waitForOnline). Rolling-window speed tracker → `DownloadProgress.bytesPerSecond + etaSecs`. `decryptWithProgress(fn, onProgress)` wraps any async decrypt function with before/after progress events (`stage: 'decrypting'` at 97%, `'complete'` at 100%). Mirrors sg-send-upload v1.1.0 API surface for symmetry. |
| wordlist.js | `sgraph_ai_tools__static/core/send-crypto/v1/v1.0/v1.0.0/wordlist.js` | v1.0.0 | `WORDLIST` | **NEW** — 320-word English wordlist for simple token generation. |
| sg-video.js | `sgraph_ai_tools__static/core/video/v1/v1.0/v1.0.0/sg-video.js` | v1.0.0 | `loadFFmpeg()`, `getVideoInfo()`, `splitVideo()`, `trimVideo()`, `extractAudio()`, `extractVideo()`, `parseTime()`, `formatTime()`, `isWasmSupported()` | **NEW** — Browser video processing via FFmpeg WASM. Lazy-loads ~30MB FFmpeg from CDN. Split, trim, extract audio/video with -c copy (no re-encoding). |
| sg-vault-write.js | `sgraph_ai_tools__static/core/vault-write/v1/v1.0/v1.0.0/sg-vault-write.js` | v1.0.0 | `writeVaultFile()`, `deriveWriteKeys()`, `deriveWriteKeysFromSimpleToken()` | **NEW** — Browser vault write module. Encrypt blobs, create commit_v2 + tree_v1 objects, update branch refs via PUT API. Fully sgit-compatible schemas. Derives encrypt+decrypt capable keys. Supports both full vault keys (`passphrase:vaultId`) and simple tokens (`word-word-NNNN`). |
| sg-vault-write.js | `sgraph_ai_tools__static/core/vault-write/v1/v1.1/v1.1.1/sg-vault-write.js` | v1.1.1 | Same exports as v1.0.0 | **UPDATED** — No API change, path-aligned with vault-client v1.2.1. |
| sg-vault-client.js | `sgraph_ai_tools__static/core/vault-client/v1/v1.2/v1.2.1/sg-vault-client.js` | v1.2.1 | `readFile()`, `readFileAsJson()`, `walkTree()`, `openVaultTree()`, `decryptMetadata()`, `encryptMetadata()`, `computeObjectId()`, `deriveBranchRefFileId()` | **UPDATED** — vaultId regex relaxed from `{8}` to `{4,24}`, added `encryptMetadata()`, `computeObjectId()`, `deriveBranchRefFileId()`. |
| vault-id-utils.js | `sgraph_ai_tools__static/core/vault-client/v1/v1.2/v1.2.1/vault-id-utils.js` | v1.2.1 | `objectIdToBarePath()`, `barePathToObjectId()`, `validateObjectId()`, `VALID_TYPES`, `VALID_DERIVATIONS`, `VALID_MUTABILITIES` | **UPDATED** — Added `snw` to VALID_MUTABILITIES, fixed `idx` bare path to `bare/idx`. |
| sg-vault-cache.js | `sgraph_ai_tools__static/core/vault-cache/v1/v1.0/v1.0.0/sg-vault-cache.js` | v1.0.0 | `cachedFetchRaw()`, `cachedFetch()`, `cachedFetchJson()`, `fetchRefRaw()`, `fetchRef()`, `warmCache()`, `clearCache()`, `getCacheStats()` | **NEW** — Cache API read-through layer for vault objects. Immutable objects (`-imm-` suffix) cached permanently. Refs always fetched live. |
| sg-vault-tree-model.js | `sgraph_ai_tools__static/core/vault-tree-model/v1/v1.0/v1.0.0/sg-vault-tree-model.js` | v1.0.0 | `createTreeModel()` → TreeModel with `loadRoot()`, `loadSubTree()`, `needsLoading()`, `getTreeId()`, `getNode()`, `getRoot()`, `insertFile()`, `updateFile()`, `removeEntry()`, `renameEntry()`, `moveEntry()`, `createFolder()`, `buildTreeEntries()`, `listFolder()`, `getAllFilePaths()`, `getStats()` | **NEW** — Persistent in-memory tree with STUB/LOADED folder states. Preserves unloaded stubs during commit. |
| sg-vault-session.js | `sgraph_ai_tools__static/core/vault-session/v1/v1.0/v1.0.0/sg-vault-session.js` | v1.0.0 | `createSession()` → VaultSession with `open()`, `commit()`, `push()`, `pull()`, `getAheadCount()`, `getBehindCount()`, `getFile()`, `loadSubTree()`, `uploadBlob()` + getters `writable`, `opened`, `headCommitId`, `namedHeadId` | **NEW** — Two-branch session orchestrator. Named HEAD (shared) + Clone HEAD (local). Integrates cache, tree model, and vault-client. |
| sg-vault-mutations.js | `sgraph_ai_tools__static/core/vault-mutations/v1/v1.0/v1.0.0/sg-vault-mutations.js` | v1.0.0 | `addFile()`, `updateFile()`, `removeFile()`, `renameFile()`, `moveFile()`, `createFolder()`, `removeFolder()`, `renameFolder()`, `moveFolder()` | **NEW** — 9 high-level mutation operations on a VaultSession. Auto-loads stub folders before mutating. Each mutation validates, mutates tree model, and commits. |
| sg-vault-init.js | `sgraph_ai_tools__static/core/vault-init/v1/v1.0/v1.0.0/sg-vault-init.js` | v1.0.0 | `createVault()`, `vaultExists()`, `generateVaultId()`, `generateMemorablePassphrase()` | **NEW** — Bootstrap a brand-new vault from scratch. Writes empty tree + initial commit + named HEAD ref to the server. After init, a fresh VaultSession can open the vault and start committing. |
| sg-markdown.js | `sgraph_ai_tools__static/core/markdown/v1/v1.0/v1.0.0/sg-markdown.js` | v1.0.0 | `renderMarkdown()`, `inlineMarkdown()`, `escapeHtml()` | **NEW** — Reusable markdown → HTML renderer. Pure JS, no deps. Handles fenced code, headings, HR, blockquotes, tables, lists, paragraphs, inline bold/italic/code/links. Extracted from infographic-gen tool. |
| sg-vfs-events.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-events.js` | v0.1.0 | `SGL_VFS` (frozen constants object, 30+ event names) | **NEW** — VFS event name constants. Request/response/error pairs for all file ops (read, write, delete, list, stat, createFolder, copy, rename, exists), provider lifecycle events, write-queue/drain events, vault commit events, batch generator events. |
| sg-vfs-path.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-path.js` | v0.1.0 | `normalisePath()`, `joinPath()`, `dirname()`, `basename()`, `extname()`, `isDescendant()`, `splitPath()`, `ancestors()`, `assertValidPath()` | **NEW** — POSIX-style absolute path utilities. No Node.js dependency. Resolves `.`/`..`, validates, splits. |
| sg-vfs-request-id.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-request-id.js` | v0.1.0 | `newRequestId()`, `requestCount()` | **NEW** — Monotonic short request ID generation (`vfs-0001-a3f9`). Correlates request↔response events. |
| sg-vfs-provider-base.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-provider-base.js` | v0.1.0 | `VfsProviderBase` (abstract class) | **NEW** — Abstract base class for all VFS storage providers. Defines interface: readFile, writeFile, deleteFile, listFolder, stat, createFolder, copyFile, rename, exists. |
| sg-vfs-provider-memory.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-provider-memory.js` | v0.1.0 | `MemoryProvider` | **NEW** — In-memory VFS provider. JavaScript Map store. Supports files + folders with auto-created parents, sorting (folders first), stat metadata, clear(). Lost on page reload. For testing + fast layer in layered stack. |
| sg-vfs-core.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-core.js` | v0.1.0 | `VirtualFileSystem` | **NEW** — VFS orchestrator class. Wraps any VfsProviderBase. Exposes async API + DOM event bridge. Every operation emits request→response (or error) events with requestId, wallTime, provider name. All public methods accept `opts = { requestId }` so bus-routed operations preserve the caller's requestId in responses. |
| sg-vfs-provider-indexeddb.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-provider-indexeddb.js` | v0.1.0 | `IndexedDbProvider` | **NEW** — Persistent IndexedDB VFS provider. Survives page reloads. Same interface as MemoryProvider. Single IDB object store keyed by path. Auto-creates parent folders. `clear()` to wipe all stored entries. Accepts `{ dbName, namespace }` options. |
| sg-vfs-provider-layered.js | `sgraph_ai_tools__static/core/sg-vfs/v0/v0.1/v0.1.0/sg-vfs-provider-layered.js` | v0.1.0 | `LayeredProvider` | **NEW** — Multi-provider coordinator. Register providers at priorities (lower = faster/first). Read: waterfall through layers, back-fill faster layers on hit. Write: write-through to all layers simultaneously. Emits `PROVIDER_HIT` / `PROVIDER_MISS` events on bus. `addProvider(provider, priority)`, `summary()`. |
| sg-send-sim.js | `sgraph_ai_tools__static/core/sg-send-sim/v0/v0.1/v0.1.0/sg-send-sim.js` | v0.1.0 | `generateVaultId()`, `createSimVault()`, `openSimVault()`, `writeSimFile()`, `readSimFile()`, `readSimFileAsJson()`, `getSimHead()`, `getSimCommitLog()`, `getSimVaultState()`, `listSimObjects()`, `decryptSimEntry()` | **NEW** — VFS-backed simulation of the SG/Send vault API. Derives vault keys identically to the real server (PBKDF2-SHA-256 @ 600k). Stores encrypted objects in VFS at `bare/data/`, `bare/refs/` mirroring the real vault layout. Full commit cycle: AES-256-GCM blob → tree_v1 → commit_v2 → ref_v1 update. Uses same self-describing object IDs (`obj-cas-imm-{hex}`, `ref-pid-muw-{hex}`). |
| sg-audio.js | `sgraph_ai_tools__static/core/sg-audio/v0/v0.1/v0.1.0/sg-audio.js` | v0.1.0 | `startRecording()`, `stopRecording()`, `isMediaRecorderSupported()`, `getBestMimeType()`, `runHealthCheck()` | **NEW** — Segment-based MediaRecorder wrapper. Records audio in fixed-duration segments (default 30s), delivers each Blob via `onSegment` callback immediately for VFS write. Auto-detects best MIME type (webm/opus → webm → ogg/opus → mp4). Health check: WebAssembly, AudioContext, MediaRecorder MIME support, microphone permission. |
| sg-whisper.js | `sgraph_ai_tools__static/core/sg-whisper/v0/v0.1/v0.1.0/sg-whisper.js` | v0.1.0 | `loadWhisperModel()`, `transcribeBuffer()`, `runHealthCheck()`, `isHealthCheckCached()`, `MODEL_TINY`, `MODEL_BASE` | **NEW** — WASM speech-to-text wrapper using HuggingFace transformers.js (ONNX Runtime Web). Two-model strategy: `Xenova/whisper-tiny.en` (~39 MB) for real-time captions, `Xenova/whisper-base.en` (~74 MB) for post-recording cleanup. 10-step health check mirrors video-tool pattern. sessionStorage caching (4h). Works iOS Safari 16.4+, no SharedArrayBuffer required. |
| sg-sandbox.js | `sgraph_ai_tools__static/core/sg-sandbox/v0/v0.1/v0.1.0/sg-sandbox.js` | v0.1.0 | `SGSandbox` | **NEW** — Sandboxed JavaScript execution. Runs LLM-generated code in a `sandbox="allow-scripts"` iframe (null origin — no parent DOM/cookie/localStorage access). Communication via postMessage only. `execute(code, input?)` → `Promise<*>`. `code` may use `return`; `input` is available as variable. Configurable timeout (default 10s). `destroy()` frees iframe. Phase 1: JS only. Phase 3 will add Pyodide (Python). |
| sg-tool-api-events.js | `sgraph_ai_tools__static/core/sg-tool-api/v0/v0.1/v0.1.0/sg-tool-api-events.js` | v0.1.0 | `SGA_TOOL` (frozen event constants, 7 events) | **NEW** — JS API Primitive event name constants. `TOOL_READY`, `TOOL_CONNECTED`, `TOOL_DISCONNECTED`, `GENERATION_STARTED`, `GENERATION_COMPLETE`, `GENERATION_ERROR`, `GENERATION_CANCELLED`. All namespaced `tool:*`. Exposed as `window.SGA_TOOL`. |
| sg-tool-api-registry.js | `sgraph_ai_tools__static/core/sg-tool-api/v0/v0.1/v0.1.0/sg-tool-api-registry.js` | v0.1.0 | `registerTool()`, `findTool()`, `findAllTools()`, `findToolById()`, `listTools()` | **NEW** — RC-6 instance-aware tool registry. Keyed by `{toolName}:{instanceId}`. `window.__tools[instanceId]` per instance, `window.__tool` only when exactly one instance (deleted when multiple). `window.__tool_registry` discovery API (`find/findAll/findById/list`). Accounts for sg-layout fractal nature — multiple instances of same tool in one page. |
| sg-tool-api.js | `sgraph_ai_tools__static/core/sg-tool-api/v0/v0.1/v0.1.0/sg-tool-api.js` | v0.1.0 | `SgToolApi` (class) | **NEW** — JS API Primitive core class. Constructor: `{name, version, panelId, manifest, skills}`. `register(name, fn, opts)` with sanitiseParams hook, async/sync support, event emission, environment guards. `activate()` calls registerTool + fires TOOL_READY. `_emit(eventName, detail)` injects instanceId. `_appendLog()` ring buffer (LOG_MAX=500). `meta` object: getManifest, getMethods, getSkills, getVersion, getEvents, health, getLog. `_detectPanelId()` via `data-panel-id` attribute walk. |
| manifest-loader.js | `sgraph_ai_tools__static/core/manifest-loader/v0/v0.1/v0.1.0/manifest-loader.js` | v0.1.0 | `loadManifest(manifestUrl)` | **NEW** — Manifest-driven dependency loader. Fetches `manifest.json`, groups `dependencies.loader[]` entries by `phase` field, processes phases in numeric order. Phase 1: CSS files (injected as `<link rel="stylesheet">`) + non-module external scripts (`module:false` e.g. jsPDF/JSZip UMDs). Phase 2: ES module side-effect imports (custom element registrations). Phase 3+: tool-specific JS modules; the entry flagged with `"entry": true` is `import()`-ed last in its phase and its exported `init(manifest)` is called after all phases complete. URL resolution: absolute/root-relative paths passed through; relative paths resolved against the manifest directory. Idempotency: checks for existing `<link href>` / `<script src>` before inserting. Used by PlaybookLM v0.1.39. |
| sg-video.js | `sgraph_ai_tools__static/core/video/v1/v1.0/v1.0.1/sg-video.js` | v1.0.1 | `loadFFmpeg()`, `getVideoInfo()`, `splitVideo()`, `trimVideo()`, `extractAudio()`, `extractVideo()`, `parseTime()`, `formatTime()`, `isWasmSupported()`, `convertToMp4(webmBlob, onProgress?)` | **NEW** — Additive extension of v1.0.0. Adds `convertToMp4()`: re-encodes WebM to MP4 via FFmpeg WASM (libx264 + aac, faststart). On Safari (input already MP4), passes through unchanged. Used by sg-video-compressor component. |
| sg-tts.js | `sgraph_ai_tools__static/core/sg-tts/v0/v0.1/v0.1.0/sg-tts.js` | v0.1.0 | `SgTts` (class), `createPool(size)`, `getPool()`, `loadWorker(entry, cdnUrl, voice, speed)`, `generateAudio(text, voice, speed)`, `streamAudio(text, voice, speed)` | **NEW** — Kokoro TTS core module. 8-worker pool (POOL_SIZE=8), each worker holds an independent ONNX heap. `generateAudio()` splits text at sentence boundaries, dispatches all chunks in parallel via `Promise.all`, zero-copy Float32Array transfer. `streamAudio()` async generator yielding per-chunk in sentence order. |
| sg-tts-worker.js | `sgraph_ai_tools__static/core/sg-tts/v0/v0.1/v0.1.0/sg-tts-worker.js` | v0.1.0 | Web Worker | **NEW** — Worker for sg-tts.js. Loads `esm.sh/kokoro-js`, initialises KokoroTTS instance, responds to `{ cmd:'generate', text, voice, speed }` messages with zero-copy Float32Array transfer. Handles 6 known output shapes from the kokoro-js API. |
| sg-video-recorder.js | `sgraph_ai_tools__static/core/sg-video-recorder/v0/v0.1/v0.1.0/sg-video-recorder.js` | v0.1.0 | `startRecording(canvas, audioStream, options)`, `stopRecording()`, `getRecordingState()`, `getBestMimeType()`, `isSupported()` | **NEW** — Canvas + audio MediaStream capture via MediaRecorder. Wires `canvas.captureStream(fps)` + AudioContext `audioStream`. Produces WebM or MP4 blob. Used by video-creator tool. |
| sg-video-recorder.js | `sgraph_ai_tools__static/core/sg-video-recorder/v0/v0.1/v0.1.1/sg-video-recorder.js` | v0.1.1 | All v0.1.0 exports + `startRecordingStream(stream, options)` | **NEW** — Additive extension of v0.1.0. `startRecordingStream(stream)` accepts any MediaStream directly (no canvas required) — used by video-recorder tool with streams from sg-capture.js. |
| sg-video-recorder.js | `sgraph_ai_tools__static/core/sg-video-recorder/v0/v0.1/v0.1.2/sg-video-recorder.js` | v0.1.2 | `getBestMimeType()`, `isSupported()` | **NEW** — Pure utility exports only. Removes the module-level singleton recorder state from v0.1.0/v0.1.1 (`startRecording`, `startRecordingStream`, `stopRecording`, `getRecordingState`, `getRecorder`). The singleton prevented concurrent recorders and caused state leaks. recorder-pipeline.js manages its own MediaRecorder instances via `RecordingSession` and only needs the format-detection utilities. |
| sg-video-composer.js | `sgraph_ai_tools__static/core/video-composer/v0/v0.1/v0.1.0/sg-video-composer.js` | v0.1.0 | `createComposer({project,assets,canvas,fps})`, `exportComposerProject({project,assets,fps,preferMp4?,bitsPerSecond?,onProgress?})`, `snapToFps()`, `clipDuration()`, `getProjectDuration()`, `validateProject()` | **NEW** — Phase 1 multi-clip video composition engine. Multi-track (round-8-A): `project.tracks[]` is bottom-up (last index = top z-order), playback + export paint the full stack each tick so the top-most active clip wins visually; audio routes from the top-most active unmuted track only (silence if that track is muted). MP4 preferred (Chromium/Safari native via `MediaRecorder`); WebM→MP4 fallback via `core/video.convertToMp4` (FFmpeg WASM). Internal sub-modules: `composer-schema.js` (snapping, clip math, validation, `findActiveClipsPerTrack`, `findTopmostActiveAudioClip`), `composer-playback.js` (canvas-bound playback handle — emits `composer:playhead-changed` + `composer:ended`; builds a video element per assetId across all tracks), `composer-scheduler.js` + `composer-scheduler-frame.js` (wall-clock rAF tick that paints the bottom-up track stack and swaps the audio source on top-most-track changes), `composer-export.js` (canvas captureStream → MediaRecorder orchestrator; dispatches `composer:export-debug` CustomEvents on `document`), `composer-export-setup.js` (videos + audio graph with always-on near-silent oscillator keepalive + recorder builder), `composer-export-loop.js` + `composer-export-tick.js` (round-8-A: wall-clock loop replaces the round-7 clip-by-clip iterator so multiple tracks paint simultaneously and image-tick repaints stay every-rAF), `composer-export-debug.js` (CustomEvent dispatcher), `composer-mp4.js` (MIME picking + ensureMp4 fallback). Used by `sg-video-editor` tool. Also handles image clips (PNG/JPEG/WebP/GIF) via `composer-images.js`: aspect-preserving draw, silent audio passthrough. Round-9-A: per-clip optional `transform` `{x, y, scale}` (centre + scale multiplier, normalised 0..1 of canvas) and `crop` `{x, y, w, h}` (sub-rect of source frame, normalised 0..1) are now applied during draw — new `composer-clip-fields.js` exposes `defaultTransform` / `defaultCrop` / `getClipTransform` / `getClipCrop` / `clampTransform` / `clampCrop` (re-exported from `composer-schema.js`); `composer-draw.js` `paintTransformedFrame` is the single draw helper for both video + image (calls `computeClipDestRect` for the math; the same helper is exported for the overlay UI in R9-B); both `composer-scheduler-frame.js` and `composer-export-tick.js` pass each clip's transform + crop through. With both fields absent, the draw reproduces today's aspect-fit letterboxed placement exactly. Round-9-C: `composer-playback.js` adds `updateProject(next)` — swaps the active project (mutated transform/crop only) and repaints the current frame without destroy+recreate, used by the editor for live mid-drag transform/crop preview without rebuilding `<video>` elements. Same revision: `seek()` now also waits for not-yet-decoded image assets (mirrors the existing `seeked` wait for video) so a freshly-dropped image paints as soon as it loads instead of staying black until the next user interaction. Round-9-D: clips are now a discriminated union — asset clip (existing `assetId`) OR `kind: 'shape'` with `shape: {type:'rect', fill, w, h}` OR `kind: 'text'` with `text: {content, color, fontSize, fontFamily, w, h}`. New `composer-clip-kinds.js` exposes `isAssetClip` / `isShapeClip` / `isTextClip` / `getClipKind` / `getClipIntrinsicDims` / `defaultShape` / `defaultText` / `clampShape` / `clampText` (re-exported from `composer-schema.js`). `composer-draw.js` adds `paintShape` (rect via `ctx.fillRect`) and `paintText` (centred `ctx.fillText`, font scales with the rect); both reuse `computeClipDestRect` so transform handles work unchanged. `composer-scheduler-frame.js` and `composer-export-tick.js` branch on clip kind. `findTopmostActiveAudioClip` skips shape/text so a graphic on top doesn't silence the video below. `computeClipDestRect` also fixed (Round-9-D): `baseScale` now uses the FULL source dims so a partial crop trims pixels off the edge instead of re-fitting the cropped portion to the canvas. `preview-overlay-move.computeNextTransform` recentres after scale so the diagonally-opposite anchor stays put (drag east → west edge stays). Round-9-G: composer-playback.js adds a public `refresh()` method on the playback handle that synchronously re-paints the current frame via `sched.paintAt(playhead)` — used by the new transport-bar redraw button and by the tool's `refreshPreview` SgToolApi method; same revision fixes the "preview stays black after first asset drop" bug by extending `seek()`'s freshly-created-video wait to listen for `loadeddata` and `canplay` in addition to `seeked` (a fresh `<video>` whose `currentTime` is being set to its existing value of 0 may NOT fire `seeked`, so the listener-of-one used to leave the canvas black until the user pressed play to force `v.play()`); the new `waitForVideoFrame(video, cb)` helper races all three events and cleans up siblings on first fire. |
| sg-capture.js | `sgraph_ai_tools__static/core/sg-capture/v0/v0.1/v0.1.0/sg-capture.js` | v0.1.0 | `getCameraStream(constraints?)`, `getAudioStream(options?)`, `getScreenStream(options?)`, `mergeAsPiP(screenStream, cameraStream, options?)`, `buildStream(sources)`, `stopStream(stream)`, `isCameraSupported()`, `isScreenSupported()` | **NEW** — Unified MediaStream acquisition. Supports all 7 combinations of camera/audio/screen. `buildStream({camera, audio, screen, pipOptions})` assembles the correct MediaStream. `mergeAsPiP()` composites camera overlay on screen via Canvas + setInterval (not rAF — rAF is throttled in background tabs during screen recording). Returns `{ stream, stop }` — stop() must be called to release tracks. |
| sg-audio-viz.js | `sgraph_ai_tools__static/components/sg-audio-viz/v0/v0.1/v0.1.0/sg-audio-viz.js` | v0.1.0 | `setSource(source)`, `setMode(mode)`, `setColors({primary,secondary})`, `start()`, `stop()`, `captureStream(fps)`, `getAudioCaptureStream()`, `getAnalyser()`, `wasHidden` (getter), `destroy()` | **NEW** — Audio-reactive canvas animation Web Component (`<sg-audio-viz>`). Sources: MediaStream, HTMLMediaElement, Blob, URL string. 10 modes: waveform, bars, mirror-bars, mirror-wave, circular-wave, circular-bars, blob, eq-bands, mirror-eq, smooth-eq. `captureStream(fps)` exposes the canvas as a live video MediaStream for MediaRecorder. `getAudioCaptureStream()` returns the analyser passthrough stream. `wasHidden` getter: true if document.hidden fired while running (signals viz may be frozen — used to offer post-recording regeneration). Background tab strategy: setTimeout loop + silent BufferSourceNode to keep AudioContext active and exempt from Chrome timer throttling. |
| sg-youtube-upload.js | `sgraph_ai_tools__static/core/youtube-upload/v0/v0.1/v0.1.0/sg-youtube-upload.js` | v0.1.0 | `requestAccess(clientId, opts)`, `class YouTubeUpload`, `YOUTUBE_UPLOAD_SCOPE` | **NEW** — YouTube Data API v3 client-side uploader. `requestAccess()` mirrors the GIS scoped-token pattern from sg-drive-appdata, returning `{accessToken, expiresAt, scope}` so the caller can cache. `YouTubeUpload.uploadVideo(file, metadata, {onProgress, signal})` runs the resumable upload protocol: POST `/upload/youtube/v3/videos?uploadType=resumable` → PUT bytes (XHR for progress) → resolves `{id, url, snippet, status}`. Pure JS, no UI, no localStorage — caching lives in the component layer. |
| sg-youtube-analytics.js | `sgraph_ai_tools__static/core/youtube-analytics/v0/v0.1/v0.1.0/sg-youtube-analytics.js` | v0.1.0 | `class YouTubeAnalytics`, `YOUTUBE_ANALYTICS_SCOPE` | **NEW** — YouTube Analytics API v2 client. Separate scope (`yt-analytics.readonly`) and separate quota (~1 query/sec/user, no daily unit cap). Methods: `getChannelSummary({startDate, endDate, days})` (defaults to last 28 days ending yesterday — analytics lags ~1 day), `getVideoSummary(videoId, opts)` (per-video equivalent, filters with `video==<id>`), `getDailyTrend(videoId, {days})` (daily views + watch-time time-series, sorted ascending — drives sparklines), `getRetentionCurve(videoId)` (audience retention curve: dimensions=elapsedVideoTimeRatio, metrics=audienceWatchRatio + relativeRetentionPerformance; returns ~100 points 0.0→1.0 along video timeline). Internally normalises the v2 column-headers + row-arrays response into arrays of `{col: value}` objects. Pure JS, no UI. |
| sg-youtube-api.js | `sgraph_ai_tools__static/core/youtube-api/v0/v0.1/v0.1.0/sg-youtube-api.js` | v0.1.0 (additive) | `getMyChannel()` extended | **UPDATED** — `getMyChannel()` now requests `part=snippet,statistics,contentDetails,brandingSettings,topicDetails,status` (still 1 unit) and returns extra fields: `bannerUrl`, `country`, `defaultLanguage`, `publishedAt`, `keywords`, `topicCategories` (Wikipedia URLs) + `topicLabels` (human-readable last-segment), `hiddenSubscribers`, `madeForKids`, `channelUrl`, `studioUrl`. |
| sg-youtube-upload.js | `sgraph_ai_tools__static/core/youtube-upload/v0/v0.1/v0.1.0/sg-youtube-upload.js` | v0.1.0 | (additive) `requestAccess(clientId, {scope})` | **UPDATED** — `requestAccess` now accepts a `scope` option (default = `YOUTUBE_UPLOAD_SCOPE`); youtube-editor passes `YOUTUBE_FULL_SCOPE` for read+edit. Backwards compatible. |

### Skills Library

| Skill | Path | Status |
|---|---|---|
| sgit — Zero-Knowledge Encrypted Vault Operations | `library/skills/use_sgit-and-vaults/SKILL.md` | **NEW** — Comprehensive sgit CLI skill document. Covers: init, commit, status, push, pull, clone, branch, diff, stash, revert, share, publish, export, vault key store, PKI, diagnostics, safe removal. Ported from SGraph-AI__App__Send. |

> **PKI support map.** PKI-relevant capability is spread across `sg-ssh.js` (Ed25519 keygen), the `<vault-schema-pki-key>` renderer, the `sgit pki` skill verbs, and the unbuilt auth-mvp Phase 2 vault identity key. For the consolidated, gap-aware picture (what exists vs. what is still PROPOSED — there is **no** X.509/CSR/CA code) see the Architect brief `team/explorer/architect/v0.1.59__arch-brief__pki-support-map.md`.

### Components

| Component | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-header.js | `sgraph_ai_tools__static/components/header/v1/v1.0/v1.0.0/sg-header.js` | v1.0.0 | `SgHeader` (Web Component) | Built — inline CSS (superseded by sg-site-header) |
| sg-footer.js | `sgraph_ai_tools__static/components/footer/v1/v1.0/v1.0.0/sg-footer.js` | v1.0.0 | `SgFooter` (Web Component) | Built — inline CSS (superseded by sg-site-footer) |
| sg-component.js | `sgraph_ai_tools__static/components/base/v1/v1.0/v1.0.0/sg-component.js` | v1.0.0 | `SgComponent`, `SgComponentPaths` | Base class — Shadow DOM, resource loading (vault pattern) |
| sg-tokens.css | `sgraph_ai_tools__static/components/tokens/v1/v1.0/v1.0.0/sg-tokens.css` | v1.0.0 | CSS custom properties | Canonical brand tokens for all sgraph.ai domains |
| sg-site-header.js | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.0/sg-site-header.{js,html,css}` | v1.0.0 | `SgSiteHeader` (Web Component) | **FROZEN** — Shared sticky header — SG/ logo, nav, locale div |
| sg-site-header.js | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.1/sg-site-header.{js,html,css}` | v1.0.1 | `SgSiteHeader` (Web Component) | **NEW** — Adds `<slot name="locale">` for composable locale picker, teal accent line |
| sg-site-header.js | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.2/sg-site-header.{js,html,css}` | v1.0.2 | `SgSiteHeader` (Web Component) | **NEW** — Adds inline SVG favicon before SG/ brand text, matching SG/Send header pattern |
| sg-site-header.js | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.3/sg-site-header.{js,html,css}` | v1.0.3 | `SgSiteHeader` (Web Component) | **NEW** — Removes inline SVG favicon. Logo is plain text only (SG/Product), matching the SG/Send header at sgraph.ai. No other changes from v1.0.2. |
| sg-site-header.js | `sgraph_ai_tools__static/components/site-header/v1/v1.0/v1.0.5/sg-site-header.{js,html,css}` | v1.0.5 | `SgSiteHeader` (Web Component) | **NEW** — SITE_CONFIGS environment-aware nav URLs (dev/main/prod); `active-nav` attribute highlights current nav item; auto-generates standard SGraph nav (How it Works / Vaults / Security / Tools / Pricing) when `site="Tools"` and no `nav-items` override; cross-site links no longer use `target="_blank"`. |
| sg-site-footer.js | `sgraph_ai_tools__static/components/site-footer/v1/v1.0/v1.0.0/sg-site-footer.{js,html,css}` | v1.0.0 | `SgSiteFooter` (Web Component) | Shared footer — brand, configurable columns, sherpa CTA |
| sg-locale-picker.js | `sgraph_ai_tools__static/components/locale-picker/v1/v1.0/v1.0.0/sg-locale-picker.{js,html,css}` | v1.0.0 | `SgLocalePicker`, `SG_LOCALES` (Web Component) | **FROZEN** — Language selector — 17 locales, flag emojis, 2-column dropdown |
| sg-locale-picker.js | `sgraph_ai_tools__static/components/locale-picker/v1/v1.0/v1.0.1/sg-locale-picker.{js,html,css}` | v1.0.1 | `SgLocalePicker`, `SG_LOCALES` (Web Component) | **NEW** — Fixes `[hidden]` CSS bug in Shadow DOM, adds sessionStorage write on locale change |
| sg-audio-recorder.js | `sgraph_ai_tools__static/components/audio/sg-audio-recorder/v0/v0.1/v0.1.0/sg-audio-recorder.js` | v0.1.0 | `SgAudioRecorder` (Web Component), `SGR_EVENTS` | **NEW** — Recording UI: start/stop buttons with pulse animation, segment timer, waveform visualiser (AnalyserNode FFT bars). Emits `sg-recorder:segment`, `sg-recorder:started`, `sg-recorder:stopped`, `sg-recorder:error`. `segment-duration` attribute (ms). Public API: `start()`, `stop()`, `isRecording`. Wraps `sg-audio.js` core. |
| sg-audio-transcript.js | `sgraph_ai_tools__static/components/audio/sg-audio-transcript/v0/v0.1/v0.1.0/sg-audio-transcript.js` | v0.1.0 | `SgAudioTranscript` (Web Component) | **NEW** — Live caption display: scrolling segment list, confidence colour coding (high/mid/low border), model badge (tiny.en vs base.en), word count + segment count header. Public API: `appendSegment({ index, text, latencyMs, modelId, confidence })`, `clear()`, `fullText` getter. |
| sg-upload-dropzone.js | `sgraph_ai_tools__static/components/upload-dropzone/v1/v1.0/v1.0.0/sg-upload-dropzone.{js,html,css}` | v1.0.0 | `SgUploadDropzone` (Web Component) | **NEW** — Drag-and-drop file upload zone with visual feedback |
| sg-key-generator.js | `sgraph_ai_tools__static/components/key-generator/v1/v1.0/v1.0.0/sg-key-generator.{js,html,css}` | v1.0.0 | `SgKeyGenerator` (Web Component) | **NEW** — Friendly encryption key creator with entropy visualisation |
| sg-key-input.js | `sgraph_ai_tools__static/components/key-input/v1/v1.0/v1.0.0/sg-key-input.{js,html,css}` | v1.0.0 | `SgKeyInput` (Web Component) | **NEW** — Mobile-optimised input for friendly decryption keys |
| sg-image-editor.js | `sgraph_ai_tools__static/components/image/v1/v1.0/v1.0.0/sg-image-editor.{js,css}` | v1.0.0 | `SgImageEditor` (Web Component) | **NEW** — Image editor: preview, toolbar (convert/resize/crop/redact/text), operations stack, export. Composes crop/redact/text sub-components. |
| sg-image-crop.js | `sgraph_ai_tools__static/components/image/v1/v1.0/v1.0.0/sg-image-crop.{js,css}` | v1.0.0 | `SgImageCrop` (Web Component) | **NEW** — Interactive crop overlay with drag handles, aspect ratio presets, coordinate mapping. |
| sg-image-redact.js | `sgraph_ai_tools__static/components/image/v1/v1.0/v1.0.0/sg-image-redact.{js,css}` | v1.0.0 | `SgImageRedact` (Web Component) | **NEW** — Redact overlay: draw rectangles, black fill or pixelate mode, numbered regions. |
| sg-image-text.js | `sgraph_ai_tools__static/components/image/v1/v1.0/v1.0.0/sg-image-text.{js,css}` | v1.0.0 | `SgImageText` (Web Component) | **NEW** — Text placement: click to position, font/size/colour/bold/italic/outline controls. |
| sg-send-drop.js | `sgraph_ai_tools__static/components/send-drop/v1/v1.0/v1.0.0/sg-send-drop.{js,css}` | v1.0.0 | `SgSendDrop` (Web Component) | **NEW** — Share-via-SG/Send drop zone. Simple token generation, SGMETA envelope, AES-256-GCM encryption, 3-step upload API. Copy token/link/open buttons. |
| sg-send-receive.js | `sgraph_ai_tools__static/components/send-receive/v0/v0.1/v0.1.0/sg-send-receive.{js,css}` | v0.1.0 | `SgSendReceive` (Web Component) | **NEW** — Receive-side SG/Send component. Shadow DOM. Accepts a friendly token (`word-word-NNNN`), derives transfer ID + AES-256-GCM key via PBKDF2, downloads ciphertext, decrypts SGMETA envelope, returns `{ filename, blob, objectUrl, token, transferId }`. Events: `sg-receive-start`, `sg-receive-progress`, `sg-receive-complete`, `sg-receive-error`. Public API: `receive(token?)`, `setToken(token)`, `reset()`. Attributes: `send-url`, `token`, `placeholder`. Pair to `sg-send-drop` for a full send/receive workflow on any tool page. |
| sg-transfer-progress.js | `sgraph_ai_tools__static/components/transfer-progress/v1/v1.0/v1.0.0/sg-transfer-progress.js` | v1.0.0 | `SgTransferProgress` (Web Component), `normalizeProgress()`, `formatSpeed()`, `formatEta()` | **NEW** — Reusable upload/download progress bar. Zero dependencies. Accepts both `ChunkedUploader`/`ChunkedDownloader` UploadProgress/DownloadProgress objects (percent, bytesTotal, bytesUploaded/bytesDownloaded, bytesPerSecond, etaSecs, stage, chunksTotal, chunksDone) and the simpler `{ loaded, total, phase }` format. `normalizeProgress()` normalises either shape. Renders: phase label, animated bar (CSS transition), MB/MB bytes row, speed (MB/s) + ETA row (hidden when no speed data), N/M parts row (hidden for non-multipart). CSS custom properties: `--sg-tp-bar-bg/fill/fill-complete/text/text-muted/height/radius/gap`. Shadow DOM — no external CSS file. Compatible with sg-send-upload v1.1+ and sg-send-download v1.0+. |
| sg-video-splitter.js | `sgraph_ai_tools__static/components/video-splitter/v1/v1.0/v1.0.0/sg-video-splitter.{js,css}` | v1.0.0 | `SgVideoSplitter` (Web Component) | **NEW** — Video splitter/trimmer/extractor. Three modes: split (fixed/custom segments), trim (cut both ends), extract (audio-only or video-only). FFmpeg WASM via sg-video.js. |
| sg-llm-events.js | `sgraph_ai_tools__static/components/llm/sg-llm-events/v0/v0.1/v0.1.0/sg-llm-events.js` | v0.1.0 | `SGL_LLM` (frozen event constants, 34 events) | **NEW** — Shared event contract for the entire sg-llm component family. Frozen — additions only. Also on `window.SGL_LLM`. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.0/sg-llm-request.js` | v0.1.0 | `SgLlmRequest` (headless Web Component) | **NEW** — Headless fetch engine. 4 providers (OpenRouter, Ollama, Anthropic, OpenAI). Streaming SSE. Multimodal. AbortController cancel. Event-scoped to parentElement. |
| sg-llm-connection.js | `sgraph_ai_tools__static/components/llm/sg-llm-connection/v0/v0.1/v0.1.0/sg-llm-connection.js` | v0.1.0 | `SgLlmConnection` (Web Component) | **NEW** — Provider/key/model management panel. 4 provider tabs, model listing, localStorage persistence, key show/hide. |
| sg-llm-output.js | `sgraph_ai_tools__static/components/llm/sg-llm-output/v0/v0.1/v0.1.0/sg-llm-output.js` | v0.1.0 | `SgLlmOutput` (Web Component) | **NEW** — Streaming response display. Built-in markdown renderer (no deps). Copy/download. Bundle restore. |
| sg-llm-reality-core.js | `sgraph_ai_tools__static/components/llm/sg-llm-reality/v0/v0.1/v0.1.0/sg-llm-reality-core.js` | v0.1.0 | `BLOCK_TYPES`, `createBlock()`, `estimateTokens()`, `totalTokens()`, `blockTokens()`, `assembleMessages()`, `assembleMemoryUpdateMessages()` | **NEW** — Pure JS logic module (no DOM). Block factory, token estimation, messages array assembly for both send modes. |
| sg-llm-block.js | `sgraph_ai_tools__static/components/llm/sg-llm-reality/v0/v0.1/v0.1.0/sg-llm-block.js` | v0.1.0 | `SgLlmBlock` (Web Component) | **NEW** — Single block editor for system/context/history/memory/question. setData/getData API. Emits plain DOM events (block-change, block-remove, block-toggle). |
| sg-llm-reality.js | `sgraph_ai_tools__static/components/llm/sg-llm-reality/v0/v0.1/v0.1.0/sg-llm-reality.js` | v0.1.0 | `SgLlmReality` (Web Component) | **NEW** — Reality constructor orchestrator. Manages sg-llm-block list. Two send modes (Build + Memory-update). Attachment awareness. Bundle restore. REALITY_CHANGED + SEND emitter. |
| sg-llm-attachments.js | `sgraph_ai_tools__static/components/llm/sg-llm-attachments/v0/v0.1/v0.1.0/sg-llm-attachments.js` | v0.1.0 | `SgLlmAttachments` (Web Component) | **NEW** — File drop zone, clipboard paste (Ctrl+V text + screenshots), file picker, attachment cache. Emits ATTACHMENT_ADDED/REMOVED/CLEARED. Text files → textContent, images → dataUrl. |
| sg-llm-stats.js | `sgraph_ai_tools__static/components/llm/sg-llm-stats/v0/v0.1/v0.1.0/sg-llm-stats.js` | v0.1.0 | `SgLlmStats` (Web Component) | **NEW** — Token accounting panel. Pre-send estimate (from REALITY_CHANGED), last-request metrics (prompt/completion/cost/latency/speed), session totals, streaming toggle (emits STREAMING_CHANGED). |
| sg-llm-debug.js | `sgraph_ai_tools__static/components/llm/sg-llm-debug/v0/v0.1/v0.1.0/sg-llm-debug.js` | v0.1.0 | `SgLlmDebug` (Web Component) | **NEW** — Full request inspector. Captures every SEND event, logs messages array colour-coded by block type (system=blue, context=green, memory=cyan, history=amber, question=purple). Expandable entries with response/error. Pure observer. |
| sg-llm-bundle.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle/v0/v0.1/v0.1.0/sg-llm-bundle.js` | v0.1.0 | `SgLlmBundle` (Web Component, headless) | **NEW** — Headless bundle orchestrator. Saves/loads/deletes bundles to localStorage. `parent_id` fork tree for time-travel. Auto-save on REQUEST_COMPLETE. Index management (newest-first). API: `saveBundle()`, `loadBundle(id)`, `deleteBundle(id)`, `getBundle(id)`, `getIndex()`, `getActiveBundleId()`. |
| sg-llm-bundle-list.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle-list/v0/v0.1/v0.1.0/sg-llm-bundle-list.js` | v0.1.0 | `SgLlmBundleList` (Web Component) | **NEW** — Visual bundle browser. Reverse-chronological list. Click to load (time travel). Delete button. Active bundle highlighted. Reads index from sibling `sg-llm-bundle`. Attrs: `show-save-button`, `max-entries`. |
| sg-llm-infographic.js | `sgraph_ai_tools__static/components/llm/sg-llm-infographic/v0/v0.1/v0.1.0/sg-llm-infographic.js` | v0.1.0 | `SgLlmInfographic` (Web Component) | **NEW** — SVG infographic extractor + renderer. Live SVG preview during streaming (REQUEST_CHUNK). Final extraction on REQUEST_COMPLETE. 6 style presets (executive/technical/playful/minimal/bold/data). Export SVG + PNG. `getSystemPrompt()` for reality system block. SVG sanitised (strips script + event handlers). |
| sg-llm-model-picker.js | `sgraph_ai_tools__static/components/llm/sg-llm-model-picker/v0/v0.1/v0.1.0/sg-llm-model-picker.js` | v0.1.0 | `SgLlmModelPicker` (Web Component) | **NEW** — Compact model dropdown with live refresh. 4 providers (openrouter/ollama/anthropic/openai). setProvider/setModel/getModel API. Emits llm:model-changed, consumes llm:connected. Also on `window.SgLlmModelPicker`. |
| sg-llm-chat-history.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-history/v0/v0.1/v0.1.0/sg-llm-chat-history.js` | v0.1.0 | `SgLlmChatHistory` (Web Component) | **NEW** — Multi-turn conversation display + state manager. Owns turn array. User/assistant bubbles (user right, assistant left). Collapsible system prompt editor. Streaming live updates. Multimodal attachment support (images inline, text files appended). Assembles messages array + emits SEND. API: `getMessages()`, `setSystemPrompt()`, `clear()`, `exportMarkdown()`. |
| sg-llm-chat-history.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-history/v0/v0.1/v0.1.1/sg-llm-chat-history.js` | v0.1.1 | **BUG FIX** — Scroll lock: auto-scroll pauses on manual scroll-up; ↓ jump button resumes. Raw text during REQUEST_CHUNK, full markdown on REQUEST_COMPLETE (avoids broken partial markdown artefacts). |
| sg-llm-chat-input.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-input/v0/v0.1/v0.1.0/sg-llm-chat-input.js` | v0.1.0 | `SgLlmChatInput` (Web Component) | **NEW** — Message input bar. Auto-resize textarea (Enter=send, Shift+Enter=newline). File picker + drag-and-drop + clipboard paste. Attachment previews (image thumbs, filename badges). Disables during REQUEST_START, shows Cancel. Emits `llm:chat-message { text, attachments }`. API: `clear()`, `focus()`, `setDisabled()`. |
| sg-llm-chat-input.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-input/v0/v0.1/v0.1.1/sg-llm-chat-input.js` | v0.1.1 | **BUG FIX** — Textarea stays writable during streaming (only Send disabled, Cancel shown). Fixed clipboard image paste via `shadowRoot.activeElement`. |
| sg-llm-token-viz.js | `sgraph_ai_tools__static/components/llm/sg-llm-token-viz/v0/v0.1/v0.1.0/sg-llm-token-viz.js` | v0.1.0 | `SgLlmTokenViz` (Web Component) | **NEW** — Token breakdown visualiser. Stacked bar by section (system/history/current/images). Cost estimate from built-in pricing table (14 models). Context growth sparkline across last 20 requests. Session totals (tokens + cost + request count). Consumes SEND for pre-request estimate, REQUEST_COMPLETE for actuals. API: `getStats()`, `clear()`. |
| sg-llm-attachment-manager.js | `sgraph_ai_tools__static/components/llm/sg-llm-attachment-manager/v0/v0.1/v0.1.0/sg-llm-attachment-manager.js` | v0.1.0 | `SgLlmAttachmentManager` (Web Component) | **NEW** — Standalone attachment UI. File picker, drag-and-drop, clipboard paste (images). Image thumbnails + filename badges with remove buttons. Emits standard `llm:attachment-added/removed/cleared` events. Optional `disable-on-send` attr. Composable with any LLM component. API: `getAttachments()`, `clear()`, `addFile()`, `removeAttachment()`. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.1/sg-llm-request.js` | v0.1.1 | **NEW** — v0.1.1 adds: `rawChunks[]` (all parsed SSE objects) + `images[]` (from `delta.images`) accumulated in `_readStream()` and included in REQUEST_COMPLETE detail. Non-streaming path includes `rawResponse` (full parsed JSON). `_bus()` updated to walk `[data-llm-bus]` ancestor. |
| sg-llm-response-inspector.js | `sgraph_ai_tools__static/components/llm/sg-llm-response-inspector/v0/v0.1/v0.1.0/sg-llm-response-inspector.js` | v0.1.0 | `SgLlmResponseInspector` (Web Component) | **NEW** — Collapsible JSON browser for LLM response data. 4 tabs: Summary (model/tokens/latency/finish_reason), Images (inline preview, OpenAI image_url + Anthropic base64), Chunks (Prev/Next SSE chunk navigation), Full Data (collapsible JSON tree + Expand/Collapse All). Requires sg-llm-request v0.1.1+. API: `clear()`, `getData()`. |
| sg-llm-chat-history.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-history/v0/v0.1/v0.1.2/sg-llm-chat-history.js` | v0.1.2 | **NEW** — v0.1.2 adds: assistant bubble image rendering from `e.detail.images[]` (REQUEST_COMPLETE); `_bus()` walks `[data-llm-bus]` ancestor; tracks attachments from sg-llm-attachment-manager events (`llm:attachment-added/removed/cleared`) and merges them with chat-message attachments on SEND; emits `llm:attachments-cleared` after SEND to reset attachment-manager UI. |
| sg-llm-chat-input.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-input/v0/v0.1/v0.1.2/sg-llm-chat-input.js` | v0.1.2 | **NEW** — v0.1.2: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-connection.js | `sgraph_ai_tools__static/components/llm/sg-llm-connection/v0/v0.1/v0.1.1/sg-llm-connection.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-stats.js | `sgraph_ai_tools__static/components/llm/sg-llm-stats/v0/v0.1/v0.1.1/sg-llm-stats.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-bundle.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle/v0/v0.1/v0.1.1/sg-llm-bundle.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-bundle-list.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle-list/v0/v0.1/v0.1.1/sg-llm-bundle-list.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-token-viz.js | `sgraph_ai_tools__static/components/llm/sg-llm-token-viz/v0/v0.1/v0.1.1/sg-llm-token-viz.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-attachment-manager.js | `sgraph_ai_tools__static/components/llm/sg-llm-attachment-manager/v0/v0.1/v0.1.1/sg-llm-attachment-manager.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-model-picker.js | `sgraph_ai_tools__static/components/llm/sg-llm-model-picker/v0/v0.1/v0.1.1/sg-llm-model-picker.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-debug.js | `sgraph_ai_tools__static/components/llm/sg-llm-debug/v0/v0.1/v0.1.1/sg-llm-debug.js` | v0.1.1 | **NEW** — v0.1.1: `_bus()` updated to walk `[data-llm-bus]` ancestor (backwards compatible). |
| sg-llm-attachment-manager.js | `sgraph_ai_tools__static/components/llm/sg-llm-attachment-manager/v0/v0.1/v0.1.2/sg-llm-attachment-manager.js` | v0.1.2 | **BUG FIX** — Paste guard walks shadow roots to avoid double-capture with sg-llm-chat-input; always listens for llm:attachments-cleared; remove button (✕) color corrected. |
| sg-llm-attachment-manager.js | `sgraph_ai_tools__static/components/llm/sg-llm-attachment-manager/v0/v0.1/v0.1.3/sg-llm-attachment-manager.js` | v0.1.3 | **NEW** — PDF, audio, video file support. Binary types read as dataUrl (base64). Type-specific icons: 📄 PDF, 🎵 audio, 🎬 video. Updated DEFAULT_ACCEPT and file picker. |
| sg-llm-chat-input.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-input/v0/v0.1/v0.1.3/sg-llm-chat-input.js` | v0.1.3 | **NEW** — PDF, audio, video file support via file picker and drag-and-drop. Binary types read as dataUrl (base64). Type-specific icons: 📄 PDF, 🎵 audio, 🎬 video. Updated accept string. |
| sg-llm-chat-history.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-history/v0/v0.1/v0.1.8/sg-llm-chat-history.js` | v0.1.8 | **BUG FIX** — `_assembleMsgs` now includes binary file attachments (PDF/audio/video) as `binary_file` content parts. Previously these were silently dropped — only images and text reached the API. `_addBubble` shows type-specific icons. Requires sg-llm-request v0.1.2+. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.2/sg-llm-request.js` | v0.1.2 | **NEW** — Translates `binary_file` content parts: Anthropic PDF → `document` block; OpenAI/OpenRouter audio → `input_audio` block; Ollama — all binary types silently dropped (unsupported). |
| sg-llm-chat-history.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-history/v0/v0.1/v0.1.9/sg-llm-chat-history.js` | v0.1.9 | **BUG FIX** — Registers itself on the bus element as `bus.__sgLlmChatHistory = this` in `connectedCallback` and de-registers in `disconnectedCallback`. Enables `sg-llm-bundle` to capture chat state across `sg-layout`'s shadow DOM boundary (querySelector cannot pierce shadow DOM). |
| sg-llm-chat-history.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-history/v0/v0.1/v0.1.10/sg-llm-chat-history.js` | v0.1.10 | **NEW** — Adds copy and save buttons to image message bubbles. Each image in the chat now shows a three-button row: "Open ↗" (opens in new tab), "Copy image" (draws onto canvas, copies PNG to clipboard via `navigator.clipboard.write()`), "Save ↓" (downloads via `<a download>`). Helper `_imgUrlToPngBlob(url)` draws the image onto a Canvas and resolves a PNG Blob for clipboard API. |
| sg-llm-bundle.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle/v0/v0.1/v0.1.6/sg-llm-bundle.js` | v0.1.6 | **BUG FIX** — `_captureChat()` reads `bus.__sgLlmChatHistory` (registration pattern) instead of `querySelector('sg-llm-chat-history')`. Fixes bundle chat capture when chat-history is inside `sg-layout`'s shadow DOM. Debug logging removed (was v0.1.5). |
| sg-llm-bundle-list.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle-list/v0/v0.1/v0.1.2/sg-llm-bundle-list.js` | v0.1.2 | **NEW** — Adds `{}` inspect button per entry alongside the delete button. Clicking emits `llm:bundle-inspect-requested { bundleId }` — consumed by `sg-llm-bundle-viewer` to display raw bundle data. |
| sg-llm-bundle-viewer.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle-viewer/v0/v0.1/v0.1.0/sg-llm-bundle-viewer.js` | v0.1.0 | **NEW** — Raw bundle data inspector. Listens for `llm:bundle-inspect-requested { bundleId }`. Fetches bundle from sibling `sg-llm-bundle.getBundle(id)`. 4 tabs: Overview (metadata table), Chat (turns with attachment badges), Reality (blocks), JSON (collapsible tree + Expand/Collapse All + Copy JSON). Attr: `follow-active` to auto-show the active bundle. Dark theme. |
| sg-llm-events.js | `sgraph_ai_tools__static/components/llm/sg-llm-events/v0/v0.1/v0.1.1/sg-llm-events.js` | v0.1.1 | `SGL_LLM` (frozen event constants, 41 events) | **NEW** — Extends v0.1.0 with tool calling + agentic loop events: `TOOL_CALLS` (LLM returned tool calls), `TOOL_RESULT` (single tool executed), `TOOL_RESULTS_COMPLETE` (batch done, messages extended), `AGENTIC_START/STEP/DONE/ABORT`. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.6/sg-llm-request.js` | v0.1.6 | **BUG FIX** — Corrects OpenRouter PDF format. v0.1.5 used `{type:'document'}` Anthropic-native blocks which OpenRouter strips on its OpenAI-compatible endpoint. v0.1.6 uses `{type:'file', file:{filename, file_data}}` — OpenRouter's actual content type for PDFs, works for all models (not just `anthropic/*`). The `isAnthropicModel` restriction removed. `sg-llm-request-builders.js` and `sg-llm-request-parsers.js` updated accordingly. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.5/sg-llm-request.js` | v0.1.5 | **NEW** — Split into 3 files: `sg-llm-request-builders.js` (headers, body builders, endpoint), `sg-llm-request-parsers.js` (SSE delta, usage, images, tool calls), `sg-llm-request.js` (Web Component). Two bug fixes: (1) Anthropic: added `anthropic-dangerous-direct-browser-access: true` header required for direct browser-to-API calls. (2) OpenRouter: PDF `binary_file` parts now translated to Anthropic `document` blocks when model starts with `anthropic/` — OpenRouter forwards these to Anthropic's API for Claude models. All v0.1.4 tool-calling behaviour preserved. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.4/sg-llm-request.js` | v0.1.4 | **NEW** — Tool calling support. `llm:send` detail now accepts `tools[]`, `tool_choice`, `response_format`. Request bodies include tools for all 4 providers (Anthropic tool format auto-converted from OpenAI JSON schema). Streaming: accumulates `tool_calls` deltas by index. Non-streaming: extracts `tool_calls` from response. Emits `llm:tool-calls` alongside `llm:request-complete` when tool calls present. `REQUEST_COMPLETE` detail gains `toolCalls[]` (always present, may be empty). Imports `sg-llm-events v0.1.1`. |
| sg-llm-chat-input.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-input/v0/v0.1/v0.1.4/sg-llm-chat-input.js` | v0.1.4 | **BUG FIX** — `:host` and `.ci-wrap` now set `width:100%; box-sizing:border-box` so the component reflows correctly when the containing sg-layout panel is resized horizontally. |
| sg-llm-chat-input.js | `sgraph_ai_tools__static/components/llm/sg-llm-chat-input/v0/v0.1/v0.1.5/sg-llm-chat-input.js` | v0.1.5 | **BUG FIX** — Adds `ResizeObserver` in `connectedCallback` watching `:host` element. When the sg-layout panel is resized vertically, `_resize(ta)` is called to recompute the textarea height, preventing it from staying at its initial size after panel drag. |
| sg-llm-request.js | `sgraph_ai_tools__static/components/llm/sg-llm-request/v0/v0.1/v0.1.3/sg-llm-request.js` | v0.1.3 | **BUG FIX** — HTTP error responses now parse the JSON body to extract the provider's human-readable message (e.g. OpenRouter: "No endpoints found that support input audio"). Full message used in `err.message` and forwarded in `REQUEST_ERROR` as `detail.bodyError`. Previously only the generic HTTP status text was shown. |
| sg-llm-bundle.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle/v0/v0.1/v0.1.7/sg-llm-bundle.js` | v0.1.7 | **NEW** — Adds `importBundle(data)` method. Accepts a parsed bundle JSON object, assigns a fresh ID (avoids collisions), saves to localStorage, emits `BUNDLE_SAVED` + `BUNDLE_LIST_CHANGED`, then auto-loads the bundle (emits `BUNDLE_LOADED`) so the UI restores immediately. |
| sg-llm-bundle-list.js | `sgraph_ai_tools__static/components/llm/sg-llm-bundle-list/v0/v0.1/v0.1.3/sg-llm-bundle-list.js` | v0.1.3 | **NEW** — Export + Import. ↓ download button per entry (downloads full bundle as `bundle-{id}.json`). Drag-and-drop .json files onto the list to import via `sg-llm-bundle.importBundle()` (auto-loads on import). "Import" file picker button in the header as DnD alternative. Requires sg-llm-bundle v0.1.7+. |
| sg-openrouter-balance.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-balance/v0/v0.1/v0.1.0/sg-openrouter-balance.js` | v0.1.0 | **NEW** — OpenRouter credit balance badge. Fetches `GET /api/v1/key`. Three states: healthy (green >20%), low (amber 5–20%), critical (red <5%). Reacts to `llm:connected` (openrouter only), `llm:disconnected`, `llm:request-complete`. `compact` attribute. API: `refresh()`, `clear()`. |
| sg-openrouter-key-stats.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-key-stats/v0/v0.1/v0.1.0/sg-openrouter-key-stats.js` | v0.1.0 | **NEW** — Full OpenRouter key details panel. Replaces balance badge with rich display: label, status dot, credit bar (remaining/limit with % + colour health states), usage spent, rate limit badge, free-tier flag, created_at, expires_at, rolling period bars. 5-minute localStorage cache keyed by last-8-chars of API key. Cache age shown in toolbar. Bypasses cache on `llm:request-complete`. Reacts to `llm:connected` (openrouter), `llm:disconnected`, `llm:request-complete`. API: `refresh()`, `clear()`. |
| sg-openrouter-spending.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-spending/v0/v0.1/v0.1.0/sg-openrouter-spending.js` | v0.1.0 | **NEW** — Admin spending analytics panel. Fetches all activity pages (limit=100, up to 500 rows) via `GET /api/v1/activity`. 15-minute localStorage cache keyed by last-8-chars of management key. Shows: 4 summary cards (today/week/month/all-time cost + requests), SVG line chart (daily spend last 30 days with fill area + today dot), horizontal bar chart (top 12 models by cost), per-key-hash table (total/today/reqs enriched with labels from `or:keys-loaded`). Listens for `or:admin-connected` on document. Refresh button bypasses cache. |
| sg-openrouter-log-viewer.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-log-viewer/v0/v0.1/v0.1.0/sg-openrouter-log-viewer.js` | v0.1.0 | **NEW** — CSV log importer + viewer + analytics. Parses openrouter.ai CSV exports (columns: generation_id, created_at, cost_total, tokens_prompt, tokens_completion, model_permaslug, provider_name, api_key_name, finish_reason_normalized, generation_time_ms, etc). localStorage storage (or-logs-data, max 10,000 rows, deduped by generation_id). 3 tabs: **Import** (file drop zone + paste textarea, Parse/Append/Clear buttons, row count); **Table** (search + key dropdown + finish filter, sortable columns, 500-row DOM cap); **Analytics** (6 stat cards: total cost/success rate/avg latency/total tokens/cancelled/avg cost per req, requests-per-day SVG line chart, cost-by-model/key/provider horizontal bar charts). |
| sg-openrouter-log-viewer.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-log-viewer/v0/v0.1/v0.1.1/sg-openrouter-log-viewer.js` | v0.1.1 | **UPDATED** — Adds: (1) Custom in-shadow confirmation dialog replacing browser confirm() on Clear; (2) Brighter bar colours (blue `#3b82f6` model / purple `#7c3aed` key / green `#10b981` provider) with req count sub-label; (3) Global key filter toolbar at top of Analytics — all charts re-aggregate for selected key; (4) Row detail overlay — click any table row to see all CSV fields in a two-column panel; (5) Maximize button ⤢/✕ in tab bar — toggles `position:fixed; inset:0; z-index:9999` without sg-layout changes; (6) New **Daily** tab — dual-line SVG chart (blue requests + amber cost, separate axes) + per-day sortable table (date/requests/cost/avg cost/tokens/success%/avg latency), filtered by shared key filter. |
| sg-openrouter-models.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-models/v0/v0.1/v0.1.0/sg-openrouter-models.js` | v0.1.0 | **NEW** — OpenRouter model explorer. Fetches `GET /api/v1/models`. Searchable/filterable table: name, context length, pricing ($/M tokens). Filter buttons: free, vision, code, cheap. Column sort. Emits `llm:model-changed` on row click. Optional `provider` attribute to pre-filter by provider slug. |
| sg-openrouter-usage.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-usage/v0/v0.1/v0.1.0/sg-openrouter-usage.js` | v0.1.0 | **NEW** — OpenRouter spending dashboard panel. Shows daily/weekly/monthly usage as progress bars with % of limit. Also shows key label, rate limit, free-tier status, expiry. Auto-refreshes on `llm:request-complete`. Fetches `GET /api/v1/key`. Consumes `llm:connected`/`llm:disconnected`/`llm:request-complete`. API: `refresh()`, `clear()`. |
| sg-openrouter-key-manager.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-key-manager/v0/v0.1/v0.1.0/sg-openrouter-key-manager.js` | v0.1.0 | **NEW** — Admin panel for OpenRouter provisioned keys. Requires a management key (separate from user API key, stored in localStorage). Lists all sub-keys with usage/limit bars. Create (name, credit limit, reset period), inline edit (rename, change limit/reset, enable/disable), delete with confirm. New key value shown once on create. No bus events — fully standalone. API: `refresh()`. |
| sg-openrouter-generation.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-generation/v0/v0.1/v0.1.0/sg-openrouter-generation.js` | v0.1.0 | **NEW** — Generation request detail viewer. Auto-captures generation IDs from `llm:request-complete` events. Calls `GET https://openrouter.ai/api/v1/generation?id={id}`. Internal fractal sg-layout: left panel (Recent history) + right tabs (Detail / Raw JSON). Detail tab: cost + token bar (prompt/completion/reasoning/cached breakdown) + formatted request/latency/token/cost sections. History stored in sessionStorage (up to 50). Manual fetch via ID input. `fetchGeneration(id)`, `clear()` API. Reacts to `llm:connected`, `llm:disconnected`, `llm:request-complete`, `or:generation-selected`, `or:admin-connected`. |
| sg-openrouter-generation.js | `sgraph_ai_tools__static/components/openrouter/sg-openrouter-generation/v0/v0.1/v0.1.1/sg-openrouter-generation.js` | v0.1.1 | **UPDATED** — `_fetch` retries up to 3 times (2 s / 4 s / 6 s back-off) on HTTP 404 so generation records have time to propagate on OpenRouter before reporting failure. `hide-recent` boolean attribute: when present the inner layout renders only Detail + Raw JSON tabs without the Recent history column. |
| sg-infographic-model-picker.js | `sgraph_ai_tools__static/components/infographic/sg-infographic-model-picker/v0/v0.1/v0.1.0/sg-infographic-model-picker.js` | v0.1.0 | **NEW** — Curated model selector for infographic generation. Hard-coded list (no API fetch): Top 3 Recommended optgroup + Image Models optgroup + SVG/Text Models optgroup. Each option shows `label · cost · speed` (e.g. `Gemini 2.5 Flash Image · ~$0.08/img ⚡⚡⚡`). Exports: `ALL_IMAGE_MODELS`, `ALL_SVG_MODELS`, `ALL_MODELS`, `TOP_3_MODELS`, `DEFAULT_MODEL`, `isImageModel(id)`. API: `getModel()`, `setModel(id)`. Fires `infographic:model-changed { model }` (bubbles, composed). |
| sg-tool-api-explorer.js | `sgraph_ai_tools__static/components/tool-api/sg-tool-api-explorer/v0/v0.1/v0.1.0/sg-tool-api-explorer.js` | v0.1.0 | **NEW** — Live debug panel Web Component for any SgToolApi instance. Shadow DOM, 4 tabs: Health (status badge, instanceId, methodCount, version), Methods (list of registered methods), Events (live stream of all `SGA_TOOL` window events, click-to-expand detail, newest-first, 100-event ring buffer), Log (execution log entries from `meta.getLog()`, click-to-expand params+result, newest-first). Auto-binds to `window.__tool` or `window.__tool_registry.findById(tool-id attr)`. Polls `meta.health()` + log every 2s. `tool-id` attribute for multi-instance pages. |
| sg-tool-api-console.js | `sgraph_ai_tools__static/components/tool-api/sg-tool-api-console/v0/v0.1/v0.1.0/sg-tool-api-console.js` | v0.1.0 | **NEW** — Interactive method caller Web Component for any SgToolApi instance. Shadow DOM, two-panel layout: left 260px call-panel (method list from `meta.getMethods()`, JSON params textarea pre-populated from per-method templates, ▶ Call button), right output-panel (result display green=ok/red=error + duration, 20-entry history with click-to-restore). Invokes `window.__tool[method](params)` asynchronously. Auto-binds to `window.__tool`. `tool-id` attribute for multi-instance pages. |
| sg-tool-api-manifest.js | `sgraph_ai_tools__static/components/tool-api/sg-tool-api-manifest/v0/v0.1/v0.1.0/sg-tool-api-manifest.js` | v0.1.0 | **NEW** — Manifest and API spec viewer Web Component for any SgToolApi instance. Shadow DOM, 3 tabs: Identity (name/slug/version/status badge/category/description/dependencies grouped by core/components/shared), API (method accordion cards with params+type+optional/required+returns+note, events list with when+detail), Skills (expandable cards showing raw fetched SKILL file content). Fetches data via `meta.getManifest()` + `meta.getSkills()`. Auto-binds to `window.__tool`. `tool-id` attribute for multi-instance pages. |
| sg-infographic-export.js | `sgraph_ai_tools__static/components/infographic/sg-infographic-export/v0/v0.1/v0.1.0/sg-infographic-export.js` | v0.1.0 | **NEW** — Download / copy-to-clipboard / share controls for a generated infographic. Hidden (`:host { display:none }`) until `setSource(imgSrc, filename)` is called. Three action buttons: **⬇ Download PNG/SVG** (creates `<a download>`), **⎘ Copy to Clipboard** (Canvas `toBlob` → `ClipboardItem`), **↗ Share via SG/Send** (reveals `sg-send-drop` widget, calls `offerFile(blob, filename)`). `imgSrcToBlob()` helper lives inside the component. |
| sg-vault-connect.js | `sgraph_ai_tools__static/components/vault/sg-vault-connect/v0/v0.1/v0.1.0/sg-vault-connect.js` | v0.1.0 | **NEW** — Vault connection panel. Key entry form (full `passphrase:vaultId` or simple token `word-word-NNNN`), server URL input, connect/disconnect button, status. Derives write-capable keys via `sg-vault-write`. Emits `vault:connected { vault, vaultId, apiBaseUrl, keys }` and `vault:disconnected {}` on `[data-vault-bus]`. localStorage persistence for vault key + API URL. |
| sg-vault-connect.js | `sgraph_ai_tools__static/components/vault/sg-vault-connect/v0/v0.1/v0.1.3/sg-vault-connect.js` | v0.1.3 | **UPDATED** — Session-based. Creates a `VaultSession` via `createSession()`, calls `session.open()` during connect. Emits `vault:connected { session, vault, vaultId, apiBaseUrl, keys }` — session object is the primary interface for other components. Shows tree stats (files/folders) in connected info. |
| sg-vfs-bus.js | `sgraph_ai_tools__static/components/vfs/sg-vfs-bus/v0/v0.1/v0.1.0/sg-vfs-bus.js` | v0.1.0 | `SgVfsBus` (Web Component) | **UPDATED** — Headless `<sg-vfs-bus>` component. `provider` attribute now supports `"memory"`, `"indexeddb"`, and `"layered"` (Memory priority 0 + IndexedDB priority 1). Forwards original requestId from inbound events so response IDs match. Emits PROVIDER_REGISTERED + PROVIDER_READY on init. |
| sg-vfs-seeder.js | `sgraph_ai_tools__static/components/vfs/sg-vfs-seeder/v0/v0.1/v0.1.0/sg-vfs-seeder.js` | v0.1.0 | `SgVfsSeeder` (Web Component) | **NEW** — VFS test-data seeder. Shadow DOM UI with 4 preset buttons: Web Project, Docs Folder, Sample Data, Deep Tree. Each preset defines a realistic file+folder structure with real content (JS modules, CSS, HTML, Markdown, CSV, JSON). Seeds via `vfs:write-request`/`vfs:write-response` event bus protocol — no direct VFS reference needed. `#vfsOp()` dispatches request, awaits matching response by requestId with 10s timeout. Progress bar updates per-file. Dispatches `sg-vfs-seeder:complete { preset, label, total, errors, wallTime }` on bus parent. Emits `SGL_VFS.BATCH_STARTED/PROGRESS/COMPLETE` for event viewer timeline. |
| sg-vfs-event-viewer.js | `sgraph_ai_tools__static/components/vfs/sg-vfs-event-viewer/v0/v0.1/v0.1.0/sg-vfs-event-viewer.js` | v0.1.0 | `SgVfsEventViewer` (Web Component) | **UPDATED** — 4-tab VFS event viewer. Timeline (colour-coded log, click-to-expand JSON, auto-scroll, 500-event buffer); Operations (table grouped by requestId: op, path, status, wallTime); Providers (cards per provider: hits/misses/writes/errors, hit-rate bar); Latency (per-op stats: count/min/avg/p95/max + proportional bar). |
| sg-vault-tree.js | `sgraph_ai_tools__static/components/vault/sg-vault-tree/v0/v0.1/v0.1.0/sg-vault-tree.js` | v0.1.0 | **NEW** — Vault file/folder tree panel. Listens for `vault:connected` → loads ref → commit → tree chain. Expand/collapse folders with lazy sub-tree loading. Emits `vault:file-select { path, fileId, size }` on click. New-file button emits `vault:new-file-request`. Consumes `vault:tree-refresh` to reload. Uses `[data-vault-bus]` ancestor. |
| sg-vault-tree.js | `sgraph_ai_tools__static/components/vault/sg-vault-tree/v0/v0.1/v0.1.3/sg-vault-tree.js` | v0.1.3 | **UPDATED** — Session-based. Uses `session.treeModel` for persistent state and `session.loadSubTree()` for lazy loading. No direct vault-client imports. Expanded-folder state persisted across refreshes via `_expanded` Set. |
| sg-vault-viewer.js | `sgraph_ai_tools__static/components/vault/sg-vault-viewer/v0/v0.1/v0.1.0/sg-vault-viewer.js` | v0.1.0 | **NEW** — Vault file viewer + editor. Listens for `vault:file-select` → fetches+decrypts+displays content. Edit mode (textarea), Save button calls `writeVaultFile()` (creates blob + tree + commit + ref update). Binary files show notice. Emits `vault:file-saved { path }` and `vault:tree-refresh {}` after save. |
| sg-vault-viewer.js | `sgraph_ai_tools__static/components/vault/sg-vault-viewer/v0/v0.1/v0.1.3/sg-vault-viewer.js` | v0.1.3 | **UPDATED** — Session-based. Uses `session.getFile()` for cached reads and `updateFile()` from mutations module for saves. Respects `session.writable` to hide edit button for read-only sessions. Emits `vault:file-saved { path, commitId }`. |
| sg-vault-sync-status.js | `sgraph_ai_tools__static/components/vault/sg-vault-sync-status/v0/v0.1/v0.1.0/sg-vault-sync-status.js` | v0.1.0 | **NEW** — Push/pull sync status bar. Shows ahead/behind badges + push/pull buttons. Polls `session.getBehindCount()` on configurable interval (default 30s). Uses session.push()/pull(). Emits `vault:sync-pushed`, `vault:sync-pulled`, `vault:tree-refresh`. |
| sg-vault-file-ops.js | `sgraph_ai_tools__static/components/vault/sg-vault-file-ops/v0/v0.1/v0.1.0/sg-vault-file-ops.js` | v0.1.0 | **NEW** — File/folder operation toolbar. Buttons for new file, new folder, rename, delete. Modal dialogs for input/confirmation. Uses mutations module. Only visible when session.writable. Emits `vault:tree-refresh` after operations. |
| sg-vault-commit-log.js | `sgraph_ai_tools__static/components/vault/sg-vault-commit-log/v0/v0.1/v0.1.0/sg-vault-commit-log.js` | v0.1.0 | **NEW** — Commit history viewer. Walks commit chain from HEAD (up to 50 commits), decrypts messages, shows timestamps and LOCAL/REMOTE badges. Auto-refreshes on file-saved and sync-pulled events. |
| sg-vault-file-preview.js | `sgraph_ai_tools__static/components/vault/sg-vault-file-preview/v0/v0.1/v0.1.0/sg-vault-file-preview.js` | v0.1.0 | **NEW** — Rich file preview. Renders by extension: .md → HTML via sg-markdown, images (.png/.jpg/.gif/.webp) → object URL, .svg inline, .json → pretty-printed with syntax highlight, .csv/.tsv → table, .html → sandboxed iframe, fallback → plain text. Read-only (use viewer for edit). Listens for vault:file-select. |
| sg-vault-create.js | `sgraph_ai_tools__static/components/vault/sg-vault-create/v0/v0.1/v0.1.0/sg-vault-create.js` | v0.1.0 | **NEW** — Vault creation UI. Generates passphrase + vaultId (regeneratable), calls `createVault()` from vault-init module to write empty tree + initial commit + named ref, then opens a session and emits `vault:created` + `vault:connected`. Shows the new vault key once for the user to save with copy buttons. |
| sg-vault-manager.js | `sgraph_ai_tools__static/components/vault/sg-vault-manager/v0/v0.1/v0.1.0/sg-vault-manager.js` | v0.1.0 | `SgVaultManager` (Web Component) | **NEW** — Multi-vault registry and connect UX. Platform component reusable across tools. Maintains `localStorage['sg-vault-registry']` (vault IDs + display names, NO keys). Dropdown of known vaults + new-key input + display name + API URL fields. Derives write-capable keys via `deriveWriteKeys` / `deriveWriteKeysFromSimpleToken`. Verifies vault access (404 = new vault, created transparently on first write). "Forget" button removes from registry. Fires `vault:connected { vault, vaultId, displayName, apiBaseUrl, keys, isNew }` and `vault:disconnected { vaultId }` on `[data-vault-bus]` ancestor (falls back to `document`). Shadow DOM. |
| sg-agentic-loop.js | `sgraph_ai_tools__static/components/agentic/sg-agentic-loop/v0/v0.1/v0.1.0/sg-agentic-loop.js` | v0.1.0 | `SgAgenticLoop` (headless Web Component) | **NEW** — Orchestrator for the full LLM ↔ tool-calling feedback loop. Listens for `llm:agentic-start` (or call `.start(task, tools?, config?)`). Manages messages array, dispatches `llm:send`, listens for `llm:tool-results-complete` to continue, detects `llm:request-complete` with no tool_calls as the stop condition. Safety: `max-iterations` attr (default 10), `cost-budget` attr in $ (default 0.50, estimated at $5/M tokens). Human-in-the-loop: `human-approval` attr shows a floating approval overlay after each tool batch — Continue or Stop buttons. Fires all agentic lifecycle events. Also listens for `llm:tool-defs-changed` to keep tools in sync with sg-tool-definition. Public API: `start()`, `stop()`, `approve()`, `reject()`, `status`, `iterations`, `steps`, `tools`. |
| sg-tool-definition.js | `sgraph_ai_tools__static/components/agentic/sg-tool-definition/v0/v0.1/v0.1.0/sg-tool-definition.js` | v0.1.0 | `SgToolDefinition` (Web Component) | **NEW** — Visual editor for LLM tool definitions. 3-tab panel: **Tools** (list with enable/disable checkboxes, expandable parameter view, delete button), **Add** (name + description + JSON schema textarea, template picker dropdown), **Import/Export** (JSON round-trip). Built-in tools imported from `sg-tool-runner` (always shown, can be disabled). Template library: `fetch_url`, `get_current_time`, `parse_csv`, `format_table`. Fires `llm:tool-defs-changed { tools }` on selection change. Public API: `getActiveTools()`, `setTools(defs)`, `addTool(def)`. |
| sg-tool-runner.js | `sgraph_ai_tools__static/components/agentic/sg-tool-runner/v0/v0.1/v0.1.0/sg-tool-runner.js` | v0.1.0 | `SgToolRunner` (headless Web Component), `BUILTIN_TOOL_DEFS` | **NEW** — Headless tool execution engine. Listens for `llm:tool-calls` on `[data-llm-bus]`. Executes tools by name from registry, assembles extended messages array (assistant tool_calls + tool results), emits `llm:tool-results-complete`. Built-in tools: `list_folder`, `read_file`, `write_file`, `delete_file` (all via VFS event bridge to `[data-vfs-bus]`), `run_code` (JS via sg-sandbox). Custom tools via `register(name, asyncFn)`. `BUILTIN_TOOL_DEFS` array exported for use in `llm:send detail.tools`. `builtins="none"` attribute disables auto-registration. Attribute `window.SgToolRunner` + `window.BUILTIN_TOOL_DEFS`. |
| sg-sandbox.js | `sgraph_ai_tools__static/core/sg-sandbox/v0/v0.2/v0.2.0/sg-sandbox.js` | v0.2.0 | `SGSandbox` | **NEW (Phase 3)** — Extends v0.1.0 with Python execution via Pyodide (main thread). `execute(code, input?, language?)` supports `'javascript'` (default, iframe sandbox) and `'python'` (Pyodide via sg-pyodide.js). Python: `input` injected as Python dict via `py.globals.set()`, stdout/stderr captured, returns `{ result, stdout, stderr, error, language }`. Timeout enforced for both languages. Pyodide instance is the sg-pyodide.js singleton (shared with other components). |
| sg-python-repl.js | `sgraph_ai_tools__static/components/agentic/sg-python-repl/v0/v0.1/v0.1.0/sg-python-repl.js` | v0.1.0 | `SgPythonRepl` (Web Component) | **NEW (Phase 3)** — Reusable Python REPL Web Component. Wraps the Pyodide loading and REPL pattern used across tool pages. Shadow DOM. Attrs: `auto-load` (load Pyodide immediately on connect), `placeholder`. Public API: `load()`, `run(code)` → Promise, `clear()`, `install(packages)`. Terminal-style output: input/stdout/stderr/error rows with distinct colours. Command history (Up/Down arrows). Package installer: preset chips (numpy/pandas/cryptography/httpx/pydantic) + custom package input. Events: `sg-python-repl:ready`, `sg-python-repl:result { code, result, stdout, stderr, error }`, `sg-python-repl:error { message }`. |
| sg-tool-runner.js | `sgraph_ai_tools__static/components/agentic/sg-tool-runner/v0/v0.1/v0.1.1/sg-tool-runner.js` | v0.1.1 | **NEW (Phase 3)** — Extends v0.1.0. `run_code` built-in now accepts `language` parameter (`'javascript'`\|`'python'`, default `'javascript'`). Uses SGSandbox v0.2.0. Python results include `{ result, stdout, stderr, error }`. BUILTIN_TOOL_DEFS `run_code` definition updated to document `language` enum. |
| sg-json-sender.js | `sgraph_ai_tools__static/components/agentic/sg-json-sender/v0/v0.1/v0.1.0/sg-json-sender.js` | v0.1.0 | `SgJsonSender` (Web Component) | **NEW (Phase 3)** — Structured JSON request builder. 3-tab panel: **Prompt** (system prompt + user message), **Schema** (field editor with name/type/description rows, 4 template chips: Summary/Classification/Data Extraction/Q&A), **Preview** (JSON payload preview). Dispatches `llm:send` with `response_format: { type:'json_schema', json_schema:{...} }`. Public API: `getSchema()`, `setSchema(schema)`. |
| sg-json-receiver.js | `sgraph_ai_tools__static/components/agentic/sg-json-receiver/v0/v0.1/v0.1.0/sg-json-receiver.js` | v0.1.0 | `SgJsonReceiver` (Web Component) | **NEW (Phase 3)** — JSON response parser with collapsible tree viewer. Listens for `llm:request-complete` on `[data-llm-bus]`. Auto-detects response type: plain text / JSON object / tool calls. JSON responses: collapsible tree (expand/collapse all), schema validation (if `setSchema()` called), copy + download buttons. Tool call responses: shows name + formatted args per call. Streaming: shows indicator during `REQUEST_CHUNK`. Events: `sg-json-receiver:parsed { type, data }`. Public API: `setSchema(schema)`, `show(data)`. |
| sg-recording-size.js | `sgraph_ai_tools__static/components/sg-recording-size/v0/v0.1/v0.1.0/sg-recording-size.{js,html,css}` | v0.1.0 | `SgRecordingSize` (Web Component) | **NEW** — Real-time recording size display. `attach(mediaRecorder)` subscribes to `dataavailable` events and accumulates byte count. `reset()` zeroes counter. `readonly bytes`, `readonly formattedSize`. Observed attributes: `threshold-mb` (default 500), `warn-at-percent` (default 80), `label`. Events: `sg-recording-size:warn { bytes, mb, thresholdMb }` at warn %, `sg-recording-size:limit` at 100%. Visual: warn→amber, limit→red. Shadow DOM + SgComponent. |
| sg-video-player.js | `sgraph_ai_tools__static/components/sg-video-player/v0/v0.1/v0.1.0/sg-video-player.{js,html,css}` | v0.1.0 | `SgVideoPlayer` (Web Component) | **NEW** — Reusable video player for Blob or URL playback. `setBlob(blob, mimeType?)` creates an internal object URL (auto-revoked). `setUrl(url)`. `play()` → Promise. `pause()`. `seek(seconds)`. `readonly duration/currentTime/paused`. `destroy()` revokes object URL — must be called before removal. v0.1.0 uses native `<video controls>`. Events: `sg-video-player:ready/play/pause/ended/error/timeupdate`. Shadow DOM + SgComponent. |
| sg-video-compressor.js | `sgraph_ai_tools__static/components/sg-video-compressor/v0/v0.1/v0.1.0/sg-video-compressor.{js,html,css}` | v0.1.0 | `SgVideoCompressor` (Web Component), `compressVideo(blob, opts)` | **NEW** — Video compression / format conversion backed by FFmpeg WASM. `compress(blob)` → Promise<Blob>. `cancel()` (best-effort). `readonly busy`. Observed attributes: `resolution` (original/1080p/720p/480p/360p), `format` (mp4/webm), `video-kbps`, `audio-kbps`. UI: resolution/format/bitrate selectors + animated progress bar + status text. Events: `sg-video-compressor:start/progress/complete/error/cancelled`. Also exports headless `compressVideo(blob, opts)` for non-UI pipelines. Depends on `sg-video.js` v1.0.1. Shadow DOM + SgComponent. |
| sg-auth-tokens.js | `sgraph_ai_tools__static/components/auth/sg-auth-tokens/v0/v0.1/v0.1.0/sg-auth-tokens.js` | v0.1.0 | `saveToken(provider,data)`, `getToken(provider)`, `hasValidToken(provider)`, `removeToken(provider)`, `listTokens()`, `clearAll()`, `SgAuthTokens` (headless Web Component) | **NEW** — Auth token localStorage store. Token shape: `{provider, idToken, accessToken?, claims, expiresAt, savedAt}`. Key: `sg-auth-token-{provider}`. Events: `sg-auth:token-saved`, `sg-auth:token-removed`, `sg-auth:tokens-cleared`. |
| sg-auth-google.js | `sgraph_ai_tools__static/components/auth/sg-auth-google/v0/v0.1/v0.1.0/sg-auth-google.js` | v0.1.0 | `SgAuthGoogle` (Web Component), `parseJwt(token)` | **NEW** — Google Identity Services (GIS) OAuth component. Lazy-loads `accounts.google.com/gsi/client`. `initialize(clientId)`, `signIn()`, `signOut()` (calls revoke + removeToken). Renders GIS button or signed-in state (avatar + name + sign-out). Attrs: `client-id`, `auto-prompt`. Events: `sg-auth:signed-in {provider, claims, idToken}`, `sg-auth:signed-out`, `sg-auth:error`. Imports sg-auth-tokens. |
| sg-auth-status.js | `sgraph_ai_tools__static/components/auth/sg-auth-status/v0/v0.1/v0.1.0/sg-auth-status.js` | v0.1.0 | `SgAuthStatus` (Web Component) | **NEW** — Compact auth state indicator. `getStatus()` → `{signedIn, expired, providers[], expiresAt, tokenCount}`. `refresh()`. `compact` attr (dot only). Three visual states: state-out (grey), state-in (teal), state-exp (amber). Listens to 5 auth events. |
| sg-auth-user.js | `sgraph_ai_tools__static/components/auth/sg-auth-user/v0/v0.1/v0.1.0/sg-auth-user.js` | v0.1.0 | `SgAuthUser` (Web Component) | **NEW** — Client-side user profile card. Shows avatar (from claims), name, email, provider badge. Expandable details: token status, expiry, sub, localStorage key count. Clear all tokens button. `show-details` attr. Listens to 5 auth events. |
| sg-credential-store.js | `sgraph_ai_tools__static/components/auth/sg-credential-store/v0/v0.1/v0.1.0/sg-credential-store.js` | v0.1.0 | `detect()`, `store(name, secret)`, `get(opts)`, `markRemoved(name)`, `getIndex()`, `SgCredentialStore` (Web Component) | **NEW** — Browser password manager integration. `detect()` → `{supported, method:'native'\|'form', browser, nativeSupported}`. `store()` tries `navigator.credentials.store(new PasswordCredential)` then falls back to hidden form submission for Safari/Firefox. Metadata index `sg-cred-index` in localStorage (names+timestamps, NO secrets). Events: `sg-cred:stored`, `sg-cred:retrieved`, `sg-cred:unsupported`, `sg-cred:error`, `sg-cred:removed`. |
| sg-credential-list.js | `sgraph_ai_tools__static/components/auth/sg-credential-list/v0/v0.1/v0.1.0/sg-credential-list.js` | v0.1.0 | `SgCredentialList` (Web Component) | **NEW** — Lists credentials stored by this app (metadata index only, no secrets). Per-entry: name, stored-at, method badge, Retrieve button, ✕ Index button. Inline add form. `refresh()`, `getCount()`. Security note: "✕ Index" removes from list only — secret stays in browser password manager. |
| sg-vault-picker.js | `sgraph_ai_tools__static/components/vault/sg-vault-picker/v0/v0.1/v0.1.0/sg-vault-picker.js` | v0.1.0 | `createVault(name)`, `openVaultByName(name)`, `saveVault(name, vaultKey)`, `listVaults()`, `removeVault(name)`, `SgVaultPicker` (Web Component) | **NEW** — Vault creation and management UI (Stage 1: localStorage). `_generateKey()` produces `{24hex}:{8hex}` mock key mirroring real SG/Send format. Create, open, import, delete vaults. Active vault banner with masked key. Stage badge: "Stage 1 · localStorage". Events: `vault:opened {vaultKey, vaultId, name, source}`, `vault:created`, `vault:saved`, `vault:removed`, `vault:cancelled`. Stage 2 will use PasswordCredential API. See `team/explorer/dev/plans/v0.1.50__auth-mvp__phase2-vault-pki-integration.md`. |
| sg-youtube-upload.js | `sgraph_ai_tools__static/components/video/upload/sg-youtube-upload/v0/v0.1/v0.1.0/sg-youtube-upload.{js,html,css}` | v0.1.0 | `SgYouTubeUpload` (Web Component) | **NEW** — `<sg-youtube-upload>` Web Component wrapping `core/youtube-upload`. Attributes: `client-id`, `default-privacy`, `default-title`, `auto-connect`. Methods: `setFile(file)`, `connect({silent})`, `upload(override)`, `signOut()`, `isConnected()`. Token cache via sibling `youtube-token-cache.js` (delegates to `sg-auth-tokens` provider `youtube-upload`, 60 s expiry skew). Designed so the cache adapter is the only file that changes when storage migrates from localStorage to vault. Events: `youtube-connected`, `youtube-disconnected`, `youtube-upload-{start,progress,complete,error}`. |
| sg-youtube-videos.js | `sgraph_ai_tools__static/components/video/manage/sg-youtube-videos/v0/v0.1/v0.1.0/sg-youtube-videos.{js,html,css}` | v0.1.0 | `SgYouTubeVideos` (Web Component) | **NEW** — `<sg-youtube-videos>` grid of the signed-in user's uploads, paginated lazily via the uploads playlist (50/page). After each page load, follow-up `getVideos()` enriches rows with privacy badge, view count, and ISO-8601 duration → `m:ss` / `h:mm:ss`. Built-in toolbar: text filter (matches title + description on already-loaded items) and sort dropdown (Newest / Oldest / Most viewed / Longest / Shortest / Title A→Z). Methods: `setToken(t)`, `refresh()` (defers if called pre-bind, replays in onReady), `loadMore()`, `getItems()`. Events: `videos-loaded`, `videos-error`, `video-selected`. Attributes: `page-size`, `auto-load`. |
| sg-youtube-video-editor.js | `sgraph_ai_tools__static/components/video/manage/sg-youtube-video-editor/v0/v0.1/v0.1.0/sg-youtube-video-editor.{js,html,css}` | v0.1.0 | `SgYouTubeVideoEditor` (Web Component) | **NEW** — `<sg-youtube-video-editor>` edit form for one video. Methods: `setToken(t)`, `setVideoSummary(s)` (queues if pre-bind), `loadVideo(id)` (queues if pre-bind, also kicks off playlist-membership fetch), `save()`, `uploadThumbnail()`, `deleteVideo()`. Edits title/description/tags/privacy. Replace thumbnail via image picker. **Inline YouTube preview** (toggle ▶ Preview / ✕ Hide preview) lazily loads `youtube.com/embed/{id}` iframe. **Playlist picker** lists user playlists with checkboxes — ticked = video is in; tick/untick fires `addToPlaylist` / `removeFromPlaylist`. Danger-zone delete with typed-title confirm (gated by `allow-delete` attribute, NOT enabled by the youtube-editor tool). Events: `video-loaded`, `video-saved`, `video-deleted`, `video-thumbnail-set`, `video-playlists-changed`, `video-error`. |
| sg-youtube-playlists.js | `sgraph_ai_tools__static/components/video/manage/sg-youtube-playlists/v0/v0.1/v0.1.0/sg-youtube-playlists.{js,html,css}` | v0.1.0 | `SgYouTubePlaylists` (Web Component) | **NEW** — `<sg-youtube-playlists>` list of the signed-in user's playlists. Click a row to expand and lazy-load its videos (cached after first expand). Click an inner item to emit `playlist-item-selected` → tools forward to VIDEO_SELECTED so the editor opens. Methods: `setToken(t)`, `refresh()`, `getItems()`. Events: `playlists-loaded`, `playlists-error`, `playlist-expanded`, `playlist-item-selected`. |
| sg-timeline.js | `sgraph_ai_tools__static/components/sg-timeline/v0/v0.1/v0.1.0/sg-timeline.js` | v0.1.0 | `SgTimeline` (Web Component), `SGT_EVENTS` | **NEW** — Phase 1 interactive timeline. Shadow DOM. Renders ruler, single video lane, clip rectangles, and playhead. Public methods: `setProject(project)`, `setPlayheadTime(t)`, `setSelectedClip(clipId\|null)`, `setPixelsPerSecond(pps)`, `setSelectedClipColor(color\|null)`. Drop target for MIME `application/x-sg-asset` (asset id payload). Emits `sg-timeline:clip-added`, `sg-timeline:clip-moved`, `sg-timeline:clip-trimmed`, `sg-timeline:clip-selected`, `sg-timeline:clip-deleted`, `sg-timeline:clip-split`, `sg-timeline:clip-color-requested`, `sg-timeline:playhead-changed` — all bubbling, composed. Pointer interactions: drag clip body to move, drag clip edges to trim, click ruler to scrub. **Never mutates the project** — only emits events; the consuming tool drives state via SgToolApi. Round-5 part B: 6-shade contrasting palette (indigo / teal / amber / rose / purple / sky); per-clip optional `color` override applied as inline style; toolbar colour-picker control (`timeline-color-picker.js`) with 12-preset swatch popover + native `<input type="color">` + Use-auto reset, mounted next to the zoom buttons; new event `sg-timeline:clip-color-requested` → consumer calls `api.setClipColor({clipId, color})` (`color: null` removes the override). Round-6 part A: colour swatch button rendered as 22×22 rounded square (no longer collapses to a vertical line); Fit button now measures the scroll viewport (first scrolling ancestor of the lane) instead of the lane itself, so projects already wider than the viewport actually shrink; new toolbar Split button (`timeline-split-button.js`) — text label "Split", title "Split selected clip at playhead (S)", enabled only when a clip is selected and playhead is strictly inside its timeline range, dispatches the same `CLIP_SPLIT_REQUESTED` as the keyboard `S` shortcut. Files: sg-timeline.{js,css}, timeline-events.js, timeline-render.js, timeline-interactions.js, timeline-zoom.js, timeline-toolbar-dom.js, timeline-color-picker.js, timeline-split-button.js, timeline-keyboard.js, timeline-drop.js, timeline-feedback.js. Round-8 part B: multi-layer UI — `<sg-timeline>` now renders N video lanes (top-of-UI = highest array index = top z-order) via `timeline-render.js` iterating `getVideoTracks(project)` in reverse, with per-track `.lane-row` containing a `.lane` (clips) and a sticky-left `.track-header` (label + M mute toggle + × remove, disabled when only one track remains; via `timeline-track-headers.js`); cross-track drag — pointer-Y → target-lane lookup (`timeline-lane-finder.js`) so a clip body drag can change tracks, dispatching new event `sg-timeline:clip-track-changed` `{clipId, fromTrackId, toTrackId, timelineStart}`; asset-panel drop respects the lane it lands on (`clip-added.detail.trackId`); new toolbar `+ Track` button (`timeline-track-buttons.js`) emits `sg-timeline:track-add-requested`; new events `sg-timeline:track-{add,remove,mute}-requested`; tiny state addition `setTrackMutedOp` + `api.setTrackMuted({trackId, muted})` so the M toggle drives the composer's audio rule. New helpers: timeline-lane-render.js, timeline-track-headers.js, timeline-lane-finder.js, timeline-track-buttons.js, timeline-clip-drag.js. |
| sg-preview-canvas.js | `sgraph_ai_tools__static/components/sg-preview-canvas/v0/v0.1/v0.1.0/sg-preview-canvas.js` | v0.1.0 | `SgPreviewCanvas` (Web Component) | **NEW** — Phase 1 video preview surface. Shadow DOM. Holds an inner `<canvas>` (default 1280×720; override via `width`/`height` attributes) and a transport bar with back/play/forward + time readout. Public methods: `getCanvas()`, `attachComposer(handle)`, `detachComposer()`, `setSize(w,h)`. Listens for `composer:playhead-changed` (drives time), `composer:state-changed` and `composer:ended` (drive the play/pause icon — round-5 fix). Wires transport buttons to the attached composer's `play()`/`pause()`/`seek()`. Round-7-A: 1px white-tinted outline + subtle teal halo on the canvas so the capture-area boundary is visible against the dark wrapper. Files: sg-preview-canvas.{js,css}, preview-transport.js. Round-9-B: new on-canvas overlay layer (`preview-overlay.js` + `preview-overlay-move.js` + `preview-overlay-crop.js` + `preview-overlay-utils.js`) renders 8 corner/edge handles plus a centre move handle in `'move'` mode, and a dashed crop rectangle with 8 handles + a translucent dim mask in `'crop'` mode. Overlay layer is `pointer-events: none` so clicks fall through to the canvas in `'select'` mode; only handles opt into pointer events. New public methods: `setEditorMode('select'\|'move'\|'crop')`, `setActiveClip({clipId, transform, crop, srcWidth, srcHeight}\|null)`, `setCanvasSize(w,h)` (alias of `setSize`). Drag pointermove emits `sg-preview:transform-requested` / `sg-preview:crop-requested` CustomEvents on the host. Handles only render when `setActiveClip(info)` is non-null AND mode ≠ `'select'`. Display↔canvas pixel mapping uses `canvas.getBoundingClientRect()` together with `composer-draw.computeClipDestRect`. Round-9-G: transport bar gains a fourth button (↻ "Redraw current frame", `els.redraw` returned from `buildTransport`) inserted between the fast-forward button and the time read-out. `wireTransport` wires its click to `composer.refresh()` (gracefully falls back to `seek(getCurrentTime())` against older composers). `setTransportEnabled(false)` leaves the redraw button enabled at all times — it's a cheap no-op when no composer is attached. The on-canvas behaviour is unchanged when there are no clips. |
| sg-json-viewer.js | `sgraph_ai_tools__static/components/sg-json-viewer/v0/v0.1/v0.1.0/sg-json-viewer.js` | v0.1.0 | `SgJsonViewer` (Web Component) | **NEW** — Read-only collapsible JSON tree viewer. Shadow DOM. Public methods: `setData(obj)`, `getData()`, `setExpandedDepth(n)` (default 2). Recursive render via pure `renderJson(value, opts)` helper (`json-render.js`) — objects/arrays render as `<details>` with item-count summary; primitives are colour-coded spans (string=teal, number=amber, bool=blue, null=grey, key=light blue). Monospaced 12px theme. Files: sg-json-viewer.{js,css}, json-render.js. Mounted as a sg-layout pane in sg-video-editor v0.1.54 (round-5). |

---

### NEW — vault-embed namespace (v0.1.0, 27 Apr 2026)

All five components live under `sgraph_ai_tools__static/components/vault-embed/` and form a cohesive read-only vault embed stack. **None** use `data-vault-bus`, `localStorage`, write keys, or globals. Coordination is via element ID references only.

| Component | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-vault-key | `sgraph_ai_tools__static/components/vault-embed/sg-vault-key/v0/v0.1/v0.1.0/sg-vault-key.js` | v0.1.0 | `SgVaultKey` (Web Component) | **NEW** — Headless key-import component. Reads `read-key` (base64url AES-256 key), `vault-id`, and `endpoint` attributes. Calls `importReadKey()` from vault-client v1.2.2. Public API: `getKey()` → Promise\<CryptoKey\>, `getEndpoint()`, `getVaultId()`. Emits `sg-vault-key:key-ready { vaultId, cryptoKey, endpoint, derivationMs }` and `sg-vault-key:key-error { vaultId, error }`. `:host { display:none }`. Events file: `events.js` exporting frozen `SGVK_EVENTS`. |
| sg-vault-fetch | `sgraph_ai_tools__static/components/vault-embed/sg-vault-fetch/v0/v0.1/v0.1.0/sg-vault-fetch.js` | v0.1.0 | `SgVaultFetch` (Web Component) | **NEW** — Headless fetch+decrypt component. Resolves key-source element via `document.getElementById(key-source)`, waits for KEY_READY if needed. Builds fetch URL via `fileIdToPath(objectId)`. Inline AES-256-GCM decrypt (IV_LENGTH=12 prefix). Fires 5 progress events (FETCH_STARTED, FETCH_COMPLETED, DECRYPT_STARTED, DECRYPT_COMPLETED, CONTENT_READY). Content-type sniffed via `sg-vault-fetch-content-type.js`: PNG/JPEG/GIF/WebP (magic bytes), JSON ({/[), HTML (<), UTF-8 text, binary fallback. Public API: `fetch()`, `getCurrentContent()`. Events file: `events.js` exporting frozen `SGVF_EVENTS` (6 events). `:host { display:none }`. |
| sg-vault-content | `sgraph_ai_tools__static/components/vault-embed/sg-vault-content/v0/v0.1/v0.1.0/sg-vault-content.js` | v0.1.0 | `SgVaultContent` (Web Component) | **NEW** — All-in-one vault content component. Accepts `read-key`, `vault-id`, `endpoint`, `object-id`, `render` (markdown/image/json). Creates internal sg-vault-key and sg-vault-fetch siblings (auto-generated IDs). Dynamically imports the appropriate content renderer from `components/content/`. Renderer receives `content-source` pointing at the internal fetch component. Module-level counter `_counter` ensures unique generated IDs. |
| sg-vault-manifest | `sgraph_ai_tools__static/components/vault-embed/sg-vault-manifest/v0/v0.1/v0.1.0/sg-vault-manifest.js` | v0.1.0 | `SgVaultManifest` (Web Component) | **NEW** — Manifest loader with strict security validation. Fetches `src` URL, JSON.parses, validates: version must be '1.0', write_key forbidden (recursive check), slots must have valid object_id (`/^obj-cas-imm-/`), render must be markdown/image/json. Emits MANIFEST_LOADED `{ src, slotCount }`, SLOT_READY `{ slotName, objectId, render, vaultId, readKey }` per slot, MANIFEST_ERROR `{ error }` on validation failure. Exact write_key error: `'Manifest validation error: write_key is forbidden in manifests.'`. Events file: `events.js` exporting frozen `SGVM_EVENTS` (3 events). `:host { display:none }`. |
| sg-vault-trace | `sgraph_ai_tools__static/components/vault-embed/sg-vault-trace/v0/v0.1/v0.1.0/sg-vault-trace.js` | v0.1.0 | `SgVaultTrace` (Web Component) | **NEW** — Developer introspection panel. Listens for all 11 vault embed events on `document`. Per-entry detail sanitisation: CryptoKey → `'[CryptoKey]'`, ArrayBuffer → `'[ArrayBuffer N bytes]'`. Public API: `getLog(limit?)`, `clearLog()`. `format` attribute: `compact` (single line per event) or `detailed` (expanded JSON). Renders `.sgvt-entry` rows in shadow DOM. Useful for demos and debugging without touching component internals. |

### NEW — content namespace (v0.1.0, 27 Apr 2026)

Three content renderers live under `sgraph_ai_tools__static/components/content/`. They have **zero imports** from `components/vault-embed/` or `core/vault-*` — they only depend on `SgComponent` and (for markdown) `sg-markdown.js`. They are driven by any CustomEvent with the shape `demo:content-ready` or `sg-vault-fetch:content-ready` on their `content-source` element.

| Component | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-content-markdown | `sgraph_ai_tools__static/components/content/sg-content-markdown/v0/v0.1/v0.1.0/sg-content-markdown.js` | v0.1.0 | `SgContentMarkdown` (Web Component) | **NEW** — Markdown renderer. Listens for `demo:content-ready` or `sg-vault-fetch:content-ready` on source element. Renders `detail.text` via `renderMarkdown()` into `#content` div. XSS-safe (escapeHtml is called inside renderMarkdown before parsing). Static `jsUrl = import.meta.url`. |
| sg-content-image | `sgraph_ai_tools__static/components/content/sg-content-image/v0/v0.1/v0.1.0/sg-content-image.js` | v0.1.0 | `SgContentImage` (Web Component) | **NEW** — Image renderer. Accepts `detail.bytes` (ArrayBuffer). Creates Blob URL via `URL.createObjectURL`, sets on `#img`. AC-6: `disconnectedCallback()` calls `URL.revokeObjectURL` before `super.disconnectedCallback()` to prevent memory leaks — tested with 100 mount/unmount cycles. |
| sg-content-json | `sgraph_ai_tools__static/components/content/sg-content-json/v0/v0.1/v0.1.0/sg-content-json.js` | v0.1.0 | `SgContentJson` (Web Component) | **NEW** — JSON renderer/emitter. `mode="render"`: pretty-prints JSON into `#pre` via `JSON.stringify(data, null, 2)`. `mode="emit"`: fires `sg-content-json:json-parsed { objectId, data }` on self. Events file: `events.js` exporting frozen `SGCJ_EVENTS.JSON_PARSED`. |

### NEW — sg-mermaid components v0.1.0 (02 May 2026)

Three components for creating, editing and rendering Mermaid v11 diagrams. Fully decoupled — wiring is the tool's job.

| Component | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-mermaid-render | `sgraph_ai_tools__static/components/sg-mermaid-render/v0/v0.1/v0.1.0/sg-mermaid-render.js` | v0.1.0 | `SgMermaidRender` (Web Component), `loadMermaidLib(theme)` | **NEW** — Mermaid diagram renderer. Lazy-loads Mermaid v11 from CDN on first `render()` call. Module-level shared load promise + shared SVG cache across all instances. Shadow DOM. Methods: `render(markup, {cache})`, `clear()`, `clearCache()`, `getSvg()`. Zoom toolbar (+/−/Fit, percentage label, cache badge). Fullscreen overlay (Esc to close, independent zoom). Events: `sg-mermaid:rendered { markup, svg }`, `sg-mermaid:error { markup, error }`. Attribute: `theme` (dark/default/neutral/forest/base). |
| sg-mermaid-editor | `sgraph_ai_tools__static/components/sg-mermaid-editor/v0/v0.1/v0.1.0/sg-mermaid-editor.js` | v0.1.0 | `SgMermaidEditor` (Web Component) | **NEW** — Monospace Mermaid markup editor. Shadow DOM, flex-fill textarea. Methods: `getValue()`, `setValue(text, silent?)`, `focus()`, `clear()`. Tab key inserts 2 spaces. Copy and Clear toolbar buttons with status flash. Debounced `input` → `sg-mermaid:markup-changed { markup }`. Attributes: `placeholder`, `debounce` (ms, default 600). |
| sg-mermaid-demos | `sgraph_ai_tools__static/components/sg-mermaid-demos/v0/v0.1/v0.1.0/sg-mermaid-demos.js` | v0.1.0 | `SgMermaidDemos` (Web Component) | **NEW** — Scrollable demo gallery with 8 built-in Mermaid examples: flowchart-api, sequence-login, class-domain, state-task, er-blog, gantt-sprint, mindmap-sgraph, gitgraph-feature. Shadow DOM. Methods: `getDemos()`, `selectDemo(id)`. Click → `sg-mermaid:demo-selected { id, type, label, markup }`. Active card highlighted. |

---

### NEW — sg-local-bridge v0.1.0 (13 May 2026)

Headless Web Component + pure-JS client module that bridge an LLM tool-runner to a local FastAPI bridge service (`sgraph_bridge`). Part of the `agent-with-tools` composition (Phase 2). Lives under `components/agentic/` alongside `sg-tool-runner`.

| Component | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-local-bridge-client.js | `sgraph_ai_tools__static/components/agentic/sg-local-bridge/v0/v0.1/v0.1.0/sg-local-bridge-client.js` | v0.1.0 | `BridgeError` (class), `ping()`, `readFile()`, `writeFile()`, `deleteFile()`, `listFolder()`, `runBash()`, `fetchUrl()` | **NEW** — Pure JS module (no Web Component, no UI). Typed fetch wrappers for all 6 sgraph_bridge endpoints + GET /ping. Internal `call()` helper does POST JSON, checks status, throws `BridgeError` (`.errorCode`, `.detail`, `.status`) on 4xx/5xx or network failure. All functions accept `endpoint` base URL + `timeoutMs` via AbortController. 105 lines. Named exports only. |
| sg-local-bridge.js | `sgraph_ai_tools__static/components/agentic/sg-local-bridge/v0/v0.1/v0.1.0/sg-local-bridge.js` | v0.1.0 | `SgLocalBridge` (headless Web Component) | **NEW** — Headless `<sg-local-bridge>` Web Component. Attrs: `endpoint` (default `http://localhost:8000`), `workspace` (default `/workspace`, display only), `auto-connect` (boolean), `tools` (CSV or `*`, default `*`), `timeout-ms` (default 30000). `connectedCallback` finds `[data-llm-bus]` ancestor; if `auto-connect`, calls `connect()`. `connect()` pings the bridge, fires `sg-local-bridge:status` on success (or `sg-local-bridge:error` on failure), then calls `_registerTools()`. `_registerTools()` finds `sg-tool-runner` inside the bus; if not yet available, listens for `sg-tool-runner:ready`. Registers up to 6 `lb_*` tools via `runner.register(name, fn)`: `lb_read_file`, `lb_write_file`, `lb_delete_file`, `lb_list_folder`, `lb_run_bash`, `lb_fetch_url`. Each wrapper fires `sg-local-bridge:tool-call { name, args, result, ms }` on the bus after every invocation. Bus events: `sg-local-bridge:status { ok, version, workspace, latency_ms }`, `sg-local-bridge:tool-call { name, args, result, ms }`, `sg-local-bridge:error { message, detail }`. All events: `bubbles:false, composed:false`. 182 lines. Named exports only. |
| sg-local-bridge-shim.js | `sgraph_ai_tools__static/components/agentic/sg-local-bridge/v0/v0.1/v0.1.0/sg-local-bridge-shim.js` | v0.1.0 | `normaliseToolCalls(message)`, `isJsonInContent(message)` | **NEW (P4)** — Pure JS ES module. JSON-in-content tool-call fallback for Ollama models that don't emit native `tool_calls` (e.g. `mistral:7b`, `codellama:7b`). `normaliseToolCalls` detects JSON embedded in `content` (bare or fenced), strips code-block wrappers, parses, validates `tool` key, and returns a new message with `content: null` and a native `tool_calls` array. Returns the original object unchanged if native `tool_calls` already present, content is empty/non-JSON, or parsed JSON has no `tool` key. Never mutates input. `isJsonInContent` returns true when the shim would modify the message. Maps `{tool, parameters, id?}` → `{id, type:'function', function:{name, arguments}}`. 151 lines. Named exports only. No DOM/Web Component dependency (testable in Node.js). Wired in `agent-with-tools-api.js` via `llm:send` + `llm:request-complete` event listeners. |
| sg-local-bridge-shim.test.js | `sgraph_ai_tools__static/components/agentic/sg-local-bridge/v0/v0.1/v0.1.0/sg-local-bridge-shim.test.js` | v0.1.0 | — | **NEW (P4)** — Plain Node.js test file (no test runner). 14 test cases covering: native tool_calls passthrough, single JSON object, JSON array of two calls, fenced code blocks (bare + prose prefix), plain text passthrough, JSON without `tool` key, and all 6 `isJsonInContent` variants. Run with `node sg-local-bridge-shim.test.js`. All 14 cases pass. |
| demo.html | `sgraph_ai_tools__static/components/agentic/sg-local-bridge/v0/v0.1/v0.1.0/demo.html` | v0.1.0 | — | **NEW** — Standalone developer test page. No external dependencies. Inline stub `sg-tool-runner` with `register(name,fn)` + `execute(name,args)`. Mounts `<div data-llm-bus>` with `<sg-tool-runner id="runner">` + `<sg-local-bridge auto-connect>`. Shows bridge status dot (grey→green/red). Lists registered tool names as clickable buttons with hardcoded test args. Result box shows last invocation JSON + `sg-local-bridge:tool-call` event log lines. |

---

### NEW — sg-heic v0.1.0 (15 May 2026)

Shared client-side HEIC/HEIF decode primitive extracted from the v0.1.58 probe
(`tools/v0/v0.1/v0.1.58/en-gb/heic-decode/heic-probe.js`). Required by the
`heic-converter` tool at v0.1.59 and will be re-used by the upcoming
`photo-pack` and `google-photos-connector` tools (Architect plan v0.2.58).

| Module | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-heic.js | `sgraph_ai_tools__static/core/sg-heic/v0/v0.1/v0.1.0/sg-heic.js` | v0.1.0 | `isHeic(file)`, `decodeHeicToCanvas(file)`, `decodeHeicToBlob(file, {format, quality})` | **NEW** — Lazy-loads heic-to (primary) from CDN, falls back to libheif-js if heic-to refuses to load. Module-level cache so a session pays the ~2.7MB load cost once. Returns real DOM canvas elements so callers can hand off to `core/image/sg-image.js` for resize / re-encode. `isHeic` sniffs by extension + MIME (no `ftyp` byte sniff — documented limitation). Pinned CDN URLs: heic-to@1.4.2, libheif-js@1.18.2. Pure ES module, no UI, no build step. |

---

### NEW — sg-video-frames v0.1.0 (25 May 2026)

Shared client-side first-frame extraction primitive. Required by `heic-converter`
v0.2.0 (folder-batch video → still) and partially delivers the §5 "video
still-frame extraction" net-new item of Architect plan v0.2.58 (the
single-still half; thumbnail-strip generation is still to come).

| Module | Path | Version | Exports | Status |
|---|---|---|---|---|
| sg-video-frames.js | `sgraph_ai_tools__static/core/sg-video-frames/v0/v0.1/v0.1.0/sg-video-frames.js` | v0.1.0 | `isVideoFile(file)`, `extractFirstFrame(file, {at, format, quality, onProgress})` | **NEW** — Extracts a single still from a video as a metadata-free image Blob. Fast path: a native `<video>` element seeks + draws to a canvas (works for web-friendly mp4; browser applies rotation before drawImage). Fallback: FFmpeg WASM `-ss <at> -frames:v 1 -update 1 frame.png` (autorotate left ON for iPhone rotation) re-drawn to a canvas + re-encoded — used for HEVC `.mov` which the `<video>` element can't decode on most desktops. Fast path times out at 8s, then falls through. Both paths end in a canvas/FFmpeg re-encode → no source metadata (no EXIF/GPS) survives. `isVideoFile` sniffs ext (mp4/mov/m4v/qt) + MIME. Imports `loadFFmpeg` from `core/video v1.0.1` and `exportImage` from `core/image v1.0.0`. NOTE: FFmpeg-path orientation correctness is unverified without a browser — flagged as a follow-up. Pure ES module, no build step. |

---

### NEW — core patch: vault-client v1.2.2 (27 Apr 2026)

| Module | Path | Version | New Exports | Status |
|---|---|---|---|---|
| sg-vault-client.js | `sgraph_ai_tools__static/core/vault-client/v1/v1.2/v1.2.2/sg-vault-client.js` | v1.2.2 | All v1.2.1 exports + `importReadKey(readKeyBase64Url)` → Promise\<CryptoKey\>, `readObject(apiBaseUrl, vaultId, objectId, cryptoKey)` → Promise\<ArrayBuffer\> | **NEW** — Additive patch on v1.2.1. `importReadKey` validates 32-byte AES-256-GCM key from base64url; rejects on wrong length or malformed base64url. `readObject` fetches and decrypts a single content-addressed object via the vault read API. Private `base64UrlToBytes()` helper added. Used by vault-embed components. |

