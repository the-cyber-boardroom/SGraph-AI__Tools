# Debrief: sg-llm Component Family — Foundation (Phases 1–4)

**Version:** v0.19.11 (matches architect brief)
**Date:** 31 March 2026
**From:** Dev (Explorer team)
**Branch:** `claude/start-explorer-team-qoeFw`
**Session type:** Implementation — foundation phases of sg-llm component family
**Status:** Phases 1–4 complete, committed and pushed. Phases 5–14 ready to continue.

---

## Context

This session implemented the foundation of the **sg-llm component family** as specified in:

- **Architecture brief:** `team/humans/dinis_cruz/briefs/03/30/v0.19.11__architect-review__sg-llm-reality-constructor.md`
- **IFD convention:** `library/development/ifd/v1.3.0__ifd__surgical-overrides-version-stamped-filenames.md`

The component family lives under `sgraph_ai_tools__static/components/llm/` — a namespace folder. Each component has independent IFD versioning.

The workspace source files for Phases 9–10 (bundle system) were cloned from `the-cyber-boardroom/SGraph-AI__App__Send` into `/tmp/sg-send-repo/` and read during the session. The relevant files are:

- `sgraph_ai_app_send__ui__workspace/v0/v0.1/v0.1.1/components/bundle-manager/bundle-manager.js` (607 lines)
- `sgraph_ai_app_send__ui__workspace/v0/v0.1/v0.1.1/components/bundle-list/bundle-list.js` (353 lines)

---

## What Was Built (Phases 1–4)

### Phase 1 — `sg-llm-events` v0.1.0

**Path:** `components/llm/sg-llm-events/v0/v0.1/v0.1.0/`

The shared frozen event contract for the entire component family. 34 event name constants in a `SGL_LLM` object (frozen). Groups:

- Connection: `CONNECTED`, `DISCONNECTED`, `MODEL_CHANGED`, `MODELS_LOADED`
- Reality: `REALITY_CHANGED`, `BLOCK_ADDED`, `BLOCK_REMOVED`, `BLOCK_REORDERED`
- Request lifecycle: `SEND`, `CANCEL`, `REQUEST_START`, `REQUEST_CHUNK`, `REQUEST_COMPLETE`, `REQUEST_ERROR`, `REQUEST_CANCEL`
- Output: `OUTPUT_READY`, `OUTPUT_COPIED`
- Stats: `STREAMING_CHANGED`
- Infographic: `INFOGRAPHIC_READY`, `INFOGRAPHIC_EXPORTED`
- Memory: `MEMORY_UPDATED`
- Attachments: `ATTACHMENT_ADDED`, `ATTACHMENT_REMOVED`, `ATTACHMENTS_CLEARED`
- Bundles: `BUNDLE_SAVE_REQUESTED`, `BUNDLE_SAVED`, `BUNDLE_LOAD_REQUESTED`, `BUNDLE_LOADED`, `BUNDLE_DELETED`, `BUNDLE_LIST_CHANGED`

Also exposes `window.SGL_LLM` for surgical overrides. Frozen — additions only.

---

### Phase 2 — `sg-llm-request` v0.1.0

**Path:** `components/llm/sg-llm-request/v0/v0.1/v0.1.0/`
**Test:** `tests/sg-llm/request.test.html`

Headless fetch engine. The ONLY component that calls external LLM APIs. Key design decisions:

- **Dumb pipe** — receives the messages array exactly as built by sg-llm-reality, does NOT modify it
- **4 providers:** OpenRouter, Ollama, Anthropic, OpenAI — each with its own header/body builder
- **Multimodal handling:** detects `Array.isArray(message.content)`, transforms per provider:
  - OpenAI/OpenRouter: pass through as content arrays
  - Anthropic: convert `image_url` → `{ type: 'image', source: { type: 'base64', ... } }`
  - Ollama: text parts joined, images extracted as base64 array
- **Streaming:** SSE line-by-line reader, emits `REQUEST_CHUNK` per delta
- **Cancel:** AbortController, emits `REQUEST_CANCEL`
- **Security:** never sends API keys to `*.sgraph.ai`
- **Event scope:** listens on `this.parentElement || document` — scoped to the component's container

Test page covers: custom element registration, `.busy` starts false, mock non-streaming request (start→complete), 401 error handling, AbortController cancel.

---

### Phase 3 — `sg-llm-connection` v0.1.0

**Path:** `components/llm/sg-llm-connection/v0/v0.1/v0.1.0/`
**Test:** `tests/sg-llm/connection.test.html`

Provider configuration panel. Uses Shadow DOM with inline styles (no separate CSS file — simpler for this self-contained component). Key design decisions:

- **No SgComponent base** — written as plain `HTMLElement` with `attachShadow({ mode: 'open' })` and inline template/styles, matching the pattern of sg-image-editor
- **4 provider tabs:** OpenRouter, Ollama (no-key), Anthropic (builtin model list), OpenAI
- **Model loading:** fetches from provider API (OpenAI-compat `/v1/models` or Ollama `/api/tags`), falls back to builtin list for Anthropic
- **localStorage persistence:** saves `{ provider, apiKey, baseUrl, model, favourites }` under `'sg-llm-config'`
- **Emits on connect:** `CONNECTED` with `{ provider, model, baseUrl, apiKey }` — this is what `sg-llm-request` listens to
- **Key show/hide toggle**, refresh models button

---

### Phase 4 — `sg-llm-output` v0.1.0

**Path:** `components/llm/sg-llm-output/v0/v0.1/v0.1.0/`
**Test:** `tests/sg-llm/output.test.html`

Streaming response display. Key design decisions:

- **During streaming:** renders plain text only (fast, avoids repeated markdown parse cost), with animated cursor
- **After complete:** renders markdown (toggleable) or plain text
- **Markdown renderer:** built-in, no external deps. Handles: h1-h3, bold/italic, fenced code blocks (language-tagged), inline code, blockquotes, ul/ol lists, tables, HR, links. XSS-safe (HTML-escapes before processing, only inserts trusted HTML from renderer)
- **Footer:** shows token counts + latency from `REQUEST_COMPLETE` event
- **`BUNDLE_LOADED`:** restores `bundle.response.text` into the display
- **Public setter:** `output.content = text` for programmatic use in tests and tool pages

Test page covers: start (waiting state), streaming chunks, complete (OUTPUT_READY fired), error display, programmatic content set, markdown renders h2, BUNDLE_LOADED restore.

---

## Architecture Decisions Made

### Event scoping (important)

All components listen on `this.parentElement || document` — NOT on `document` directly. This means a common ancestor div (or sg-layout panel) acts as the event bus for each LLM instance. Two separate LLM instances on the same page won't leak events between each other. The tool page is responsible for placing all components inside a shared container.

### No SgComponent base for these components

The sg-llm components use plain `HTMLElement` with `attachShadow({ mode: 'open' })` and inline styles/templates rather than extending `SgComponent`. Reason: sg-llm components don't need the HTTP-fetch-based resource loading pattern (no `.html`/`.css` sibling files to fetch). Inline is simpler and more robust for these headless or self-contained components.

### Headless components (request, bundle)

`sg-llm-request` is a true headless custom element — no shadow DOM, no UI. It's placed on the page (inside the shared container) and communicates entirely via events. Same will apply to `sg-llm-bundle`.

---

## What Was Cloned (for Phase 9 reference)

The workspace bundle-manager source is now understood. The adaptation plan for `sg-llm-bundle`:

| Workspace pattern | sg-llm-bundle adaptation |
|---|---|
| `window.sgraphWorkspace.events.on/emit` | DOM CustomEvents on container |
| Vault storage (`vault.addFile`) | `localStorage` (MVP) |
| `document.querySelector('llm-connection')` for state capture | Listen to `REALITY_CHANGED`, `CONNECTED`, `REQUEST_COMPLETE` events |
| `document.querySelector('bundle-manager')` | `this.parentElement.querySelector('sg-llm-bundle')` |
| Workspace-specific panels (document-viewer, script-editor) | Strip out — capture `blocks[]` from REALITY_CHANGED detail |

The key data structure from the workspace that maps cleanly to sg-llm:

```js
{
    id:        'bundle-20260331-143022-a1b2',
    timestamp: '2026-03-31T14:30:22.000Z',
    parent_id: 'bundle-...',
    config:    { provider, model, streaming },
    reality:   { blocks: [...] },   // from sg-llm-reality REALITY_CHANGED
    response:  { text, tokens: { prompt, completion, total }, cost, duration_ms, finish_reason },
}
```

---

## What Remains (Phases 5–14)

### Phase 5 — `sg-llm-reality` v0.1.0 ← **NEXT — the unlock**

The central component. Build the reality constructor: a vertical list of blocks (system, context, history, memory, question) with:

- Add/remove/reorder blocks (drag to reorder)
- Per-block editors (textarea for each type; history has paired user/assistant textareas)
- Per-block token count (`text.length / 4`)
- Toggle blocks on/off (excluded from messages array but kept in reality)
- `SEND` event assembles the full messages array (Mode 1: Build)
- `SEND` event for memory update (Mode 2) — different messages array
- `ATTACHMENT_ADDED` (text) → auto-creates Context block
- `ATTACHMENT_ADDED` (image) → tracked for multimodal Question content
- `BUNDLE_LOADED` → restores all blocks
- Live cost estimate (from MODEL_CHANGED pricing — can start as static estimate)
- Templates: pre-built block sets for common tasks

**Path to create:** `components/llm/sg-llm-reality/v0/v0.1/v0.1.0/sg-llm-reality.js`
**Test:** `tests/sg-llm/reality.test.html`

### Phase 6 — `sg-llm-attachments` v0.1.0

Drag-drop zone, clipboard paste (Ctrl+V text + screenshots), file picker. Owns the attachment cache. Text files → `ATTACHMENT_ADDED` with `textContent`. Images → `ATTACHMENT_ADDED` with `dataUrl`. Thumbnails for images, text previews for files.

**Path:** `components/llm/sg-llm-attachments/v0/v0.1/v0.1.0/`

### Phase 7 — `sg-llm-stats` v0.1.0

Token accounting panel. Last request in/out tokens, cost, speed (tok/s), latency. Session totals. Streaming toggle. Reads `REQUEST_START`, `REQUEST_COMPLETE`, `REALITY_CHANGED`.

**Path:** `components/llm/sg-llm-stats/v0/v0.1/v0.1.0/`

### Phase 8 — `sg-llm-debug` v0.1.0

Full request inspector. Timestamped log of `SEND` events. Click entry to see the complete messages array — each block labelled and colour-coded by type (system=blue, context=green, memory=cyan, history=amber, question=purple). Shows raw response. Pure observer (no emits).

**Path:** `components/llm/sg-llm-debug/v0/v0.1/v0.1.0/`

### Phase 9 — `sg-llm-bundle` v0.1.0

Headless bundle orchestrator. Adapted from workspace bundle-manager.js. Key changes:
- Use DOM CustomEvents instead of `window.sgraphWorkspace.events`
- Use localStorage instead of vault
- `captureState()` reads from events (tracks `REALITY_CHANGED` detail for blocks, `CONNECTED` for config, `REQUEST_COMPLETE` for response)
- `parent_id` fork tree
- Auto-save on `REQUEST_COMPLETE` (optional, off by default)
- Tree index in localStorage key `'sg-llm-bundles-index'`

**Path:** `components/llm/sg-llm-bundle/v0/v0.1/v0.1.0/`

### Phase 10 — `sg-llm-bundle-list` v0.1.0

Visual bundle browser. Adapted from workspace bundle-list.js. Key changes:
- DOM CustomEvents, Shadow DOM
- Same UI: reverse-chronological, timestamp/model/prompt-preview, active highlight, click to load, rename (inline input), delete

**Path:** `components/llm/sg-llm-bundle-list/v0/v0.1/v0.1.0/`

### Phase 11 — `sg-llm-infographic` v0.1.0

SVG extraction + rendering. Listens to `REQUEST_CHUNK` for live preview, `REQUEST_COMPLETE` for final extraction. Style preset selector (executive/technical/playful/minimal). Export to SVG/PNG.

**Path:** `components/llm/sg-llm-infographic/v0/v0.1/v0.1.0/`

### Phase 12 — Tool: One-Shot Chat

**Path:** `tools/v0/v0.1/v0.1.6/en-gb/llm-chat/index.html`

Thin tool page composing: connection + request + reality + output + stats + debug + bundle + bundle-list via sg-layout. All events scoped to one container div.

### Phase 13 — Tool: Infographic Generator

**Path:** `tools/v0/v0.1/v0.1.6/en-gb/llm-infographic/index.html`

Thin tool page composing: connection + request + reality + output + infographic + stats via sg-layout. Reality pre-configured with `infographic-executive` template.

### Phase 14 — Tool: LLM Dev Environment

**Path:** `tools/v0/v0.1/v0.1.6/en-gb/llm-dev-env/index.html`

The primary consumer. Adds a code editor textarea + iframe live preview alongside the sg-llm components. The iframe loads component files from tools.sgraph.ai CDN. Full bundle+memory loop for iterative component development.

### Phase 15 (unlisted) — Update reality document

After all phases complete, update `team/explorer/librarian/reality/v0.1.0__what-exists-today.md` with all new components and tool pages. Create v0.1.6 section.

---

## Files On Branch (committed)

```
sgraph_ai_tools__static/
  components/llm/
    sg-llm-events/v0/v0.1/v0.1.0/
      sg-llm-events.js
      manifest.json
    sg-llm-request/v0/v0.1/v0.1.0/
      sg-llm-request.js
      manifest.json
    sg-llm-connection/v0/v0.1/v0.1.0/
      sg-llm-connection.js
      manifest.json
    sg-llm-output/v0/v0.1/v0.1.0/
      sg-llm-output.js
      manifest.json
  tests/sg-llm/
    request.test.html
    connection.test.html
    output.test.html
```

---

## How to Continue in a New Session

1. **Read:** `.claude/CLAUDE.md` and `.claude/explorer/CLAUDE.md` (mandatory per project rules)
2. **Read:** `team/explorer/librarian/reality/v0.1.0__what-exists-today.md` (reality check)
3. **Read this debrief** (you're reading it)
4. **Check out branch:** `git checkout claude/start-explorer-team-qoeFw`
5. **Start with Phase 5** (`sg-llm-reality`) — it's the unlock
6. **Note:** workspace bundle source is at `/tmp/sg-send-repo/` only if the same machine — otherwise re-clone: `the-cyber-boardroom/SGraph-AI__App__Send`, path `sgraph_ai_app_send__ui__workspace/v0/v0.1/v0.1.1/components/`

### Immediate next command to give:

> "Continue Phase 5 — build sg-llm-reality. It's the unlock: system + context + history + memory + question blocks, drag reorder, token counts, two send modes, attachment awareness, bundle restore."

---

## Lessons Learned

1. **Event scoping is the key architectural decision.** Listening on `parentElement` instead of `document` makes multi-instance LLM tools possible with zero extra code. Establish this early (done).

2. **The workspace bundle-manager is mostly workspace-specific plumbing.** The core logic (`captureState`, `_updateTreeIndex`, `parent_id` tracking) maps cleanly to the new pattern. The vault storage → localStorage swap is trivial. The state capture DOM queries → event tracking is the main rewrite.

3. **Inline styles + templates work better than fetched .html/.css for these components.** No network round-trips, no 404 errors in local dev, simpler testing. Reserve the SgComponent fetch pattern for components that genuinely need it (site-header, etc.).

4. **The IFD v1.3.0 double-dash convention is ready to use.** When surgical overrides are needed (e.g. streaming SSE for sg-llm-request--v0.1.1.js, tiktoken for better token estimates), the pattern is established and documented.

5. **Test pages are the first tool.** Each `reality.test.html` is a working mini-LLM client even before the formal tool pages exist. Build the test pages first — they prove the component works end-to-end.

---

*Explorer team — Dev role*
*31 March 2026*
