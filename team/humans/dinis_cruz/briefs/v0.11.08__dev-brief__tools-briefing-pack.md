# Dev Brief: Briefing Pack for sgraph_ai__tools — Session Bootstrap System

**version** v0.11.08  
**date** 5 Mar 2026  
**from** Human (project lead)  
**to** Librarian (lead), Developer, Architect  
**type** Dev brief — session bootstrapping, knowledge transfer, repo setup  

---

## What Is a Briefing Pack?

When spinning up a new Claude Code session to work on any SGraph repo, the session needs everything to be productive immediately. It has no memory of previous sessions. It doesn't know the team roles, the coding conventions, the architecture decisions, or what's been built. The **briefing pack** is a self-contained document that bootstraps a new session from zero to fully productive in one read.

The Librarian creates and maintains the briefing pack. Every session contributes back to it. The briefing pack IS the persistent memory of the project.

---

## Why This Is a Separate Document

The briefing pack for `sgraph_ai__tools` must reference decisions and architecture from multiple days and sessions:

**From 4 March (v0.11.1):**
- HTML Workbench and JavaScript Transformation Pipeline — the `sg-llm.js` module that the LLM client tool will extract from
- Workspace plugin architecture — the plugin model that tools follow
- Git-Like Encrypted Object Store — the versioning model that informs folder-based module versioning
- Chrome Extension — the `sg-send.js`, `sg-crypto.js` modules that the extension will import from tools.sgraph.ai

**From 5 March (v0.11.08):**
- [tools.sgraph.ai — Video Splitter and Tool Platform](v0.11.08__dev-brief__tools-sgraph-video-splitter.md) — the first standalone tool specification
- [tools.sgraph.ai — Modular Architecture, Repo, and Briefing Pack](v0.11.08__dev-brief__tools-modular-architecture-and-repo.md) — the repo structure, module library, MVP tools list, and session bootstrap flow
- [tools.sgraph.ai as the Canonical Component Library](v0.11.08__arch-brief__tools-canonical-component-library.md) — the dependency inversion architecture, three-tier structure, versioning model, migration path, and CDN serving strategy

The briefing pack synthesises all of this into a single document that a new session reads first.

---

## Contents of BRIEF_PACK.md

The briefing pack for `sgraph_ai__tools` must contain the following sections. The Librarian is responsible for keeping every section current.

### 1. Project Overview

What tools.sgraph.ai is, in three paragraphs:
- The canonical component library for the entire SGraph ecosystem
- Three tiers: `core/` (pure JS logic), `components/` (reusable UI), `tools/` (standalone pages)
- Dependency direction: send, vault, workspace, and extension all import FROM tools.sgraph.ai

Include the ecosystem diagram from the architecture brief showing how all projects import from tools.sgraph.ai.

### 2. Architecture Decisions

Summarise the key decisions with links to the full briefs:

| Decision | Brief | Date |
|---|---|---|
| tools.sgraph.ai is the canonical source, not a consumer | Canonical Component Library (v0.11.08) | 5 Mar |
| Three-tier structure: core, components, tools | Canonical Component Library (v0.11.08) | 5 Mar |
| Folder-based versioning with pinned and latest | Canonical Component Library (v0.11.08) | 5 Mar |
| Each module has independent IFD pipeline | Modular Architecture and Repo (v0.11.08) | 5 Mar |
| Tools are mashups of core + components + tool-specific UI | Modular Architecture and Repo (v0.11.08) | 5 Mar |
| Module extraction from workspace for sg-llm.js | JS Transformation Pipeline (v0.11.1) | 4 Mar |
| Plugin architecture for workspace modules | Scalable Classification Hash Caching (v0.11.1) | 4 Mar |
| Chrome extension imports crypto from tools.sgraph.ai | Chrome Extension Key Vault (v0.11.1) | 4 Mar |

### 3. Team Roles

All agent roles active for this repo:

| Role | Responsibility in this repo |
|---|---|
| **Developer** | Build tools, extract modules, write tests, implement CI/CD |
| **Architect** | Module API design, dependency management, versioning strategy |
| **Librarian** | Maintain BRIEF_PACK.md, document module APIs, track what exists |
| **Designer** | Consistent tool UX, shared styling, landing page design |
| **DevOps** | CI/CD pipelines per module, S3 deployment, CloudFront config, cache headers |
| **Explorer** | Prototype new tools, test FFmpeg WASM, test Whisper WASM, experiment |

Include the role definitions — what each role does, how they interact, the decision-making hierarchy.

### 4. Coding Conventions

- **Vanilla JS only.** No React, no Vue, no frameworks. Pure HTML + CSS + ES modules.
- **ES modules.** All JS uses `import`/`export`. No CommonJS, no require().
- **Web components optional.** Components MAY use Custom Elements (`class MyComponent extends HTMLElement`) but this is not required.
- **No build step.** Every file is deployable as-is. No webpack, no bundler, no transpiler.
- **File naming.** Lowercase, hyphens: `sg-video.js`, `upload-dropzone.js`, `tools-common.css`.
- **Module exports.** Every module exports named functions/classes. No default exports (they're harder to tree-shake and harder to document).
- **JSDoc comments.** Every exported function has a JSDoc comment with parameter types and return type.
- **No localStorage.** Browser storage APIs are not supported in some contexts (Claude.ai artifacts). Use in-memory state. Exception: tools that explicitly need persistence (and document it).

### 5. Repo Structure

The full folder structure from the architecture brief, with annotations explaining each folder and file. Include the three-tier diagram.

### 6. Existing Modules and Tools

A table of everything that exists, where the code is, what version it's at, and what it exports:

| Module | Location | Version | Key Exports | Status |
|---|---|---|---|---|
| `sg-send.js` | `core/crypto/` | TBD | `encrypt()`, `decrypt()`, `generateKey()` | To extract from send repo |
| `sg-llm.js` | `core/llm-client/` | TBD | `queryLLM()`, `parseResponse()` | To extract from workspace |
| `sg-ssh.js` | `core/ssh/` | TBD | `generateSSHKeyPair()` | To extract from existing tool |
| `sg-video.js` | `core/video/` | TBD | `splitVideo()`, `extractAudio()` | New — build from scratch |
| etc. | | | | |

This table is the module registry. The Librarian updates it every time a module is added, modified, or versioned.

### 7. Current Briefs

Links to all relevant briefing documents, from all sessions, with a one-line summary of each:

**5 March (v0.11.08):**
- Video Splitter — FFmpeg WASM browser tool specification
- Modular Architecture — repo structure, module library, MVP tools, session bootstrap
- Canonical Component Library — dependency inversion, three tiers, versioning, migration
- Billing Models — pricing experiments (relevant for tools that bridge to SG/Send)
- User Feedback — real user testing (relevant for tool UX priorities)

**4 March (v0.11.1):**
- HTML Workbench — workspace where sg-llm.js was first used
- JS Transformation Pipeline — the code-behind pattern, plugin model
- Scalable Classification — workspace plugins architecture
- Chrome Extension — will import modules from tools.sgraph.ai
- (Plus all other v0.11.1 briefs as context)

**3 March (v0.10.30):**
- Data Room Enhancements — parallel viewer architecture (tools may eventually become viewers)
- Strategic Signals — billing primitives, file viewers as markets

### 8. First Task

The specific task for the first session working on this repo. Currently:

**Task:** Set up the repo, deploy the infrastructure, and build the first tools.

**Steps:**
1. Create `sgraph_ai__tools` repo with the three-tier folder structure
2. Set up CI/CD pipeline (GitHub Actions → S3 → CloudFront)
3. Deploy tools.sgraph.ai with the landing page
4. Migrate the SSH Key Generator to `tools/ssh-keygen/`
5. Extract `crypto.js` from send repo as the first `core/` module
6. Build the Video Splitter tool (`tools/video-splitter/`)
7. Verify CDN imports work: another page can `import { encrypt } from 'https://tools.sgraph.ai/core/crypto/v1.0.0/crypto.js'`

**Definition of done:** tools.sgraph.ai is live with a landing page, the SSH Key Generator works, the Video Splitter works, and at least one core module is importable via CDN URL by an external page.

### 9. Deployment Instructions

Step-by-step deployment guide:
- How to deploy locally (static file server for testing)
- How the CI/CD pipeline works
- How to deploy a new module version
- How to update the `latest` alias
- How to add a new tool
- S3 bucket configuration
- CloudFront distribution settings
- Cache header rules (immutable for pinned, 5-min for latest)

### 10. Bootstrap Script

Commands to set up a development environment:

```bash
# Clone the repo
git clone [repo-url] sgraph_ai__tools
cd sgraph_ai__tools

# No dependencies to install — it's all vanilla JS and static files

# Start a local server for testing
python3 -m http.server 8080
# or
npx serve .

# Open in browser
open http://localhost:8080/tools/
```

---

## Session Bootstrap Flow

```
1. Create new Claude Code session
2. Point it at the sgraph_ai__tools repo
3. First message: "Read briefs/BRIEF_PACK.md and set up the team"
4. Session reads the briefing pack
5. Session understands: roles, conventions, architecture, what exists, what to build
6. Session begins working on the defined first task
7. At end of session: session updates BRIEF_PACK.md with new decisions, new modules, new learnings
```

Step 7 is critical. Every session must contribute back to the briefing pack. The Librarian ensures this happens. Without it, the next session starts with stale information.

---

## The Librarian's Checklist

Before every new session, the Librarian verifies:

- [ ] BRIEF_PACK.md is up to date
- [ ] Module registry table reflects current state (all modules, correct versions)
- [ ] All recent briefs are linked (not just from the tools sessions — from ALL sessions)
- [ ] Architecture decisions table includes any new decisions
- [ ] First task is defined and scoped for the next session
- [ ] Deployment instructions are current (any infrastructure changes reflected)
- [ ] Coding conventions are current (any new conventions added)
- [ ] Known issues / bugs are listed (so the next session doesn't rediscover them)

---

## Acceptance Criteria

| # | Criterion |
|---|---|
| 1 | `briefs/BRIEF_PACK.md` exists in the repo with all 10 sections populated |
| 2 | A new Claude Code session can read BRIEF_PACK.md and begin the first task without any additional context |
| 3 | Module registry table lists all existing and planned modules with status |
| 4 | Architecture decisions reference the correct source briefs with dates |
| 5 | First task is specific, scoped, and has a clear definition of done |
| 6 | Deployment instructions are verified (someone has followed them and they work) |
| 7 | Librarian checklist is followed before every new session |

---

This document is released under the Creative Commons Attribution 4.0 International licence (CC BY 4.0).
